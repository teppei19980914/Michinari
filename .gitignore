# ローカル限定の個人設定（環境固有パス・権限を含むため共有しない）
.claude/settings.local.json

# OS/エディタ
.DS_Store
Thumbs.db
*.swp

# Security (added by ClaudeCodeTemplate)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.yaml
secrets.yml
id_rsa
id_rsa.pub
.claude/.git-automation-setup-done

# Python (backend)
myvenv/
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# データベース・バックアップ（実データを誤ってコミットしないため）
data/*.db
data/*.db-journal
backend/tests/_test.db

# Node（フロントエンド、Phase 6以降）
node_modules/
frontend/dist/

# 配布パッケージのビルド成果物（PyInstaller、OPERATIONS.md 7.4参照。build_package.pyで再生成可能）
backend/build/
backend/dist/
backend/Michinari.spec
