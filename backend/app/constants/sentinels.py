"""サービス層で「未指定」と「明示的なNULLクリア」を区別するための番兵。

API層は PATCH のリクエストを `model_dump(exclude_unset=True)` で展開してサービス層へ渡す。
このとき引数が届かない＝利用者が触れていない、`None` が届く＝利用者が値を消した、という
2つの意味を区別する必要がある。既定値を `None` にすると後者を表現できず、NULL許容列を
空へ戻す操作が黙って無視される。

NULL許容列を更新するサービス関数の既定値としてのみ用いる（NOT NULL列は `None` が
「未指定」以外の意味を持たないため、従来どおり `is not None` で判定してよい）。
"""

from typing import Any

#: 「引数が渡されなかった」ことを表す唯一の番兵（同一オブジェクトの同一性で判定する）。
UNSET: Any = object()
