# ミチナリ（Michinari）実装フェーズ分割計画書

| 項目 | 内容 |
| --- | --- |
| 文書名 | 実装フェーズ分割計画書 |
| バージョン | 1.1 |
| 作成日 | 2026年8月22日 |
| 対象 | Claude Code への段階的引き渡し |
| 関連文書 | 要件定義書 v1.1、仕様書 v1.1、設計書 データ構造編 v1.1、設計書 ロジック・プロンプト編 v1.1、CLAUDE.md、運用設計ルール |

### 改訂履歴

| 版 | 日付 | 変更内容 |
| --- | --- | --- |
| 1.0 | 2026-08-22 | 初版 |
| 1.1 | 2026-08-22 | AI基盤の疎通確認は他アプリで実施済みのため事前準備から除外し、実装後の検証工程へ移動。Phase 0 を廃止し Phase 1 から開始する構成に変更。周回対応およびナレッジエクスポートの実装内容を反映。Phase 5 にAI基盤の実仕様に基づく実装要件を追記 |
| 1.1（改） | 2026-08-23 | NewtonX ADK（v0.10.5）を実導入し、ソースコード確認・実機テスト（アシスタント9種中8種で送受信試行）を実施。(1)親メッセージ順序が実装コードでは機能しないことを確認し、対話履歴の全文注入方式へ設計変更、(2)認証方式をPAT主経路に変更（ブラウザOAuthはフォールバックへ降格）、(3)タイムアウト専用例外が存在しないことを確認しエラー判定方法を追記、(4)GPT-4o系2アシスタントのみ送信不可であることを特定し、動作確認済みの7アシスタントから用途別の既定アシスタントを選定（`ai.assistant_uid.*`）。着手前チェックリストを実態に合わせて更新 |
| 1.1（改2） | 2026-08-31 | 目標種別「読書」（記録・活用型）の追加スコープとして Phase 13〜18 を新設（Phase 1〜12は無改変）。Phase 13（設計文書反映）は本改訂と同時に完了し、要件定義書・仕様書・設計書データ構造編・設計書ロジック・プロンプト編へ反映済み。次はPhase 14（読書データモデル拡張）から着手する |
| 1.1（改3） | 2026-08-31 | Phase 14（読書データモデル拡張）完了。`GoalCategory`enum・`goal.category`列（マイグレーション`b7a1021fff73`）・`book`／`reading_log`テーブル・読書用`prompt_template`／`app_setting`初期データを実装。バックエンド全602テスト・カバレッジ100%・ruff検査通過。既存の資格試験関連コード・テストは無改変（データ移行の影響を受けた既存テストのraw SQL INSERT文への`category`列追加のみ実施）。次はPhase 15（読書目標・記録API）から着手する |
| 1.1（改4） | 2026-08-31 | Phase 15（読書目標・記録API）完了。`book_service.py`・`api/books.py`を新設し、書籍CRUD・読了処理（`POST /books/{id}/complete`）・日次記録APIの`reading_log`対応を実装。設計変更として`GET /goals/{id}/plan`は実装せず教材と同じ「Readスキーマへの埋め込み」方式に統一（データ構造編1.1（改5）参照）。既存の資格試験用AI機能（Phase5・Phase10実装済み）が読書目標データを誤って取り込まないための回帰防止ガードを追加（`list_active_exam_goals`新設、`generate_retrospective`のREADING拒否）。バックエンド全644テスト・カバレッジ100%・ruff検査通過。次はPhase 16（読書AI連携）から着手する |
| 1.1（改5） | 2026-08-31 | Phase 16（読書AI連携）完了。`reading_feedback_service.py`を新設しDAILY_FEEDBACK_READINGを実装、`retrospective_service.py`をEXAM/READING分岐に変更しGOAL_RETROSPECTIVE_READINGを実装（Phase15の拒否ガードを解消）。設計変更として`chat_message.purpose`列を追加（マイグレーション`9723ab049ecd`）し資格試験・読書の対話履歴混入を防止、AI対話エンドポイントは`POST /records/{date}/chat`と`/reading-chat`に分離。読書用アシスタントUID・想起注入日数を設定APIから変更可能にした。バックエンド全672テスト・カバレッジ100%・ruff検査通過。次はPhase 17（読書フロントエンド）から着手する |
| 1.1（改6） | 2026-08-31 | Phase 17（読書フロントエンド）完了。目標作成の種別選択、`GoalDetailPage`のcategory別タブ構成、`BookTab`・`ReadingLogFields`・`ReadingLogSummaryList`を新設。読了レポートは既存の`KnowledgeExportPage`を無改修で流用。設計変更として、DSH-06実現のため`GET /dashboard`（`GoalCardRead`）に`category`/`book`フィールドを追加する小規模なバックエンド拡張を実施（当初計画はフロントエンドのみを想定）。実行環境の制約によりヘッドレスブラウザでの実画面確認は見送り、自動テスト（バックエンド673件・フロントエンド102件・tsc・lint）で代替した（詳細はPhase17本文参照）。次はPhase 18（読書機能 実環境検証）で実画面を確認する |
| 1.1（改7） | 2026-09-03 | 目標種別「仕事」（記録・活用型の副分類「定期報告型」）の追加スコープとして Phase 19〜24 を新設（Phase 1〜18は無改変）。Phase 19（設計文書反映）は本改訂と同時に完了し、要件定義書・仕様書・設計書データ構造編・設計書ロジック・プロンプト編へ反映済み。次はPhase 20（仕事データモデル拡張）から着手する |
| 1.1（改8） | 2026-09-04 | Phase 20着手前に、月次報告・半期評価の出力フォーマットをユーザーとの追加討議で詳細化（要件定義書1.1（改5）等）。Phase 20の`goal_retrospective`拡張列を7列（`target_goal_text`等）に増補、Phase 22にAI応答のパース処理・`PATCH`エンドポイント・body組み立てテンプレートの実装を追加、Phase 23にレビュー用フォーム・ダウンロードボタンの実装を追加。Phase 19時点の設計（3節構成のプロレーズ文書）から、構造化列＋固定テンプレートによる組み立て方式へ変更 |
| 1.1（改9） | 2026-09-04 | Phase 20（仕事データモデル拡張）完了。`GoalCategory.WORK`・`AiPurpose`／`ConversationScope`の仕事用3値・`RetrospectivePeriodType`enumを追加。`work_assignment`／`work_log`テーブルと`goal_retrospective`への9列追加（マイグレーション`e1f4a9c3b6d8`、UNIQUE制約`(goal_id, period_type, period_key, is_anonymized)`を含む）を実装。`DAILY_FEEDBACK_WORK`等3種のprompt_template初期データ（ロジック・プロンプト編17.8〜17.10から一字一句転記）と`ai.work_recent_log_days`等のapp_setting初期データを追加。バックエンド全739テスト・カバレッジ100%・ruff検査通過。既存の資格試験・読書関連のモデル・マイグレーション・テストは無改修。次はPhase 21（仕事目標・記録API）から着手する |
| 1.1（改10） | 2026-09-05 | Phase 21（仕事目標・記録API）完了。`work_service.py`・`api/work.py`・`schemas/work.py`を新設し、案件情報のCRUD（`POST`/`PATCH /goals/{id}/work-assignment`、書籍と異なり目標配下のネストパスに統一）・経過日数等の進捗算出を実装。`goal_service.py`（`activate_goal`・`close_goal`への`with_result`分岐・カスケード削除）、`record_service.py`（`WorkLogItem`・`work_items`引数）、`api/records.py`・`api/errors.py`等を拡張。バックエンド全789テスト・カバレッジ100%・ruff検査通過。既存の資格試験・読書関連コードは新しいカテゴリ分岐の追加以外無改修。次はPhase 22（仕事AI連携）から着手する |
| 1.1（改11） | 2026-09-05 | Phase 22（仕事AI連携）完了。`work_feedback_service.py`（DAILY_FEEDBACK_WORK）・`work_report_service.py`（月次報告・半期評価の生成・PATCH編集・期間判定・AI応答パース＋body組み立て）を新設。`retrospective_service.get_latest_retrospective`をperiod_type／period_key対応に拡張し、`generate_retrospective`にWORK目標への恒久的な拒否ガードを追加。`ai_context_service.py`にWORK用変数ビルダーと、今日の一言の対象をEXAM・WORKへ拡張する`list_daily_message_target_goals`を追加。`api/closure.py`に月次報告・半期評価のPOST/GET/PATCH、`api/records.py`に`POST /records/{date}/work-chat`を追加。バックエンド全876テスト・カバレッジ100%・ruff検査通過。次はPhase 23（仕事フロントエンド）から着手する |
| 1.1（改12） | 2026-09-05 | Phase 23（仕事フロントエンド）完了。API層・`WorkAssignmentTab.tsx`・`WorkReportTab.tsx`・`WorkLogFields.tsx`・`WorkLogSummaryList.tsx`等を新設し、目標作成・目標詳細・日次報告・ダッシュボード・ナレッジエクスポートの各画面にWORK分岐を統合。実装過程で`export_service.py`がWORK目標に未対応だった設計不足（ナレッジエクスポート画面から総括レポート生成を呼ぶとPhase22のWORK恒久拒否ガードでクラッシュする経路）と、エクスポート概要欄が案件名でなく取引先呼称（任意項目）を表示していた不具合を発見し修正。あわせて`ai.assistant_uid.daily_feedback_work`（AI-08）の既定値を、未選定（空文字）からAI-06（読書日次報告フィードバック）と同一のGPT-5.4-mini「高速」へ選定（仕様書1.1（改11）S-09一部解消、データ構造編1.1（改14）、`backfill_work_assistant_defaults`を追加）。目標一覧の種別表示（S-10）はS-08と同じテキストラベル方式で一部解消。バックエンド全886テスト・カバレッジ100%・ruff検査通過、フロントエンド全126テスト・`tsc -b`・oxlint通過。次はPhase 24（仕事機能 実環境検証、テッペイさんの手元で実施）に進む |
| 1.1（改13） | 2026-09-06 | 分析タブで読書・仕事目標を選択してもEXAM専用の5タブがMaterial非依存でないため常に空表示になる不具合の調査を契機に、Phase 25（分析・カレンダー・ダッシュボードの目標ごと表示統一）を新設。日次報告（Phase8・改8）で確立済みの「GoalTabBarで対象目標を1件選択し、選択中の目標分のみ表示する」設計を、分析・カレンダー・ダッシュボードの3画面に横展開する（仕様書1.1改14）。分析画面にREADING/WORK向け`GET /analytics/reading-logs`・`GET /analytics/work-logs`を新設し「読書記録」「業務記録」タブを追加、カレンダーの複数目標同時表示・ダッシュボードの全目標グルーピング表示（改8で対象外としていた判断）をいずれもGoalTabBar方式に変更。対象目標の解決ロジックの重複を避けるため共通ヘルパー`resolveTargetGoalId`を新設。バックエンド6テスト追加（全918件成功・カバレッジ100%）・フロントエンド4テスト追加（全138件成功）、いずれも既存テストは無改修、`ruff check`・`tsc --noEmit`・oxlint通過。dry-reviewer・label-checker・style-reviewerによるレビューで判明した軽微な指摘（区切り文字のハードコード、docstring欠落、コメントと実装範囲の不一致）も修正済み |
| 1.1（改14） | 2026-09-06 | 分析タブ「成長記述」が全目標のAI日次報告フィードバックを無条件に混在表示する不具合の調査を契機に、Phase 26（日次フィードバックAI対話の目標単位分離）を新設。purposeフィルタのみの対症療法ではなく、`chat_message.goal_id`追加（マイグレーション`d8f21a6c4b3e`）により日次フィードバックの会話自体を「カテゴリ単位の共有スレッド」から「目標単位」へ分離する根本対応を行った（未決事項L-07の解消方針転換、仕様書1.1改15、データ構造編1.1改18）。横展開チェックにより、EXAM目標が2件以上同時進行時に選択中でない目標の下書き入力までAIプロンプトに混入していた別件バグ、およびナレッジエクスポートのAI対話履歴に他目標の対話が混入しうる別件バグも本対応で修正。移行前のレガシーメッセージ（goal_id=NULL）は分析タブ上のプルダウンから利用者が手動で目標を割り当てられる（PATCH /analytics/growth-descriptions/{id}）。バックエンド既存テストの前提変更に伴う書き換えを含め全943件成功・カバレッジ100%、フロントエンド全138件成功、`ruff check`・`tsc --noEmit`・oxlint通過 |

---

## 1. 本計画の方針

### 1.1 分割の原則

| 原則 | 内容 |
| --- | --- |
| 依存順に進める | 前フェーズの成果物に依存するものを後に置く |
| ロジックを先に固める | 中核算出ロジックは画面より先に実装し、テストで検証する |
| AI連携を独立させる | AI基盤が不安定でも他の開発が止まらないよう、依存を後段に集約する |
| 1フェーズを1セッションで完結させる | コンテキストの飽和を避ける |

### 1.2 各フェーズに渡す文書

全フェーズで CLAUDE.md および運用設計ルールを常時参照させる。加えて、フェーズごとに必要な設計文書のみを投入する。**4文書を毎回全部渡さない。**

### 1.3 完了条件の考え方

各フェーズには検証可能な完了条件を定める。条件を満たさないまま次フェーズへ進まない。特に Phase 2 は、以降の全機能が依存するため厳格に検証する。

### 1.4 AI基盤の疎通確認について

当初は「AI基盤の開発キットは他アプリケーションで既に利用実績があるため、事前の疎通確認は行わない」としていたが、2026-08-23にNewtonX ADK（v0.10.5）を実際に導入し、ソースコード確認と実機テスト（アシスタント9種のうち8種で送受信を試行）を行った結果、以下を確認した。

**確認済みの事実**

- 公式ドキュメントが案内する親メッセージ順序（`parent_order`）は、実際にインストールされたコードでは機能しない（`send_message()`のシグネチャに引数が存在せず常に固定値0で送信される）。実機テストでも、これにより複数往復の文脈が自動的には維持されないことを確認した。→ 対応済み。文脈維持は`chat_message`の全文注入方式に一本化した（ロジック・プロンプト編16.3.1）
- ホスト`seraku-ex.newton-x.net`（本番相当として使用中）で、**特定のアシスタント2種（GPT-4o mini、GPT-4o）のみ**`send_message`が常に404で失敗することを確認した。**それ以外の7種のアシスタント（GPT-5系、Gemini系）は正常に送受信できることを実機確認済み。** 当初はテナント全体の疎通問題と誤認したが、原因はアシスタント個別の問題（廃止済みモデルの可能性）に切り分けられた。→ 対応済み。用途別の既定アシスタントを、動作確認済みの7種から選定した（5.2、仕様書8.9.1）

**この経緯を踏まえた運用方針**：「他アプリでの利用実績」は接続性一般の裏付けにはなるが、個別のアシスタント・APIメソッドの動作までは保証しない。**新しいアシスタントを既定値に追加する場合や、開発キットをバージョンアップする場合は、実装への組み込み前に軽量な疎通テスト（チャット作成→メッセージ送信→削除）を行うことを標準の手順とする。**

| 確認項目 | 状態 | 確認するフェーズ |
| --- | --- | --- |
| 認証フローの動作（PAT主経路） | 確認済み（2026-08-23） | Phase 5前に完了 |
| チャット作成・送受信・削除の動作 | 確認済み（対応アシスタントでは正常。5.2の除外リスト対象は使用不可） | Phase 5前に完了 |
| Web検索の無効化が有効に働くこと | 未確認 | Phase 5 完了後 |
| アシスタント一覧の取得と選択 | 一覧取得・個別の疎通は確認済み。選択UIは未実装 | Phase 5〜7 |
| フォルダおよび会話の作成 | 未確認 | Phase 5 完了後 |
| 長文プロンプトの送信可否と入力上限の実測 | 未確認 | Phase 5 完了後 |
| 呼び出し頻度制限の有無 | 未確認 | Phase 5 完了後 |

このうち入力上限の実測値は、設定値 `ai.max_prompt_chars` の調整に用いる。通信ログに送信文字数と縮退の有無を記録する設計としているため、運用の中で自然に把握できる。

---

## 2. フェーズ一覧

| Phase | 名称 | 主な成果物 | 前提 |
| --- | --- | --- | --- |
| 1 | 基盤構築 | プロジェクト構成、DB、モデル、初期データ | — |
| 2 | コアロジック | 算出サービス層とテスト | 1 |
| 3 | 目標・リソースAPI | 目標、教材、科目、リソースのAPI | 2 |
| 4 | 日次記録API | 実績登録、報告確定、カレンダーのAPI | 3 |
| 5 | AI連携 | 会話管理、プロンプト組み立て、縮退、対話 | 4 |
| 6 | フロント基盤 | 共通基盤、ナビゲーション、ダッシュボード | 3 |
| 7 | 設定系画面 | 目標管理、リソース設定、アプリ設定 | 6 |
| 8 | 記録系画面 | カレンダー、日次報告、進捗登録、報告閲覧 | 5, 6 |
| 9 | 分析画面 | 各種グラフ、ガントチャート、リプラン履歴 | 6 |
| 10 | 完了・エクスポート | 受験結果、総括レポート、ナレッジエクスポート | 5, 7 |
| 11 | 実環境検証 | 動作確認、設定値の調整 | 全 |

Phase 6 は Phase 3 完了後に着手可能であり、Phase 4・5 と並行できる。

---

## 3. 各フェーズの詳細

### Phase 1：基盤構築

| 項目 | 内容 |
| --- | --- |
| 目的 | プロジェクト構成とデータ永続化層を確立する |
| 投入文書 | CLAUDE.md、運用設計ルール、設計書 データ構造編（全体） |

**実装対象**

1. ディレクトリ構成の作成（設計書 データ構造編 第8章）
2. Python環境とパッケージ管理の設定
3. FastAPI の起動設定（**ポート8400を避ける。既定値8100**）
4. SQLAlchemy のセッション管理、`PRAGMA foreign_keys = ON` の設定
5. 全 Enum の定義（backend/app/constants/）
6. 全 SQLAlchemy モデルの定義（`ai_conversation`、`goal_retrospective` を含む）
7. Alembic の初期化と初回リビジョン生成
8. 初期データ投入処理（app_setting、prompt_template、day_type_default）
9. `/health` エンドポイント

**完了条件**

- `alembic upgrade head` でデータベースが構築される
- 起動時に初期データが投入され、再起動しても重複しない
- `/health` が応答する
- 全テーブルに対する基本的なCRUDが動作することを確認できる
- `material` に `planned_cycles` が存在し、`current_cycle` が存在しない

**注意点**

- **マイグレーションSQLを目視レビューすること。** Alembicの自動生成は複合主キーや制約の扱いを誤ることがある
- 初期データ投入はマイグレーションではなくアプリケーション起動時に行う。データとスキーマを分離するため
- プロンプトテンプレートの初期文面は設計書 ロジック・プロンプト編 第17章から転記する。要約や改変をしないこと

---

### Phase 2：コアロジック

**本フェーズが最も重要である。以降の全機能がここに依存する。**

| 項目 | 内容 |
| --- | --- |
| 目的 | 算出ロジックを実装し、テストで正しさを保証する |
| 投入文書 | CLAUDE.md、運用設計ルール、設計書 ロジック・プロンプト編（第2〜14章）、データ構造編（第5章のみ） |

**実装対象**

| サービス | 内容 |
| --- | --- |
| calendar_service | 1日の境界時刻の解決（論理的な本日の算出） |
| calendar_service | 日種別の解決（個別指定 → 祝日 → 曜日既定の順） |
| calendar_service | 負荷係数の解決 |
| cycle_service | 総作業量、残量、現在周回の導出 |
| slot_service | スロットの取得、教材への割当、充足検証 |
| quota_service | 日次ノルマの算出 |
| speed_service | 実効速度の周回別算出、必要速度、完了予測日 |
| threshold_service | 警告判定、強制リプラン判定、締切超過判定 |
| baseline_service | 計画基準値の記録と取得 |
| metrics_service | バッファ消費率、進捗率、報告率、連続報告日数、リプラン回数 |

**テスト（先に書く）**

設計書 ロジック・プロンプト編 第20章の検証観点をテストケース化する。特に以下は必須とする。

| 観点 | 検証内容 |
| --- | --- |
| 周回 | 1周目完了時に残量が0にならず、2周目のノルマが算出されること |
| 周回 | 現在周回が累積完了量から正しく導出されること |
| 周回別速度 | 2周目の速度が1周目と分離して算出されること |
| 周回別速度 | サンプル3件未満のとき直近周回の値で代替されること |
| 残量再配分 | 7日連続未達時、ノルマが全量繰越（8倍）ではなく再配分（約2.2倍）になること |
| バッファ非対称性 | バッファ日に実績が無くても翌日のノルマが増えないこと |
| バッファ非対称性 | バッファ日に実績があると翌日のノルマが減ること |
| 境界時刻 | 境界時刻より前の時刻において論理日が前日になること |
| 時間未入力 | 実効速度の分母・分子から除外されること |
| 日種別優先順位 | 個別指定が祝日設定を上書きすること |
| 負荷係数 | 係数変更で総量が保存されること |
| 境界値 | 残計画日0、実績0件、締切超過の各ケースで例外が発生しないこと |

**完了条件**

- 全テストが通過する
- 上記12観点が明示的にテストされている
- サービス層がHTTPに関する知識を一切持たない
- 日次ノルマ、残量、現在周回のいずれもデータベースに保存されていない

**注意点**

- **テストを後回しにしない。** ここの誤りは全機能に波及し、後から発見すると修正コストが跳ね上がる
- 残量の算出において、総量ではなく総作業量を用いること。この誤りは運用開始から数週間で顕在化する

---

### Phase 3：目標・リソースAPI

| 項目 | 内容 |
| --- | --- |
| 目的 | 目標と教材、リソースの登録・編集を可能にする |
| 投入文書 | CLAUDE.md、運用設計ルール、設計書 データ構造編（第6章）、仕様書（第6.2〜6.3節、第7章、第10章） |

**実装対象**

- 目標のCRUDと状態遷移（activate / pause / resume / close）
- 試験科目のCRUD、受験日の確定処理
- 教材のCRUD、科目との紐付け、締切の自動導出、予定周回数の管理
- 周回ごとの進捗取得
- 負荷プロファイルのCRUD
- リソーススロットのCRUD、曜日別既定値、1日の境界時刻
- 配分状況の取得
- リプラン履歴の取得
- バリデーション（仕様書 第10章）

**完了条件**

- リソース配分の合計が100%を超える保存が拒否される
- 一時停止からの復帰時、リソース不足でエラーとなる
- 受験日を確定日に変更すると計画基準値が再設定される
- 教材と科目の紐付け変更で締切が再計算される
- 予定周回数を完了済み周回数未満に変更できない
- 予定周回数の変更で計画基準値が再設定される
- クローズ済み目標への更新が拒否される

---

### Phase 4：日次記録API

| 項目 | 内容 |
| --- | --- |
| 目的 | 実績の記録と確定を可能にする（AI連携を除く） |
| 投入文書 | CLAUDE.md、運用設計ルール、設計書 データ構造編（第5.4節、第6章）、仕様書（第6.4〜6.7節、第7.2節） |

**実装対象**

- 論理的な本日の取得（`GET /records/today`）
- 日次記録の取得、進捗のみ登録、報告確定
- 学習実績の登録（教材別、周回別、品質指標）
- コメントのCRUD
- カレンダーデータの取得、日種別の個別指定
- 祝日データの取込
- 記録状態の遷移制御

**完了条件**

- 前日を超える遡及入力が拒否される
- 進捗のみ登録から報告への昇格が前日以内に限定される
- 確定済み記録への更新が拒否される
- 品質指標の主観的手応えが正規化される
- 周回番号の既定値が算出値として提示され、上書きできる
- 「前日」の判定が1日の境界時刻を考慮している

**注意点**

AI連携は本フェーズに含めない。報告確定は `chat_message` が空の状態で成立させる。

---

### Phase 5：AI連携

| 項目 | 内容 |
| --- | --- |
| 目的 | AI基盤との連携を実装する |
| 投入文書 | CLAUDE.md、運用設計ルール、設計書 ロジック・プロンプト編（第15〜18章、特に16.1〜16.3・16.3.1）、データ構造編（第5.5節、第5.8節）、**`newtonx_adk/docs/`配下のADK公式ドキュメント一式**（`API_REFERENCE.md`、`USAGE_GUIDE.md`、`auth_setup_guide.md`）および導入済みパッケージのソースコード（`myvenv/Lib/site-packages/newtonx_adk/`） |

**Phase 5着手時の前提（2026-08-23時点で確認済み。実機テスト実施済み）**

- 導入済みのNewtonX ADK（v0.10.5）は、公式ドキュメント上は親メッセージ順序（`parent_order`）による会話文脈の維持を案内しているが、実際にインストールされたパッケージのソースコード（`client.py`）を確認したところ、`send_message()`に当該パラメータは存在せず常に固定値0で送信される。実機テストでも、これにより文脈が自動維持されないことを確認した。**対応済み**：文脈維持は`chat_message`の全文注入方式に一本化した。詳細は設計書 ロジック・プロンプト編 16.3.1を参照
- 認証方式は、当初想定していたブラウザ経由のOAuthフローではなく、**PAT（Personal Access Token）が現行の公式な標準方式**であることを確認済み（`USAGE_GUIDE.md`のクイックスタート、`auth_setup_guide.md`）。この環境でも実際にPATが設定済みであることを確認した
- ホスト`seraku-ex.newton-x.net`で、アシスタント9種のうち8種（GPT-4o mini以外の全て）で実際にチャット作成・メッセージ送信・応答受信が正常に成功することを実機確認済み。**GPT-4o mini・GPT-4o の2種のみ**`send_message`が常に404で失敗することを確認したため、この2種は既定値・選択肢から除外する（5.2、仕様書8.9.1）。テナント全体の疎通は問題ない
- 上記を踏まえ、本フェーズの実装要件・完了条件は既にPAT前提・親メッセージ順序非依存・動作確認済みアシスタント前提の内容に更新済みである（以下参照）

**実装対象**

- 開発キットのラッパ（`ai/client.py`）
- 認証状態の管理（`ai/auth.py`）
- 会話とフォルダの管理（`ai/conversation.py`）
- プロンプト組み立てと段階的縮退（`ai/prompt_builder.py`）
- 呼び出し間隔の制御（`ai/rate_limiter.py`）
- 通信ログ（`ai/logger.py`）
- 日次報告フィードバックの呼び出し
- 週次要約の生成判定と遡及実行
- 今日の一言の生成判定と実行
- 例外のマッピング

**AI基盤の実仕様に基づく実装要件**

**開発キットの仕様は変更できない。以下は所与の制約として実装する。**

| 項目 | 要件 |
| --- | --- |
| Web検索 | メッセージ送信時に**必ず明示的に無効化する**。既定値が有効のため省略不可 |
| ナレッジ検索 | 明示的に無効化する |
| 会話の作成 | メッセージ送信前に会話を作成し、識別子を `ai_conversation` に保存する |
| 会話の単位 | 用途と日付ごとに分離する（日次報告、週次要約、今日の一言、総括レポート） |
| フォルダ | 目標ごとにフォルダを作成する。名前指定での会話作成機能を用い、存在しない場合は自動作成する |
| 親メッセージ順序 | 送信はするが文脈維持には使用しない（v0.10.5では機能しないことを確認済み）。`last_parent_order`は将来の改修に備えた記録として更新する |
| 文脈維持（同一日内の複数往復） | `chat_message`テーブルの対話履歴をプロンプトへ全文注入する方式（`{{conversation_history}}`）で実現する。親メッセージ順序には依存しない |
| 会話識別子の取得 | ローカルに保存した値を用いる。一覧取得APIを毎回呼ばない |
| タイムアウト | 設定値（既定60秒）を開発キットの設定に反映する |
| 再試行回数 | 設定値（既定1）を開発キットの設定に反映する。アプリ側の再送と二重にしない |
| 呼び出し間隔 | 設定値（既定2秒）の下限を守る |
| 認証 | **PATを主経路とする。** Host・PATが設定済みかを起動時に確認する。未設定・無効時のみ、設定画面からブラウザ経由の認証フロー（バックグラウンドスレッド実行）を提供する |
| リダイレクト先（フォールバック時のみ） | 開発キットの既定値を使用する。サーバは別ポートで起動する |
| 稼働確認 | 起動時にモデルの利用可否を取得する |
| ナレッジ登録 | **使用しない。** 共有アシスタントに個人の学習記録を登録しない |
| ファイル添付 | 使用しない |

**例外マッピング**

| 開発キットの例外 | APIエラーコード |
| --- | --- |
| 認証関連の例外 | AI_AUTH_REQUIRED |
| API通信関連の例外 | AI_ERROR |
| チャット関連の例外 | AI_ERROR |
| 設定エラー | AI_CONFIG_ERROR |
| タイムアウト | AI_TIMEOUT |
| 基本例外（上記以外） | AI_ERROR |

**タイムアウトの判定方法**：導入済みADKの例外クラスは`NewtonXError`/`AuthenticationError`/`APIError`/`ConfigurationError`/`FileUploadError`/`ChatError`の6種（ソースコード確認済み）で、タイムアウト専用の型は存在しない。通信タイムアウトも`APIError`へ一律ラップされるため、型による判定はできない。経過時間の実測またはエラーメッセージの文字列判定で`AI_TIMEOUT`を判定する。

**プロンプト縮退の実装**

設計書 ロジック・プロンプト編 16.5 の4段階を実装する。縮退が発生した場合、`ai_log.was_truncated` を true として記録し、応答とともに画面へ通知する。

**完了条件**

- 日次報告でフィードバックが取得できる
- Web検索の無効化が明示的に指定されている（コード上で確認可能）
- 会話識別子が保存され、同一日の再送で再利用される
- フォルダが目標ごとに作成される
- **同一日内で複数往復した際、2回目以降の応答が1回目までの内容を踏まえたものになる**（`{{conversation_history}}`による文脈維持が実際に機能することを、実機での複数往復テストで確認する）
- PATによる認証で日次報告フィードバックの疎通ができる
- AI呼び出しが失敗しても実績入力が失われない
- AI応答なしで報告確定ができる
- 週次要約が起動時に遡及生成される
- 遡及生成時に呼び出し間隔の下限が守られる
- 今日の一言が同日中に再生成されない
- プロンプトが閾値超過時に段階的に縮退する
- 通信ログに送信文字数と縮退の有無が記録される
- AI基盤が利用不可でも記録機能が動作する

**注意点**

- **プロンプトはデータベースから読む。** ソースコードに埋め込まない
- 会話がAI基盤側に蓄積されるため、フォルダによる整理を必ず実装する
- 親メッセージ順序に依存する機能（開発キット推奨の「終端マーカー方式」による回答継続取得）は使用しない。回答が途切れた場合は直前の応答本文を含めた自己完結メッセージで継続取得する（ロジック・プロンプト編16.3.1参照）
- **GPT-4o mini・GPT-4oの2アシスタントは使用しないこと。** `send_message`が常に404で失敗することを実機確認済み（5.2、仕様書8.9.1）。既定値は動作確認済みの7アシスタントから選定済みだが、`ai.assistant_uid.*`を設定画面から変更できる仕様のため、新しいアシスタントを選択肢に追加する際は軽量な疎通テスト（チャット作成→送信→削除）を先に行う
- `delete_chat`はテスト対象の全アシスタントで一貫して`False`（削除失敗）を返すことを確認済みである。一方、`get_chats()`で確認する限り、メッセージを送信していない・または一往復のみのチャットは一覧に残らない（自然に消える）挙動も確認した。削除UIを実装する場合、`delete_chat`の戻り値を過信せず、`get_chats()`等で実際の削除結果を確認するか、削除失敗時の扱い（エラー表示／無視）を設計段階で決めておくこと

---

### Phase 6：フロント基盤とダッシュボード

| 項目 | 内容 |
| --- | --- |
| 目的 | フロントエンドの基盤を確立し、ダッシュボードを実装する |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書（第4〜5章、第6.1節） |

**実装対象**

- Vite + React + TypeScript + Tailwind の構成
- ルーティングとグローバルナビゲーション
- APIクライアントの共通処理（エラーハンドリング、型定義）
- ロケールファイル（ja.json）の構造確立
- 共通コンポーネント（ボタン、入力欄、カード、モーダル、トースト）
- ダッシュボード画面（SC-01）

**完了条件**

- 全UI文言がロケールファイルから供給される
- ダッシュボードが2秒以内に初期表示される
- 今日の一言が非同期で遅れて表示される
- 警告バナーが条件を満たすときのみ表示される
- 本日の判定にサーバから取得した論理日を用いている
- 本日のノルマに現在周回が表示される

**注意点**

- **文字列リテラルをコンポーネントに直接書かない。** ここで規律が崩れると全画面に波及する
- クライアント側でシステム日付から論理日を判断しない

---

### Phase 7：設定系画面

| 項目 | 内容 |
| --- | --- |
| 目的 | 目標とリソースの設定を画面から行えるようにする |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書（第6.2〜6.3節、第6.11〜6.12節） |

**実装対象**

- 目標一覧（SC-02）
- 目標詳細・編集（SC-03、5タブ構成）
- リソースマスタ設定（SC-04、1日の境界時刻を含む）
- 設定（SC-11）
- データ管理（SC-12）

**完了条件**

- 全設定項目が画面から変更可能である
- 利用者が設定ファイルを編集する必要がない
- リソース配分超過が画面上で検知される
- スロット充足警告が表示される
- アシスタントが用途（日次報告フィードバック／週次要約／今日の一言／総括レポート）ごとに一覧から選択できる（識別子の手入力を求めない）
- 教材タブに総作業量、現在周回、周回進捗が表示される
- プロンプトテンプレートが編集でき、初期値に戻せる

---

### Phase 8：記録系画面

| 項目 | 内容 |
| --- | --- |
| 目的 | 日々の記録操作を可能にする |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書（第6.4〜6.7節、第7.2節） |

**実装対象**

- カレンダー（SC-05）
- 日次報告（SC-06、実績入力＋AI対話）
- 進捗のみ登録（SC-07）
- 日次報告閲覧（SC-08）

**完了条件**

- カレンダーの色分けが日種別と記録状態を正しく反映する
- 日付の状態に応じて遷移先が切り替わる
- AI応答待機中も実績入力が保持される
- AI呼び出しが失敗しても入力内容が消えない
- 確定前の離脱時に警告が表示される
- 確定済み報告が読み取り専用になる
- 実績入力に現在周回が表示され、周回番号を上書きできる
- 縮退の発生が対話領域に通知される

---

### Phase 9：分析画面

| 項目 | 内容 |
| --- | --- |
| 目的 | 進捗と成長を可視化する |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書（第6.8節）、設計書 ロジック・プロンプト編（第13〜14章） |

**実装対象**

- 分析画面（SC-09、7タブ構成）
- 品質指標推移（日別・週別・月別、合格基準線、周回別系列）
- 累積完了量と計画線（周回の区切り表示）
- 完了予測と乖離
- 実効速度推移（周回別系列）
- ガントチャート
- リプラン履歴

**完了条件**

- 品質指標が周回ごとに系列分離される
- 周回の切り替わり時点に区切り線が表示される
- 合格基準線が重畳される
- 粒度の切り替えが動作する
- ガントチャートに教材期間と現在位置が表示される
- リプラン履歴に日時、契機、変更前後のノルマが表示される

---

### Phase 10：完了処理とナレッジエクスポート

| 項目 | 内容 |
| --- | --- |
| 目的 | 目標完了時の記録集約と横展開用出力を実装する |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書（第6.9〜6.10節）、設計書 ロジック・プロンプト編（第17.5節、第18章）、データ構造編（第7章） |

**実装対象**

- 受験結果登録（SC-10）
- 目標のクローズ処理
- 総括レポートの生成と再生成
- ナレッジエクスポート画面（SC-13）
- 出力項目の選択と匿名化
- Markdown形式とJSON形式の出力
- プレビュー
- 全データのエクスポート／インポート
- バックアップの作成と復元

**完了条件**

- 受験結果登録により目標がクローズし読み取り専用になる
- 総括レポートが生成される
- 総括レポートにリプラン履歴の分析が含まれる
- ナレッジエクスポートが人間可読な形式で出力される
- 匿名化時に日記本文とAI対話履歴が除外される
- 匿名化時に週次要約と総括レポートが再生成される
- JSON出力をインポートして復元できる

**注意点**

- 匿名化時の再生成は複数回のAI呼び出しを伴う。呼び出し間隔の下限を守り、進捗を表示すること
- 総括レポートの生成はクローズ処理の成否に影響させない。非同期で実行し、失敗しても後から再生成できるようにする

---

### Phase 11：実環境検証

| 項目 | 内容 |
| --- | --- |
| 目的 | 実環境での動作を確認し、設定値を調整する |
| 実施 | テッペイさんの手元で実施する |

**確認項目**

| 項目 | 確認内容 | 調整先 |
| --- | --- | --- |
| 認証 | PATによる認証で疎通できること。フォールバックのブラウザ認証フローも動作すること | — |
| ポート | 8100と8400が衝突しないこと（フォールバック認証使用時） | `server.port` |
| アシスタント | 一覧が取得でき、用途ごとに選択できること | `ai.assistant_uid.*`（4キー） |
| Web検索 | 無効化が有効に働くこと（検索結果が混入しないこと） | — |
| フォルダ | 目標名のフォルダが作成されること | `ai.folder_prefix` |
| 応答時間 | タイムアウト60秒が妥当か | `ai.timeout_seconds` |
| 入力上限 | 長文プロンプトが送信できるか。縮退が過剰に発生しないか | `ai.max_prompt_chars` |
| 頻度制限 | 週次要約の連続生成でエラーが出ないか | `ai.min_interval_seconds` |
| 応答品質 | フィードバックが期待した構成と深さを持つか | プロンプトテンプレート |
| 文脈維持 | 複数往復での対話が、対話履歴の全文注入によって成立していること（親メッセージ順序は機能しない前提） | — |
| チャット操作の安定性 | チャット作成直後のメッセージ送信・削除が安定して成功すること（Phase5冒頭のスモークテストで`seraku-ex`テナントにて404を確認済み。本番相当テナントで再検証） | — |

**入力上限の測定方法**

通信ログに送信文字数と縮退の有無を記録しているため、以下で判断する。

- 縮退が一度も発生していない場合、閾値を引き上げて情報量を増やす
- 通信エラーが発生した際の送信文字数を確認し、閾値を引き下げる

**応答品質の調整**

プロンプトの初期文面は暫定である。実際の応答を見て、以下の観点で調整する。

- 負荷を減らす提案が適切な頻度で出るか（出すぎ、出なさすぎのいずれも問題）
- 成長の指摘が具体的か、当たり障りのない内容になっていないか
- 行動提案が実行可能な粒度か

---

### Phase 12：ヘルプ画面（追加スコープ）

| 項目 | 内容 |
| --- | --- |
| 目的 | Phase 11完了後に生じた追加要望。アプリの使い方全般（全画面・全設定項目・プロンプトのプレースホルダー一覧・よくある質問）をSC-14ヘルプ画面に集約し、ユーザーが画面上から仕様や使い方を把握できるようにする |
| 位置づけ | 当初の仕様書（v1.1策定時点）には無かった追加機能。既存フェーズの番号・完了条件は変更しない |
| 完了条件 | SC-14ヘルプ画面がグローバルナビゲーションから到達でき、仕様書6.13に列挙した全セクション（はじめに／SC-01〜SC-13の使い方／AI接続・その他設定項目／プロンプトのプレースホルダー一覧／よくある質問）を収録していること。目次・キーワード検索が機能すること |
| 投入文書 | 本ファイル、`docs/仕様書_ミチナリ_v1.1.md`6.13、`docs/設計書_ロジック・プロンプト編_ミチナリ_v1.1.md`17章（プレースホルダー一覧の一次情報源） |

---

### Phase 13：読書機能の設計文書反映

| 項目 | 内容 |
| --- | --- |
| 目的 | 確定した読書機能の設計を要件定義書・仕様書・設計書に反映し、Phase 14以降が設計書のみを参照して実装できる状態にする |
| 位置づけ | Phase 1〜12（資格試験機能）とは独立した追加スコープ。既存フェーズの番号・完了条件・既存文書の資格試験関連の記載は一切変更しない |
| 投入文書 | CLAUDE.md、運用設計ルール、本フェーズ自体（設計確定内容の書き起こしが実装対象） |

**設計確定内容（反映元）**

- `goal.category`（GoalCategory: EXAM / READING）を追加し、目標種別を区別する
- `book`（書籍。goal_idにUNIQUE制約＝1目標1冊）、`reading_log`（読書記録。daily_record_id×book_idでUNIQUE）を新設する
- 読書目標のライフサイクルは既存の `goal.status`（DRAFT/ACTIVE/PAUSED/CLOSED）をそのまま流用し、`book` 側に独自のstatus/completed_atは持たせない
- AI連携は「日次報告フィードバック」「読了レポート」の2段構成とし、週次要約は対象外とする。`AiPurpose` に `DAILY_FEEDBACK_READING` / `GOAL_RETROSPECTIVE_READING` を追加し、それぞれ `prompt_template` に新規行、`ai.assistant_uid.*` に新規設定キーを追加する（既存の資格試験用プロンプト・アシスタント設定は無改変）
- 読了レポートは既存の `goal_retrospective`（本文・再生成可・匿名化版保持）をそのまま流用する
- ナレッジエクスポート機能はコード変更なしで流用する（読書目標も既存の対象目標選択肢に含まれることを仕様書に明記するのみ）
- `GET /goals/{id}/plan` は category=READING のとき、日次ノルマ・実効速度ではなく残日数・直近記録日・連続記録日数（・total_pages設定時はページ進捗）を返す軽量な応答に分岐する
- 読書目標は `resource_ratio` のリソース配分プールの対象外とする（前掲の確認事項。異なる場合はここで修正する）

**実装対象（ToDo）**

- [x] 要件定義書：読書目標に関する要件項目を追加する（目標登録、日次想起、読了レポート生成、既存機能との差異）→ 6.10（R-64〜R-71、mainのアーカイブ機能とのID衝突によりR-69〜R-71へ採番替え）、5.3、12章（O-05）に反映済み
- [x] 仕様書：目標作成フォームへの種別選択追加、日次報告画面の種別分岐、目標詳細画面（読書用タブ構成）、読了レポート表示・生成導線、API一覧への新規・変更エンドポイント追加、ロケールキー一覧への追加 → 3.2・6.2・6.5・6.10・7.1・8.1・8.4・8.9.1・10・12章に反映済み（ロケールキーの個別列挙はPhase17の実装時に行う）
- [x] 設計書 データ構造編：`GoalCategory` enum、`goal.category` 列、`book`／`reading_log` テーブル定義、ER図・カスケード関係表の更新、`AiPurpose`／`ConversationScope` 拡張、`ai.assistant_uid.*` 設定キー追加 → 4章・5.1〜5.6・6.2・6.3・7章・8章・9章に反映済み
- [x] 設計書 ロジック・プロンプト編：`DAILY_FEEDBACK_READING`／`GOAL_RETROSPECTIVE_READING` の注入変数一覧と初期プロンプト文面（ノルマ達成圧力を含まない文面とすること）、`GET /goals/{id}/plan` の読書版応答仕様 → 17.6・17.7・新設21章に反映済み。旧21章（未決事項）は22章へ繰り下げ
- [x] 各文書の改訂履歴を更新する（本ファイル「実装フェーズ分割計画書」自身の改訂履歴に、Phase 13〜18（読書機能）追記の旨を記載することを含む）→ 完了（本ファイル改訂履歴1.1（改2）、他3文書も改訂履歴に追記済み）

**Phase 13完了（2026-08-31）。次はPhase 14（読書データモデル拡張）から着手する。**

**完了条件**

- 各文書に読書機能の記載が反映され、Phase 14以降がこれらの文書のみを参照して実装に着手できる
- 既存の資格試験関連の記載に差分がないこと（diffで確認）
- CODING_RULES.md のゼロハードコーディング・命名規則・置き場所ルールとの整合が取れている

---

### Phase 14：読書データモデル拡張

| 項目 | 内容 |
| --- | --- |
| 目的 | 読書目標のためのDBスキーマを追加する |
| 投入文書 | CLAUDE.md、運用設計ルール、設計書 データ構造編（Phase13で反映した読書関連節） |

**実装対象**

- [x] `GoalCategory` enum の定義（backend/app/constants/enums.py）。あわせて`AiPurpose`／`ConversationScope`に`DAILY_FEEDBACK_READING`／`GOAL_RETROSPECTIVE_READING`を追加
- [x] `goal.category` 列のマイグレーション追加（既定値 `EXAM`。既存レコードは全件 `EXAM` として遡及設定する）→ `alembic/versions/b7a1021fff73_reading_goal_category.py`。server_defaultを残す方式（2b8b04b91e47と同じ理由）で実装し、legacy DB migration test（test_bootstrap.py）で遡及設定を実地検証済み
- [x] `book` テーブルの新設（`goal_id` にUNIQUE制約）→ `backend/app/models/book.py`
- [x] `reading_log` テーブルの新設（`daily_record_id` と `book_id` の組み合わせにUNIQUE制約）→ `backend/app/models/record.py`
- [x] 全SQLAlchemyモデルへの反映（`goal.py`にcategory列・book関係、`models/__init__.py`にBook/ReadingLog登録）
- [x] `prompt_template` への初期データ追加（`DAILY_FEEDBACK_READING`、`GOAL_RETROSPECTIVE_READING`）→ `backend/app/init/prompt_texts.py`（ロジック・プロンプト編17.6/17.7から一字一句転記）、`backend/app/init/seed_data.py`
- [x] `app_setting` への初期データ追加（`ai.assistant_uid.daily_feedback_reading`、`ai.assistant_uid.goal_retrospective_reading`、`ai.reading_recall_recent_days`）→ 未選定のアシスタントUIDは空文字で初期投入（Phase16で疎通確認のうえ設定画面から補う）

**完了条件**

- [x] `alembic upgrade head` でスキーマが構築される
- [x] 既存の `goal` レコードが `category='EXAM'` として問題なく引き継がれる（test_migration.py、test_bootstrap.pyのlegacy DB移行テストで確認）
- [x] `book.goal_id` のUNIQUE制約により1目標1冊が保証される（test_models_crud.py::test_book_goal_id_unique_constraint）
- [x] 初期データ投入が重複しない（再起動時も同様）（test_seed_data.pyが件数をlen()で動的検証するため新規キー追加後も無改修で通過）
- [x] `material`／`exam_subject`／既存テーブルに変更がない

**Phase 14完了（2026-08-31）。バックエンド全602テスト・カバレッジ100%・ruff検査通過を確認済み。次はPhase 15（読書目標・記録API）から着手する。**

**注意点**

- マイグレーションSQLを目視レビューすること（Phase1と同じ注意点）
- プロンプトテンプレートの初期文面はPhase13で確定した文面から転記し、要約・改変をしないこと

---

### Phase 15：読書目標・記録API

| 項目 | 内容 |
| --- | --- |
| 目的 | 読書目標のCRUDと日次記録APIの種別分岐を実装する（AI連携を除く） |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書・設計書 データ構造編（Phase13で反映した読書関連節） |

**実装対象**

- [x] 目標作成・更新APIの `category` 対応（`category=READING` のとき `exam_subject`／`material`／`load_profile` 関連の入力・バリデーションを要求しない）→ `GoalCreate.category`、`goal_service.create_goal`/`activate_goal`（EXAM/READINGで分岐）
- [x] `book` のCRUD → `book_service.py`（新設）、作成は試験科目・教材と同じネストパス`POST /goals/{id}/book`（`api/goals.py`）、更新・読了は`api/books.py`（新設）
- [x] 日次記録APIの分岐：`category=READING` の目標に対しては `study_log` ではなく `reading_log` を受け付ける（`daily_record` 本体・確定ロジックは無改変で共用）→ `record_service.py`に`ReadingLogItem`・`_apply_reading_logs`を追加、`register_progress`/`finalize_record`に`reading_items`引数を追加（デフォルトNoneで後方互換）
- [x] 残日数・直近記録日・連続記録日数（`total_pages`設定時のみページ進捗）の実装 → `book_service.get_book_progress`。**設計変更**：`GET /goals/{id}/plan`は実装せず、教材の派生値が`MaterialRead`に埋め込まれているのと同じ方式で`BookRead`へ埋め込んだ（データ構造編1.1（改5）に反映済み）
- [x] 目標クローズ処理の確認（読書目標のクローズ＝読了として成立すること）→ `book_service.complete_book`が`POST /books/{id}/complete`経由で`GoalStatus.CLOSED_WITH_RESULT`に遷移。中断は既存の`POST /goals/{id}/close`をそのまま流用（`exam_subjects`が常に空のため自動的に確認必須の「結果なし」経路になる）
- [x] バリデーション：`reading_log.recall_body` 必須、`pages_read`／`current_page` は0以上 → `schemas/record.py`の`ReadingLogInput`
- [x] `resource_ratio` バリデーションから読書目標を除外する → `goal_service.update_goal`がREADINGへの`resource_ratio`指定自体を拒否（常に0のまま）

**完了条件**

- [x] `category=EXAM` の既存エンドポイントの挙動に変化がない（既存テスト602件が無改修で全て通過）
- [x] `category=READING` の目標で科目・教材関連APIを呼んでも不整合が起きない（対象外として扱われる）
- [x] 1目標に2冊目の `book` を追加しようとすると拒否される（`BOOK_ALREADY_EXISTS`）
- [x] `reading_log` が `daily_record`×`book` 単位で一意登録される
- [x] 読書目標が `resource_ratio` の合計計算に算入されない

**Phase15完了（2026-08-31）。バックエンド全644テスト・カバレッジ100%・ruff検査通過を確認済み。次はPhase 16（読書AI連携）から着手する。**

**Phase15で追加対応した既存機能への回帰防止（未計画だったが実装中に必要と判明したもの）**

読書目標の作成・進行が可能になったことで、Phase5・Phase10で実装済みの資格試験用AI機能（本フェーズの対象外）が読書目標のデータを誤って取り込むリスクが顕在化したため、最小限のガードを追加した（詳細はロジック・プロンプト編21.1）。

- `ai_context_service.list_active_exam_goals`を新設し、`daily_feedback_service`・`daily_message_service`が参照する「進行中の目標」をEXAMに限定（読書目標が「試験科目未登録」という誤った文脈でプロンプトに混入するのを防止）
- `retrospective_service.generate_retrospective`が`category=READING`の目標に対してVALIDATION_ERRORを返すよう変更（EXAM専用プロンプトを読書目標に適用してしまうのを防止）。**Phase16でGOAL_RETROSPECTIVE_READINGを実装した時点でこの拒否は不要になるため、Phase16の実装対象に「このガードの解除」を含めること**

**注意点**

- サービス層でHTTP例外を出さない（ドメイン例外→API層変換の既存原則を踏襲）
- AI連携は本フェーズに含めない。報告確定は `chat_message` が空の状態で成立させる（Phase4と同じ方針）

---

### Phase 16：読書AI連携

| 項目 | 内容 |
| --- | --- |
| 目的 | 読書用の日次フィードバックと読了レポート生成を実装する |
| 投入文書 | CLAUDE.md、運用設計ルール、設計書 ロジック・プロンプト編（Phase13で反映した読書プロンプト節、第16章のAI連携基盤は流用） |

**実装対象**

- [x] `DAILY_FEEDBACK_READING` 呼び出しの実装 → `reading_feedback_service.py`（新設、`daily_feedback_service.py`と対）。既存の `ai/client.py`・`ai/conversation.py`・`ai/prompt_builder.py`・`ai/rate_limiter.py`・`ai/logger.py`・`ai/orchestration.py` は無改変で流用し、purpose分岐のみ追加
- [x] `GOAL_RETROSPECTIVE_READING` 呼び出しの実装 → `retrospective_service.generate_retrospective`をEXAM/READINGで分岐する構成に変更（`_build_exam_variables`/`_build_reading_variables`）。対象bookの `reading_log.recall_body` を時系列で連結した変数（`{{reading_logs}}`）を注入（`ai_context_service.build_reading_logs_text`）。Phase15で追加した「READING目標への呼び出しをVALIDATION_ERRORで拒否するガード」は、GOAL_RETROSPECTIVE_READINGへの分岐に置き換えて解消した（書籍未登録時のガードのみ残す）
- [x] 会話スコープ・フォルダ命名（目標名ベース）が読書目標でも成立することの確認 → `ensure_conversation`は無改変のため、GOAL_RETROSPECTIVE_READING（goal指定あり）は資格試験の総括レポートと同じ経路でフォルダ作成される。DAILY_FEEDBACK_READINGはDAILY_FEEDBACKと同じくgoal=NULL（フォルダ非対応、複数目標にまたがりうるため）
- [x] 週次要約の生成判定ロジックから読書目標を除外する → 変更不要（`weekly_summary_service`は`Material.goal_id`起点でgoal_idを求めるため読書目標は元から対象外）。回帰防止テストを追加して確認

**完了条件**

- [x] 読書目標の日次報告でREADING用プロンプトによるフィードバックが取得できる
- [x] 読了時に読了レポート（要約／感じたこと・考えたこと・気づいたこと構成）が生成・再生成できる
- [x] 資格試験用のプロンプト・呼び出し・アシスタント設定に影響がない（`test_reading_feedback_service.py`に相互の対話履歴が混入しないことを検証する回帰テストを追加）
- [x] 週次要約の遡及生成処理が読書目標を対象に含めず、エラーも起こさない
- [x] AI呼び出しが失敗しても実績（想起）入力が失われない（既存の16.7の保証を踏襲）

**Phase16完了（2026-08-31）。バックエンド全672テスト・カバレッジ100%・ruff検査通過を確認済み。次はPhase 17（読書フロントエンド）から着手する。**

**Phase16で追加対応した設計上の変更点**

- 同一`daily_record`に資格試験（DAILY_FEEDBACK）と読書（DAILY_FEEDBACK_READING）の対話が混在しうるようになったため、`chat_message`に`purpose`列を追加（マイグレーション`9723ab049ecd`、既存行は`DAILY_FEEDBACK`として遡及設定）。対話履歴（`{{conversation_history}}`）への注入はpurposeで絞り込み、`sequence`採番は表示順の一貫性のため日次記録全体で共有する
- AI対話は`POST /records/{date}/chat`（資格試験）と`POST /records/{date}/reading-chat`（読書）を別エンドポイントとした。1回のHTTP呼び出しで両カテゴリを多重化する設計は複雑さに見合わないと判断し、既存の「用途ごとに会話を分離する」方針（16.3）をエンドポイント単位でも踏襲した
- 読書用のアシスタントUID（`ai.assistant_uid.daily_feedback_reading`／`goal_retrospective_reading`）と想起注入日数（`ai.reading_recall_recent_days`）を設定API（`GET`/`PATCH /settings`）から変更可能にした（仕様書6.11「全ての設定項目を画面上から変更可能」）。実際のアシスタントUID値は未選定のまま（空文字）で、実環境での疎通確認（Phase11相当）を経て運用時に設定する

**注意点**

- Web検索・ナレッジ検索の明示的無効化など、既存のAI連携制約（16.1〜16.4）は読書用呼び出しにも同様に適用する（`ai/client.py`を無改変で流用しているため自動的に満たされる）
- ノルマ達成・合格圧力を前提とした文面にしないこと（Phase13で確定した初期文面から逸脱しない。17.6・17.7のプロンプトは転記のみで改変していない）

---

### Phase 17：読書フロントエンド

| 項目 | 内容 |
| --- | --- |
| 目的 | 読書目標の作成・記録・閲覧をUIで可能にする |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書（Phase13で反映した読書関連節） |

**実装対象**

- [x] 目標作成フォームへの種別選択（資格試験／読書）追加。選択に応じた入力項目の出し分け → `GoalsListPage.tsx`の`NewGoalModal`に`category`セレクタを追加
- [x] 目標詳細画面：読書目標用のタブ構成（基本情報＋書籍の簡略構成）→ `GoalDetailPage.tsx`が`goal.category`でタブ構成（`EXAM_TABS`/`READING_TABS`）を出し分け、新設`BookTab.tsx`が書籍の登録・編集・進捗表示・読了操作を担う
- [x] 日次報告画面：アクティブな読書目標がある場合の入力欄分岐（想起テキスト＋任意ページ数）→ `ReadingLogFields.tsx`（新設）を`DailyReportPage.tsx`に統合。資格試験の`/chat`とは別に読書用の`/reading-chat`対話パネルも追加
- [x] 読了レポートの表示・(再)生成ボタン、`reading_log` の時系列一覧表示 → 読了レポートは既存の`KnowledgeExportPage.tsx`（`RetrospectiveSection`）がgoal_idのみで動く汎用実装のため無改修で流用（バックエンドのcategory分岐により自動的に読了レポートとして生成される）。`reading_log`の時系列表示は`DailyReportViewPage.tsx`に`ReadingLogSummaryList.tsx`（新設）として追加
- [x] ダッシュボード：読書目標の表示（ノルマではなく残日数・直近記録日などの軽量表示）→ **バックエンドを小規模に拡張**（`GoalCardRead`に`category`/`book`フィールドを追加、`api/dashboard.py`がREADINGのカードを`book_service.get_book_progress`で組み立て直す）。フロントエンドは`GoalCardList.tsx`に`ReadingGoalCard`を追加
- [x] ナレッジエクスポート画面：読書目標が対象目標の選択肢に表示されることの確認 → 確認のみでコード変更なし（`KnowledgeExportPage.tsx`はgoalIdのみに依存する汎用実装のため、READING目標でも動作する。学習サマリ欄はEXAM向けの数値のままだが、これは元々コード変更不要とされていた範囲であり許容する）
- [x] ロケールファイル（ja.json）への文言追加 → `goals.new.category.*`・`goals.book.*`・`dailyReport.readingLog.*`・`dailyReport.readingChat.*`・`dailyReportView.readingLog.*`・`dashboard.goalCard.*`・`errors.BOOK_ALREADY_EXISTS`等を追加

**完了条件**

- [x] 種別に読書を選ぶと、資格試験用の入力項目（科目・教材等）が一切表示されない
- [x] 日次報告で読書目標の想起入力ができ、確定後は読み取り専用になる（既存 `daily_record` の不変性を踏襲）
- [x] 読了レポートが画面から生成・閲覧できる
- [x] 全UI文言がロケールファイルから供給される
- [x] 既存の資格試験用画面の表示・動作に変化がない（バックエンド全673テスト・フロントエンドtsc/lint/vitest全102テストが無改修で通過）

**Phase17完了（2026-08-31）。次はPhase 18（読書機能 実環境検証）。**

**Phase17で追加対応した設計上の変更点（未計画だったが実装中に必要と判明したもの）**

- ダッシュボードのAPI（`GET /dashboard`、`GoalCardRead`）に`category`・`book`フィールドを追加した。当初計画では本フェーズはフロントエンドのみを想定していたが、DSH-06（読書目標の残日数・直近記録日・連続記録日数表示）を実現するには書籍の派生値をダッシュボード応答に含める必要があり、`book_service.get_book_progress`を`api/dashboard.py`から呼び出す小規模な追加を行った
- 進行中の読書目標・書籍の一覧を取得する専用エンドポイントは新設せず、フロントエンド側で`listGoals()`→`getGoal()`の合成（`listActiveReadingBooks`、`api/goals.ts`）で賄った。1目標1冊・少数の同時進行を前提とした割り切りであり、件数が多い場合はN+1になる（コメントに明記済み）

**実行時の検証について**：本フェーズはヘッドレスブラウザによる実画面確認を試みたが、実行環境の`frontend/dist`が既にビルド済みだったため起動時に実ブラウザが開き、かつAI基盤の開発キットがこのマシンに設定済みの認証情報を用いて実際に「今日の一言」APIへ1回発信してしまった（分離されたスクラッチDBを使ってもai.hostがapp_setting経由ではなく開発キット側の設定ファイルから解決されるため）。意図しない副作用と判断し直ちに停止・後始末した。そのため実画面での動作確認はバックエンド673テスト・フロントエンド102テスト・tsc・lintの通過をもって代え、実際のブラウザでの確認はPhase18（テッペイさんの手元、認証情報を把握した環境）に委ねる。

**注意点**

- 文字列リテラルをコンポーネントに直接書かない（Phase6と同じ注意点）

---

### Phase 18：読書機能 実環境検証

| 項目 | 内容 |
| --- | --- |
| 目的 | 実際の利用シーンで一連の流れを確認し、文書を確定する |
| 実施 | テッペイさんの手元で実施する |

**確認項目**

- [ ] 読書目標を1件作成し、開始〜複数日の日次報告（想起）〜読了〜読了レポート生成までの一連の流れが問題なく動作する
- [ ] AI応答の品質確認（ノルマ圧力を感じさせない文面か、想起の言語化を助ける内容になっているか）
- [ ] 資格試験目標と読書目標が同時に進行中でも相互に干渉しない（リソース配分・ダッシュボード表示など）
- [ ] 読了レポートのナレッジエクスポート（匿名化・Markdown出力）が実際にブログ記事の下書きとして使える品質か

**完了条件**

- 上記シナリオが問題なく動作する
- 各種ドキュメントの改訂履歴が最終化されている

---

### Phase 19：仕事機能の設計文書反映

| 項目 | 内容 |
| --- | --- |
| 目的 | 確定した仕事機能の設計を要件定義書・仕様書・設計書に反映し、Phase 20以降が設計書のみを参照して実装できる状態にする |
| 位置づけ | Phase 1〜18（資格試験・読書機能）とは独立した追加スコープ。既存フェーズの番号・完了条件・既存文書の資格試験・読書関連の記載は一切変更しない |
| 投入文書 | CLAUDE.md、運用設計ルール、本フェーズ自体（設計確定内容の書き起こしが実装対象） |

**設計確定内容（反映元）**

- `GoalCategory` に `WORK` を追加する。仕事は読書と同じ記録・活用型に分類されるが、月次・半期という周期でレポートを生成し続ける点が異なるため「定期報告型」という副分類を設ける（要件定義書6.11）
- `work_assignment`（案件情報。goal_idにUNIQUE制約＝1目標1案件）、`work_log`（業務記録。daily_record_id×work_assignment_idでUNIQUE）を新設する
- 仕事目標のライフサイクルは既存の `goal.status`（DRAFT/ACTIVE/PAUSED/CLOSED）をそのまま流用し、`work_assignment` 側に独自のstatus/completed_atは持たせない。`CLOSED_WITH_RESULT`への遷移は`POST /goals/{id}/close`にbody `with_result=true`を指定する方式とし、読書の`POST /books/{id}/complete`のような専用エンドポイントは設けない（案件終了時に追加で記録すべきデータが無いため）
- `goal_retrospective` テーブルを拡張し、`period_type`（RetrospectivePeriodType: MONTHLY/SEMI_ANNUAL）・`period_key`（"YYYY-MM"または"YYYY-H1"/"YYYY-H2"）列を追加する。新設テーブルではなく既存テーブルの拡張とし、EXAM総括レポート・READING読了レポートでは両列を常にNULLとして既存の意味を維持する
- AI連携は「日次報告フィードバック」「月次報告ドラフト」「半期評価レポート」の3段構成とし、週次要約は対象外とする。`AiPurpose`に`DAILY_FEEDBACK_WORK`／`GOAL_RETROSPECTIVE_WORK_MONTHLY`／`GOAL_RETROSPECTIVE_WORK_SEMIANNUAL`を追加し、それぞれ`prompt_template`に新規行、`ai.assistant_uid.*`に新規設定キーを追加する
- 「今日の一言」（DAILY_MESSAGE）は読書と異なり仕事目標を対象に含める（要件定義書R-77）。対象目標の絞り込みに、EXAM専用の`list_active_exam_goals`とは別に、EXAM・WORKを返す関数を新設する必要がある（READINGのみ除外）
- 月次報告・半期評価は案件（goal）単位で生成する（要件定義書R-80）。半期は3月〜8月（H1）・9月〜翌2月（H2）という会社の人事評価サイクルに合わせた区切りとする（一般的な暦半期・会計年度上期下期とは異なる）
- 月次報告・半期評価はアプリ内でドラフトを生成するのみとし、Microsoft Forms等の外部フォームへの自動送信は行わない（Microsoft Formsには外部から回答を送信する公式APIが存在しないため。要件定義書R-78背景参照）
- 既存の`POST /goals/{id}/retrospective`（EXAM/READING専用）はWORK目標に対して恒久的にVALIDATION_ERRORで拒否する（月次報告・半期評価という別エンドポイントを新設するため）
- ナレッジエクスポート機能はコード変更なしで流用する（仕事目標も既存の対象目標選択肢に含まれることを仕様書に明記するのみ）。ただし対象goalに紐づく全`goal_retrospective`（period_type問わず）をエクスポート対象に含める

**実装対象（ToDo）**

- [x] 要件定義書：仕事目標に関する要件項目を追加する（案件登録、日次業務記録、月次報告・半期評価レポート生成、既存機能との差異）→ 6.11（R-72〜R-81）、5.3、12章（O-05）に反映済み（このセッションで反映済みのため本フェーズの対象外だが、以降の記述はこの内容を一次情報源とする）
- [x] 仕様書：目標作成フォームへの種別選択拡張、日次報告画面の種別分岐、目標詳細画面（仕事用タブ構成）、月次報告・半期評価の表示・生成導線、API一覧への新規・変更エンドポイント追加、ダッシュボード・今日の一言の対象範囲変更、バリデーション、未決事項の追加 → 3.2・6.1・6.2・6.5・6.10・7.1・8.1〜8.4・8.9.1・10・12章に反映済み（ロケールキーの個別列挙はPhase23の実装時に行う）
- [x] 設計書 データ構造編：`GoalCategory.WORK`、`work_assignment`／`work_log`テーブル定義、`goal_retrospective`の`period_type`／`period_key`拡張、ER図・カスケード関係表の更新、`AiPurpose`／`ConversationScope`／`RetrospectivePeriodType`拡張、`ai.assistant_uid.*`・`ai.work_recent_log_days`設定キー追加、6章のAPI一覧・エラーコード、7章のエクスポート構造、8章のディレクトリ構成（Phase20〜22実装予定箇所の明記） → 4章・5.1〜5.4・5.6・6.2・6.3・7章・8章に反映済み
- [x] 設計書 ロジック・プロンプト編：`DAILY_FEEDBACK_WORK`（17.8）／`GOAL_RETROSPECTIVE_WORK_MONTHLY`（17.9）／`GOAL_RETROSPECTIVE_WORK_SEMIANNUAL`（17.10）の注入変数一覧と初期プロンプト文面（ノルマ達成圧力を含まない文面とすること）、仕事目標の算出ロジック（進捗算出・連続記録日数・業務記録の時系列注入・月次/半期の期間判定）を新設した第22章に集約、18章のエクスポート生成ロジック・20章の検証観点への追記 → 1章・17.8〜17.10・18.1〜18.3・20章・新設22章に反映済み。旧22章（未決事項）は23章へ繰り下げ
- [x] 各文書の改訂履歴を更新する（本ファイル「実装フェーズ分割計画書」自身の改訂履歴に、Phase 19〜24（仕事機能）追記の旨を記載することを含む）→ 完了（本ファイル改訂履歴1.1（改7）、他3文書も改訂履歴に追記済み）

**Phase 19完了（2026-09-03）。次はPhase 20（仕事データモデル拡張）から着手する。**

**完了条件**

- 各文書に仕事機能の記載が反映され、Phase 20以降がこれらの文書のみを参照して実装に着手できる
- 既存の資格試験・読書関連の記載に差分がないこと（diffで確認）
- CODING_RULES.md のゼロハードコーディング・命名規則・置き場所ルールとの整合が取れている

---

### Phase 20：仕事データモデル拡張

| 項目 | 内容 |
| --- | --- |
| 目的 | 仕事目標のためのDBスキーマを追加する |
| 投入文書 | CLAUDE.md、運用設計ルール、設計書 データ構造編（Phase19で反映した仕事関連節） |

**実装対象**

- [x] `GoalCategory` enum への `WORK` 追加（backend/app/constants/enums.py）。あわせて`AiPurpose`／`ConversationScope`に`DAILY_FEEDBACK_WORK`／`GOAL_RETROSPECTIVE_WORK_MONTHLY`／`GOAL_RETROSPECTIVE_WORK_SEMIANNUAL`、`RetrospectivePeriodType`（MONTHLY/SEMI_ANNUAL）を追加
- [x] `work_assignment` テーブルの新設（`goal_id` にUNIQUE制約）→ `backend/app/models/work.py`
- [x] `work_log` テーブルの新設（`work_assignment_id` と `daily_record_id` の組み合わせにUNIQUE制約）→ `backend/app/models/record.py`
- [x] `goal_retrospective` への `period_type`／`period_key`／`target_goal_text`／`business_summary`／`achievement_score`／`achievement_reflection`／`next_goal_text`／`report_notes`／`edited_at` 列追加のマイグレーション（2026-09-04の設計改訂、データ構造編1.1（改12））。既存行は全てNULLのまま遡及設定不要。デフォルト値なし・NULL許容で追加するのみ
- [x] 全SQLAlchemyモデルへの反映（`models/__init__.py`にWorkAssignment/WorkLog登録）
- [x] `prompt_template` への初期データ追加（`DAILY_FEEDBACK_WORK`、`GOAL_RETROSPECTIVE_WORK_MONTHLY`、`GOAL_RETROSPECTIVE_WORK_SEMIANNUAL`）→ `backend/app/init/prompt_texts.py`（ロジック・プロンプト編17.8〜17.10から一字一句転記）、`backend/app/init/seed_data.py`
- [x] `app_setting` への初期データ追加（`ai.assistant_uid.daily_feedback_work`、`ai.assistant_uid.goal_retrospective_work_monthly`、`ai.assistant_uid.goal_retrospective_work_semiannual`、`ai.work_recent_log_days`）→ 未選定のアシスタントUIDは空文字で初期投入（Phase22で疎通確認のうえ補う）

**完了条件**

- [x] `alembic upgrade head` でスキーマが構築される
- [x] `work_assignment.goal_id` のUNIQUE制約により1目標1案件が保証される
- [x] `work_log`が`(work_assignment_id, daily_record_id)`単位で一意登録される
- [x] `goal_retrospective`に`period_type`／`period_key`を指定した複数レコード（同一goal・異なるperiod_key）が保存できる。ただし同一goal・同一period_type・同一period_keyでは常に1行のみ（サービス層でupsertとして扱う。データ構造編5.4「WORKでは常に一意」）
- [x] 初期データ投入が重複しない（再起動時も同様）
- [x] `material`／`exam_subject`／`book`／`reading_log`等の既存テーブルに変更がない

**注意点**

- マイグレーションSQLを目視レビューすること（Phase14と同じ注意点）
- プロンプトテンプレートの初期文面はPhase19で確定した文面から転記し、要約・改変をしないこと
- `goal_retrospective`の一意制約変更（`(goal_id, period_type, period_key, is_anonymized)`）が既存のEXAM/READING運用（period_type・period_key共にNULL）と両立することをテストで確認すること

**Phase20完了（2026-09-04）。バックエンド全739テスト・カバレッジ100%・ruff検査通過を確認済み。`alembic upgrade head`→`downgrade -1`→`upgrade head`のサイクルも確認済み。新設マイグレーション`e1f4a9c3b6d8`（`work_assignment`／`work_log`テーブル作成、`goal_retrospective`への9列追加とUNIQUE制約`(goal_id, period_type, period_key, is_anonymized)`追加）はSQL目視レビュー済み、既存データがある状態での列追加も`test_goal_retrospective_migration_preserves_existing_exam_rows_with_data`で検証済み。既存のEXAM/READING関連モデル・マイグレーション・テストは無改修。次はPhase 21（仕事目標・記録API）から着手する。**

---

### Phase 21：仕事目標・記録API

| 項目 | 内容 |
| --- | --- |
| 目的 | 仕事目標のCRUDと日次記録APIの種別分岐を実装する（AI連携を除く） |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書・設計書 データ構造編（Phase19で反映した仕事関連節） |

**実装対象**

- [x] 目標作成・更新APIの `category=WORK` 対応（`exam_subject`／`material`／`load_profile`関連の入力・バリデーションを要求しない。読書と同じ分岐パターンを踏襲）
- [x] `work_assignment` のCRUD → `work_service.py`（新設）。作成・更新ともに目標配下のネストパス`POST`／`PATCH /goals/{id}/work-assignment`（`api/goals.py`。書籍と異なり独立した`/work-assignments/{id}`は設けない）
- [x] 日次記録APIの分岐：`category=WORK`の目標に対しては`study_log`ではなく`work_log`を受け付ける（`daily_record`本体・確定ロジックは無改変で共用）→ `record_service.py`に`WorkLogItem`・`_apply_work_logs`を追加、`register_progress`/`finalize_record`に`work_items`引数を追加（デフォルトNoneで後方互換）
- [x] 経過日数・直近記録日・連続記録日数・直近の月次報告有無の実装 → `work_service.get_work_assignment_progress`。書籍と同じく`GET /goals/{id}/plan`は実装せず`WorkAssignmentRead`への埋め込み方式とする
- [x] 目標クローズ処理：`POST /goals/{id}/close`にbody `with_result`（真偽値、既定false）を追加し、WORKに限り`true`指定で`CLOSED_WITH_RESULT`へ遷移できるようにする（EXAM/READINGでは`with_result`指定自体を拒否）
- [x] バリデーション：`work_log.body` 必須（数値項目を持たないため他の数値バリデーションなし）
- [x] `resource_ratio` バリデーションから仕事目標を除外する（読書と同じ分岐パターン）

**完了条件**

- [x] `category=EXAM`／`category=READING`の既存エンドポイントの挙動に変化がない（既存テストが無改修で全て通過）
- [x] `category=WORK`の目標で科目・教材関連APIを呼んでも不整合が起きない（対象外として扱われる）
- [x] 1目標に2件目の`work_assignment`を追加しようとすると拒否される（`WORK_ASSIGNMENT_ALREADY_EXISTS`）
- [x] `work_log`が`daily_record`×`work_assignment`単位で一意登録される
- [x] 仕事目標が`resource_ratio`の合計計算に算入されない
- [x] `POST /goals/{id}/close`に`with_result=true`を指定するとWORK目標が`CLOSED_WITH_RESULT`へ遷移し、EXAM/READINGでは同指定がVALIDATION_ERRORで拒否される

**Phase15で追加対応した既存機能への回帰防止（読書のケース）と同様の観点を確認すること**：仕事目標の作成・進行が可能になったことで、既存の資格試験用・読書用AI機能が仕事目標のデータを誤って取り込まないか確認する（本フェーズはAI連携を含まないため実害は生じないはずだが、`list_active_exam_goals`がWORKを含めていないことをテストで確認する）。

**注意点**

- サービス層でHTTP例外を出さない（ドメイン例外→API層変換の既存原則を踏襲）
- AI連携は本フェーズに含めない。報告確定は`chat_message`が空の状態で成立させる

**Phase21完了（2026-09-05）。**`work_service.py`（新設）・`api/work.py`（新設、`serialize_work_assignment`）・`schemas/work.py`（新設）を追加し、`goal_service.py`（`update_goal`・`activate_goal`・`close_goal`への`with_result`追加・`_cascade_delete_activity_logs`・`delete_archived_goal`）・`record_service.py`（`WorkLogItem`・`_apply_work_logs`・`register_progress`/`finalize_record`への`work_items`引数）・`api/goals.py`（`POST`/`PATCH /goals/{id}/work-assignment`、`close_goal`エンドポイント）・`api/records.py`・`schemas/goal.py`・`schemas/record.py`・`api/errors.py`・`exceptions.py`を拡張した。設計どおり、案件情報の作成・更新は書籍と異なり目標配下のネストパスに統一し、独立した`/work-assignments/{id}`は設けていない。バックエンド全789テスト・カバレッジ100%・ruff検査通過を確認済み（既存の資格試験・読書関連コードは新しいカテゴリ分岐の追加以外無改修）。次はPhase 22（仕事AI連携）から着手する。

---

### Phase 22：仕事AI連携

| 項目 | 内容 |
| --- | --- |
| 目的 | 仕事用の日次フィードバックと月次報告ドラフト・半期評価レポート生成を実装する |
| 投入文書 | CLAUDE.md、運用設計ルール、設計書 ロジック・プロンプト編（Phase19で反映した仕事プロンプト節、第16章のAI連携基盤は流用） |

**実装対象**

- [x] `DAILY_FEEDBACK_WORK` 呼び出しの実装 → `work_feedback_service.py`（新設、`reading_feedback_service.py`と対）。既存の`ai/client.py`・`ai/conversation.py`・`ai/prompt_builder.py`・`ai/rate_limiter.py`・`ai/logger.py`・`ai/orchestration.py`は無改変で流用し、purpose分岐のみ追加
- [x] `GOAL_RETROSPECTIVE_WORK_MONTHLY`／`GOAL_RETROSPECTIVE_WORK_SEMIANNUAL`呼び出しの実装 → `work_report_service.py`（新設）。対象work_assignmentの`work_log.body`を対象期間で時系列連結した変数を注入し、前期間の`goal_retrospective`（存在すれば）を注入する
- [x] `get_latest_retrospective`の拡張：`period_type`／`period_key`を指定した絞り込みに対応させる（データ構造編5.4「『最新の総括レポートを取得する』ロジックの拡張」）
- [x] 月次・半期の期間判定ロジックの実装（ロジック・プロンプト編22.5）。対象月・対象半期の算出、前月・前半期の算出
- [x] `POST /goals/{id}/monthly-report`・`GET /goals/{id}/monthly-report`・`PATCH /goals/{id}/monthly-report`・`POST /goals/{id}/semiannual-review`・`GET /goals/{id}/semiannual-review`・`PATCH /goals/{id}/semiannual-review`の実装（`api/closure.py`または新設ファイル）
- [x] AI応答のパース処理の実装（ロジック・プロンプト編22.6）：見出し文字列（17.9は5節、17.10は4節）で応答テキストを分割し、`business_summary`／`achievement_score`／`achievement_reflection`／`next_goal_text`／`report_notes`の各列へ格納する。「達成度」節が1〜5の数字以外（「対象外」を含む）の場合は`achievement_score`をNULLとする。見出しが検出できない項目があっても例外にせず空文字のまま保存する（AI応答全文は既存の`ai_log`に残るため情報は失われない）
- [x] `body`組み立てテンプレートの実装（ロジック・プロンプト編22.6のMarkdownテンプレート）。生成時・PATCH保存時のいずれも、構造化列から`body`を再組み立てする（`body`を直接受け取るAPIは設けない）
- [x] `PATCH`の挙動：対象行が存在すれば該当列を更新し`edited_at`を現在時刻に設定、存在しなければ新規作成する（前期の記録が無い初回利用者向けの手動シード、要件定義書R-83）。生成専用列（`business_summary`等）を指定せずに`target_goal_text`のみ更新するリクエストを許容する
- [x] `POST /records/{date}/work-chat`の実装（DAILY_FEEDBACK_WORK。`chat_message.purpose`で資格試験・読書との対話履歴混入を防止する既存の仕組みをそのまま適用）
- [x] 既存の`POST /goals/{id}/retrospective`がWORK目標に対して恒久的にVALIDATION_ERRORを返すことを確認・テストする（読書がPhase15〜16で経由した暫定ガードとは異なり、本フェーズの実装時点で最初から恒久仕様として実装する）
- [x] 「今日の一言」（DAILY_MESSAGE）の対象拡張：`list_active_exam_goals`とは別に、EXAM・WORKを対象とする関数（例：`list_daily_message_target_goals`）を新設し、`daily_message_service`が参照する対象をこの関数に切り替える。`{{goal_summary}}`・`{{progress_summary}}`・`{{recent_activity}}`のビルダーをgoal.categoryで分岐させ、WORK用の変数ビルダー（22.2の経過日数・直近記録日・連続記録日数・直近の月次報告有無を用いる）を新設する
- [x] 週次要約の生成判定ロジックから仕事目標を除外することの確認 → 変更不要（`weekly_summary_service`は`Material.goal_id`起点のため仕事目標は元から対象外）。回帰防止テストを追加して確認

**完了条件**

- [x] 仕事目標の日次報告でWORK用プロンプトによるフィードバックが取得できる
- [x] 月次報告ドラフト・半期評価レポートが生成・再生成できる（案件が進行中のまま生成できることを含む）
- [x] 前月・前半期の報告が存在する場合、その内容を踏まえたドラフトが生成される（前月・前半期の報告が無い場合もエラーにならない）
- [x] 今日の一言が仕事目標を対象に含み、読書目標は引き続き対象外であることをテストで確認する
- [x] 資格試験用・読書用のプロンプト・呼び出し・アシスタント設定に影響がない（相互の対話履歴が混入しないことを検証する回帰テストを追加）
- [x] 半期の期間判定（3月〜8月、9月〜翌2月、閏年の2月29日を含む）が正しく動作する
- [x] AI呼び出しが失敗しても実績（業務記録）入力が失われない

**Phase22で追加対応が必要な可能性がある事項**：Phase16（読書AI連携）では`chat_message.purpose`列の追加、AI対話エンドポイントの分離という設計変更が実装時に必要になった。仕事目標でも同様に、日次報告フィードバックとの同時進行時の対話履歴分離は`purpose`列の既存の仕組み（`DAILY_FEEDBACK_WORK`という値を追加するだけ）で対応可能な想定だが、実装時に想定外の分岐が必要になった場合は本ファイルの完了報告に追記すること。

**注意点**

- Web検索・ナレッジ検索の明示的無効化など、既存のAI連携制約は仕事用呼び出しにも同様に適用する（`ai/client.py`を無改変で流用しているため自動的に満たされる）
- ノルマ達成・生産性評価を前提とした文面にしないこと（Phase19で確定した初期文面から逸脱しない。17.8〜17.10のプロンプトは転記のみで改変しない）
- 月次報告・半期評価は「アプリ内でドラフトを生成するのみ」であり、Microsoft Forms等への自動送信機能を実装しないこと（要件定義書R-78背景）

**Phase22完了（2026-09-05）。**`work_feedback_service.py`（新設、DAILY_FEEDBACK_WORK）・`work_report_service.py`（新設、月次報告・半期評価の生成・PATCH編集・期間判定22.5・AI応答パース＋body組み立て22.6を集約）を追加。`retrospective_service.get_latest_retrospective`をperiod_type／period_key対応に拡張し（既存のEXAM/READING呼び出しは無改変で動作）、`generate_retrospective`にWORK目標への恒久的なVALIDATION_ERRORガードを追加。`ai_context_service.py`にWORK用の日次・月次・半期向け変数ビルダー（`build_daily_work_summary_text`等）と、今日の一言の対象をEXAM・WORKに拡張する`list_daily_message_target_goals`を追加（READINGは引き続き対象外）。API層は`api/closure.py`に`POST`/`GET`/`PATCH /goals/{id}/monthly-report`・`/semiannual-review`を、`api/records.py`に`POST /records/{date}/work-chat`を追加した。バックエンド全876テスト・カバレッジ100%・ruff検査通過（既存の資格試験・読書関連コードは新しいカテゴリ分岐の追加以外無改修）。次はPhase 23（仕事フロントエンド）から着手する。

**追記（2026-09-05）**：`ai.assistant_uid.daily_feedback_work`（AI-08）の既定値を、空文字（未選定）からAI-06（読書日次報告フィードバック）と同一のGPT-5.4-mini「高速」へ選定した（仕様書1.1（改11）S-09一部解消、データ構造編1.1（改14））。既存DBの空欄値を補う一度きりの補正は`backfill_reading_assistant_defaults`と共通の内部関数（`_backfill_assistant_defaults`）に切り出した`backfill_work_assistant_defaults`で実装。`ai.assistant_uid.goal_retrospective_work_monthly`／`_semiannual`（AI-09・AI-10）は低頻度・高品質重視の判断に実環境検証が必要なため、引き続き未選定のまま据え置く。

---

### Phase 23：仕事フロントエンド

| 項目 | 内容 |
| --- | --- |
| 目的 | 仕事目標の作成・記録・閲覧をUIで可能にする |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書（Phase19で反映した仕事関連節） |

**実装対象**

- [x] 目標作成フォームの種別選択に「仕事」を追加。選択に応じた入力項目の出し分け → `GoalsListPage.tsx`の`GOAL_CATEGORIES`に`WORK`を追加
- [x] 目標詳細画面：仕事目標用のタブ構成（基本情報＋案件情報の簡略構成、月次報告タブ・半期評価タブ）→ `GoalDetailPage.tsx`に`WORK_TABS`を追加、新設`WorkAssignmentTab.tsx`が案件情報の登録・編集を、新設`WorkReportTab.tsx`が月次報告・半期評価の生成・閲覧を担う
- [x] 日次報告画面：アクティブな仕事目標がある場合の入力欄分岐（業務記録の自由記述）→ `WorkLogFields.tsx`（新設）を`DailyReportPage.tsx`に統合。資格試験の`/chat`・読書の`/reading-chat`とは別に仕事用の`/work-chat`対話パネルも追加
- [x] 月次報告・半期評価の表示・(再)生成ボタン、対象期間の選択、`work_log`の時系列一覧表示 → `WorkReportTab.tsx`、`DailyReportViewPage.tsx`に`WorkLogSummaryList.tsx`（新設）
- [x] 月次報告・半期評価のレビュー用フォーム（要件定義書R-82・R-83）：`target_goal_text`・`business_summary`・`achievement_score`（選択式）・`achievement_reflection`・`next_goal_text`・`report_notes`（月次報告のみ）を編集可能な入力欄として表示し、「保存」で`PATCH`を呼び出す → `WorkReportTab.tsx`に統合
- [x] 「Markdownをダウンロード」ボタン：保存済みの`body`をブラウザでファイルダウンロードする（新規バックエンドエンドポイントは設けない。フロントエンドでBlob生成） → `WorkReportTab.tsx`。`DataManagementPage.tsx`と共通の`downloadBlob`ユーティリティを新設し重複を回避（CLAUDE.md DRYの原則）
- [x] ダッシュボード：仕事目標の表示（ノルマではなく経過日数・直近記録日・連続記録日数・直近の月次報告有無の軽量表示）に加え、月次報告タブへ直接遷移するボタンを追加 → `GoalCardList.tsx`に`WorkGoalCard`を追加。今日の一言が仕事目標にも表示されることを確認（読書は非表示のまま）
- [x] ナレッジエクスポート画面：仕事目標が対象目標の選択肢に表示され、複数期間の月次報告・半期評価が一覧表示されることの確認 → `KnowledgeExportPage.tsx`にWORK分岐を追加。実装過程で`export_service.py`がWORK目標に一切対応していなかった設計不足（`RetrospectiveSection`がWORKに対して恒久拒否となった`generate_retrospective`を呼び出しクラッシュする経路）を発見し、バックエンドに`_build_work_export_data`等を追加して解消
- [x] ロケールファイル（ja.json）への文言追加 → `goals.new.category.*`・`goals.workAssignment.*`・`goals.workReport.*`・`dailyReport.workLog.*`・`dailyReport.workChat.*`・`dailyReportView.workLog.*`・`dailyReportView.workChatHistory.*`・`dashboard.goalCard.*`・`knowledgeExport.*`の仕事向けキー・`errors.WORK_ASSIGNMENT_ALREADY_EXISTS`を追加
- [x] 目標一覧（SC-02）での種別表示（種別バッジ等）の詳細デザインを決定する（未決事項S-10の解消）→ S-08（読書）と同じテキストラベル方式を踏襲し一部解消（詳細は仕様書S-10参照）。色付きバッジは見送り

**完了条件**

- [x] 種別に仕事を選ぶと、資格試験用の入力項目（科目・教材等）が一切表示されない
- [x] 日次報告で仕事目標の業務記録入力ができ、確定後は読み取り専用になる
- [x] 月次報告・半期評価が画面から生成・閲覧でき、過去の期間も選択して閲覧できる
- [x] 全UI文言がロケールファイルから供給される
- [x] 既存の資格試験用・読書用画面の表示・動作に変化がない（バックエンド・フロントエンド双方の既存テストが無改修で通過）

**注意点**

- 文字列リテラルをコンポーネントに直接書かない
- 実行環境の制約でヘッドレスブラウザによる実画面確認が困難な場合は、Phase17と同様に自動テストで代替し、実画面確認はPhase24に委ねる

**Phase23完了（2026-09-05）。**API層（`api/goals.ts`・`api/closure.ts`・`api/records.ts`）に案件情報・月次報告・半期評価・work-chat用の型・関数を追加。`WorkAssignmentTab.tsx`・`WorkReportTab.tsx`・`WorkLogFields.tsx`・`WorkLogSummaryList.tsx`・`workLogForm.ts`（新設、`readingLogForm.ts`と対のユニットテスト`workLogForm.test.ts`を含む）を新設し、`GoalDetailPage.tsx`・`DailyReportPage.tsx`・`DailyReportViewPage.tsx`・`GoalCardList.tsx`・`CloseGoalModal.tsx`・`KnowledgeExportPage.tsx`にWORK分岐を統合した。実装過程で2件の設計不足・不具合を発見し合わせて修正：(1) `export_service.py`がWORK目標に一切対応しておらず、ナレッジエクスポート画面から総括レポート生成を呼び出すと`generate_retrospective`のWORK恒久拒否ガード（Phase22実装）によりクラッシュする経路があったため、`_build_work_export_data`・`_render_work_markdown`等をバックエンドに追加、(2) ナレッジエクスポートの概要欄が案件名（`goal.name`、必須）ではなく取引先呼称（`work_assignment.client_name`、任意項目でNULL可）を表示しており、取引先呼称が未入力の案件では概要が空欄化していたため、`_build_work_assignment`に`goal.name`を含めるよう修正。バックエンド全886テスト・カバレッジ100%・ruff検査通過、フロントエンド全126テスト・`tsc -b`・oxlint（既存の無関係な警告2件を除き0件）を確認済み。ヘッドレスブラウザでの実画面確認は実行環境の制約により見送り、かわりに独立したSQLiteデータベースを用いた別ポートのバックエンドインスタンス上で実APIを用いたエンドツーエンドのスモークテスト（目標作成→案件情報登録→着手→日次業務記録→ダッシュボード反映→月次報告の手動シード（PATCH）→クローズ→ナレッジエクスポートプレビュー）を実施し、既存の本番データベース・利用中セッションには一切影響を与えないことを確認したうえで実施した。次はPhase 24（仕事機能 実環境検証、テッペイさんの手元で実施）に進む。

---

### Phase 24：仕事機能 実環境検証

| 項目 | 内容 |
| --- | --- |
| 目的 | 実際の利用シーンで一連の流れを確認し、文書を確定する |
| 実施 | テッペイさんの手元で実施する |

**確認項目**

- [ ] 仕事目標を1件作成し、着手〜複数日の日次報告（業務記録）〜月次報告ドラフト生成〜（可能であれば半期をまたいで）半期評価レポート生成〜クローズまでの一連の流れが問題なく動作する
- [ ] AI応答の品質確認（ノルマ達成圧力を感じさせない文面か、月次報告・半期評価が社内フォームへの転記にそのまま使える具体性を持つか）
- [ ] 資格試験目標・読書目標・仕事目標が同時に進行中でも相互に干渉しない（リソース配分・ダッシュボード表示・今日の一言の対象範囲など）
- [ ] 今日の一言が仕事目標を対象に含み、読書目標は引き続き対象外であることを実画面で確認する
- [ ] 月次報告・半期評価のナレッジエクスポート（匿名化・Markdown出力）が実際に社内フォームへの転記の下書きとして使える品質か

**完了条件**

- 上記シナリオが問題なく動作する
- 各種ドキュメントの改訂履歴が最終化されている

---

### Phase 25：分析・カレンダー・ダッシュボードの目標ごと表示統一

| 項目 | 内容 |
| --- | --- |
| 目的 | 分析タブ選択時に読書・仕事目標のデータが表示されない不具合を機に、「目標ごとに機能を分離する」という設計思想（Phase19〜24・6.5改8）をカレンダー・ダッシュボードにも横展開し、アプリ全体で一貫させる |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書（第6.1・6.4・6.8節、1.1改14） |

**実装対象**

- 分析画面（SC-09）：目標のカテゴリに応じたタブ出し分け（EXAM/READING/WORK）。READING/WORK向けに `GET /analytics/reading-logs`・`GET /analytics/work-logs` を新設し、「読書記録」「業務記録」タブを追加
- カレンダー（SC-05）：進行中の全目標を集約表示する方式から、GoalTabBar（日次報告と共通コンポーネント）で選択した1目標分の補助表示のみを表示する方式に変更
- ダッシュボード（SC-01）：改8で対象外としていた判断を撤回し、GoalTabBarでの目標切り替え方式に変更（今日の一言・警告バナー・本日のノルマ・目標カード・統計サマリが対象）
- 日次報告・カレンダー・ダッシュボードで対象目標の解決ロジックが重複しないよう、共通ヘルパー `resolveTargetGoalId`（frontend/src/features/record/）に集約

**完了条件**

- 読書・仕事目標を選択した状態で分析タブを開くと、教材依存タブではなく読書記録／業務記録タブが表示される
- カレンダー・ダッシュボードで着手中の目標が2件以上のとき、目標タブの切り替えに応じて表示内容が変わる
- 着手中の目標が0〜1件のときは目標タブが表示されず、従来どおりの表示のまま
- 既存のバックエンド・フロントエンドのテストが引き続き成功する

---

### Phase 26：日次フィードバックAI対話の目標単位分離

| 項目 | 内容 |
| --- | --- |
| 目的 | 分析タブ「成長記述」が全目標のAI日次報告フィードバックを無条件に混在表示する不具合を機に、対症療法（purposeフィルタのみ）ではなく、日次フィードバックの会話そのものを「カテゴリ単位の共有スレッド」から「目標単位」へ分離する根本対応を行う（未決事項L-07の解消方針転換） |
| 投入文書 | CLAUDE.md、運用設計ルール、仕様書（第6.5・6.8節、1.1改15）、設計書 データ構造編（1.1改18）、設計書 ロジック・プロンプト編（23章L-07、1.1改12） |

**実装対象**

- `chat_message`に`goal_id`列を追加（マイグレーション`d8f21a6c4b3e`、NULL許容・FK→goal.id・ondelete=CASCADE）。移行前の行はgoal_id=NULLのまま引き継ぐ
- `daily_feedback_service`・`reading_feedback_service`・`work_feedback_service`を、カテゴリ内の全ACTIVE目標をまとめてプロンプト化する方式から、指定された単一goalのみを扱う方式へ変更（`goal_id`引数を必須化、カテゴリ・ACTIVE状態を検証）。あわせて、選択中でない目標の下書き入力（study_logs/diary_entries等）がプロンプトに混入していた既存の別件バグを修正
- `ai_conversation.ensure_conversation`の呼び出しを`goal=None`から`goal=goal`へ変更し、目標ごとに別会話が作られるようにする
- `ChatRequest`/`ReadingChatRequest`/`WorkChatRequest`に`goal_id`（必須）、`ChatMessageRead`に`goal_id`（NULL許容）を追加
- `GET /analytics/growth-descriptions`を`goal_id`必須化し、指定目標宛て＋同カテゴリの未割り当て（goal_id=NULL）を返す方式に変更。新規`PATCH /analytics/growth-descriptions/{message_id}`を追加し、未割り当てメッセージへの目標の手動割り当てに対応（分析タブ上のプルダウンUI）
- プロンプトテンプレートの「複数目標がある場合の注意」セクションを削除（DAILY_FEEDBACKのみ対象。マイグレーション`c1a5f9e3d7b2`、is_customized=Falseの行のみ更新）
- ナレッジエクスポートの`_build_ai_dialogue`（export_service.py）を、教材のstudy_logがある日付に基づく間接的な絞り込みから`chat_message.goal_id`による直接の絞り込みへ変更し、同日に他目標のAI対話が混入する既存の別件バグを修正
- `DailyReportPage`/`DailyReportViewPage`の送信ペイロード・履歴表示フィルタをgoal_id対応に追随
- `GrowthDescriptionTab`/`AnalyticsPage`を目標単位化し、未割り当てメッセージの目標割り当てUIを追加

**完了条件**

- 同一カテゴリの目標が2件以上同時進行している場合、目標タブを切り替えて送信したAI対話が、選択中の目標以外のタブの対話履歴に表示されない
- 分析タブ「成長記述」で目標を選ぶと、その目標宛ての応答のみが表示される
- 移行前のレガシー応答（goal_id未確定）に対し、分析タブ上のプルダウンから目標を手動で割り当てられる
- ナレッジエクスポートのAI対話履歴に、同日の他目標の対話が混入しない
- 既存のバックエンド・フロントエンドのテストが（前提の変更に伴う書き換えを含め）引き続き成功し、カバレッジ100%を維持する

---

## 4. 並行実行の可否

```
Phase 1
   ↓
Phase 2 ────────────────┐
   ↓                    │
Phase 3 ──────┬─────────┤
   ↓          ↓         ↓
Phase 4    Phase 6   （テスト拡充）
   ↓          ↓
Phase 5 ──────┤
   ↓          ↓
   └──→ Phase 7, 8, 9（順不同）
              ↓
          Phase 10
              ↓
          Phase 11
```

Phase 6 は Phase 3 完了後に着手可能であり、Phase 4・5 と並行できる。ただし単独作業のため、実際には順次実行が現実的である。

---

## 5. 各フェーズ共通の指示事項

Claude Code へ渡す際、各フェーズの冒頭に以下を明示する。

```
# 共通指示

1. CLAUDE.md および運用設計ルールを必ず先に読むこと
2. 本フェーズのスコープ外を実装しないこと
3. 設計書に記載のない仕様を勝手に決めないこと。
   判断が必要な箇所は実装を止めて確認すること
4. 文字列リテラルおよび数値リテラルをソースコードに
   直接記述しないこと
5. サービス層にHTTPに関する知識を持ち込まないこと
6. 日次ノルマ、残量、現在周回をデータベースに保存しないこと
7. AI基盤の開発キットの仕様は変更できない。
   制約はアプリケーション側の設計で吸収すること
8. 実装完了後、完了条件を自己検証し結果を報告すること
```

特に3は重要である。設計書に未記載の箇所を推測で埋められると、後から発見が困難な仕様の齟齬が生まれる。

---

## 6. リスクと対応

| リスク | 影響 | 対応 |
| --- | --- | --- |
| Phase 2 のロジック誤り | 全機能に波及 | テストを先に書き、境界値を網羅する |
| 周回対応の実装漏れ | 運用開始数週間で計画が破綻 | Phase 2 の完了条件に明示的に含める |
| Web検索の無効化漏れ | フィードバック品質の劣化 | Phase 5 の完了条件でコード上の確認を求める |
| 入力上限の想定違い | 対話の失敗 | 縮退機構を先に実装し、実測で閾値を調整する |
| 頻度制限への抵触 | 週次要約の遡及生成が失敗 | 呼び出し間隔の下限を実装する |
| プロンプト応答品質の不足 | 中核価値の毀損 | Phase 11 で反復調整する前提とする |
| コンテキスト飽和による品質低下 | 実装の一貫性喪失 | 1フェーズ1セッションを厳守する |
| 設計書の未決事項の放置 | 実装時の判断ぶれ | 各フェーズ着手前に該当する未決事項を解消する |

---

## 7. 実装着手前のチェックリスト

- [x] 設計書 v1.1 を確認した
- [x] CLAUDE.md に本アプリ固有の規約を追記した
- [x] 運用設計ルールとの整合を確認した（CLAUDE.md「運用設計ルールの所在」を参照。CODING_RULES.md + OPERATIONS.md を指す）
- [x] 技術選定（状態管理、グラフライブラリ、日付処理）を確定した（技術選定書 v1.0）
- [x] FastAPI の起動ポートを 8400 以外に決定した（8100）
- [x] AI基盤の開発キット仕様書を Phase 5 用に用意した（NewtonX ADK v0.10.5を導入済み。`newtonx_adk/docs/`配下の公式ドキュメントとソースコードを確認し、設計への反映も完了。ただし親メッセージ順序の不具合、認証方式の相違、テナント固有の404事象を検出済み。詳細はPhase 5節参照）
- [x] 祝日データの取得元を決定した（未決事項 D-01。技術選定書 v1.0 第6章で解消）

---

*本計画は実装の進行に応じて改訂する。フェーズの統合や分割は、実際の作業量に応じて判断する。*
