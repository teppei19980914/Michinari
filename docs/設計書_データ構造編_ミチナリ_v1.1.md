# ミチナリ（Michinari）設計書 ─ データ構造編

| 項目 | 内容 |
| --- | --- |
| 文書名 | 設計書 データ構造編 |
| バージョン | 1.1 |
| 作成日 | 2026年8月22日 |
| 関連文書 | 要件定義書 v1.1、仕様書 v1.1、設計書 ロジック・プロンプト編 v1.1 |

### 改訂履歴

| 版 | 日付 | 変更内容 |
| --- | --- | --- |
| 1.0 | 2026-08-22 | 初版 |
| 1.1 | 2026-08-22 | material に予定周回数を追加。ai_conversation テーブルを新設（会話識別子の保持）。goal_retrospective テーブルを新設（総括レポート）。認証情報の保管方針を開発キット準拠に変更（5.8）。設定キーを追加。エクスポート関連APIを追加 |
| 1.1（改） | 2026-08-23 | NewtonX ADK（v0.10.5）実導入・ソースコード確認・実機テストの結果を反映。5.2にai.hostを追加、単一だったai.assistant_uidを用途別4キーに分割し既定値を選定、非対応アシスタント2種を明記。5.5のlast_parent_order説明を実態に合わせて修正、5.8の認証情報保管方針をPAT主経路に変更。D-01/D-02を解消済みに更新し、backup.retention_countを追加 |
| 1.1（改2） | 2026-08-23 | Phase3実装を反映し、8章のサービス層構成を実態に合わせて更新（goal_service.pyに集約する想定だったものを、目標/科目/教材/リソースの責務ごとにgoal_service.py・subject_service.py・material_service.py・resource_service.pyへ分割。schemas/・api/goals.py・api/materials.py・api/resources.py・api/errors.pyを追加）。6.2に GET/PUT /resources/day-boundary-hour を追加（実装フェーズ分割計画書Phase3実装対象「1日の境界時刻」、仕様書10章バリデーション対象への対応漏れを解消） |

---

## 1. 本文書の目的と範囲

本文書は、ミチナリのデータ構造、永続化方式、API構成、およびディレクトリ構成を定義する。

算出ロジックの数式定義およびプロンプト設計は「設計書 ロジック・プロンプト編」に記載する。

---

## 2. 技術構成

| 層 | 採用技術 | 選定理由 |
| --- | --- | --- |
| フロントエンド | React + TypeScript + Tailwind CSS | 開発者の習熟度 |
| バックエンド | FastAPI | AI基盤の開発キットがPython製であり、同一プロセスで動作可能 |
| ORM | SQLAlchemy 2.0 | 集計クエリの表現力。長期メンテナンスの安定性 |
| マイグレーション | Alembic | 段階的なスキーマ拡張を前提とするため必須 |
| データベース | SQLite | 単一利用者、ローカル動作。将来のPostgreSQL移行を阻害しない |

**ORM選定における除外検討**

Prisma Client Python は開発が終了しているため採用しない。SQLModel は記述量で優位だが、本アプリの中核である集計クエリで SQLAlchemy の API に降りる必要が生じ、二重の学習コストが発生するため採用しない。

**SQLite使用上の制約への対応**

| 制約 | 対応 |
| --- | --- |
| ALTER TABLE の制限 | Alembic の batch_alter_table を使用する |
| 日付型が存在しない | DATE型は ISO 8601 文字列として保存し、SQLAlchemy の Date 型で扱う |
| 真偽値型が存在しない | Boolean型は整数として保存し、SQLAlchemy の Boolean 型で扱う |
| 外部キー制約が既定で無効 | 接続時に `PRAGMA foreign_keys = ON` を実行する |

**起動ポート**

AI基盤の開発キットは、認証フローのフォールバック経路（PAT未設定時のブラウザ認証）でローカルの8400番ポートを使用する設定を既定に持つ。主経路のPAT認証ではこのポートは使用されないが、フォールバック経路を確実に動作させるため、アプリケーションサーバは引き続き8400番以外で起動する。既定値は8100とする。

---

## 3. 設計方針

### 3.1 派生値を保存しない

以下は一切保存せず、参照時に算出する。これが本アプリの中核的な設計判断である。

| 派生値 | 算出根拠 |
| --- | --- |
| 日次ノルマ | 残量、残計画日数、負荷係数 |
| 総作業量 | 総量、予定周回数 |
| 残量 | 総作業量、実績の累計 |
| 現在周回 | 総量、実績の累計 |
| 実効速度 | 実績の投下時間と完了分量（周回別） |
| 必要速度 | 残量、残計画日の利用可能時間 |
| 完了予測日 | 残量、実効速度、計画日の分布 |
| バッファ消費率 | バッファ日の実績有無 |

日次ノルマを保存しないことにより、未達分の繰り越し処理が不要になる。残量を残計画日で再配分する式が毎日評価されるため、負荷の平準化が構造的に成立する。

### 3.2 基準値のみ履歴として保存する

警告判定（日次ノルマが初期ノルマの一定倍率を超えたか）には比較対象が必要である。この基準値は `plan_baseline` テーブルに履歴として保存する。

リプラン実施時に新しい基準行を追加することで、リプラン後は新しい計画が基準となり、過去の乖離を引きずらない。

**この履歴は、総括レポートの分析根拠としても用いる。** いつ、何回、どの契機で計画を修正したかという情報は、合否要因の分析において最も重要な材料である。

### 3.3 不変性の担保

日次報告および学習実績は、確定後の更新をアプリケーション層で拒否する。データベース制約では表現せず、サービス層のガードとして実装する。

### 3.4 外部識別子の保持

AI基盤側に作成された会話の識別子はローカルに保持する。一覧取得APIによる検索を毎回行わない。これは通信回数の削減と、頻度制限への配慮による。

---

## 4. エンティティ関連

### 4.1 全体構造

```
【設定・マスタ系】

app_setting          （設定のキーバリュー）
prompt_template      （プロンプトテンプレート）
holiday              （祝日）
day_type_default     （曜日別の日種別既定値）
calendar_day_override（日付単位の日種別上書き）

resource_slot ──< resource_slot_weekday
（時間スロット）    （スロットの適用曜日）


【目標系】

goal ─┬─< exam_subject ──── exam_result
      │      │
      │      └─< material_subject >─┐
      │                              │
      ├─< material ──────────────────┘
      │      │
      │      └─< plan_baseline
      │
      ├─< load_profile
      │
      ├─< weekly_summary
      │
      ├─< goal_retrospective（総括レポート）
      │
      └─< ai_conversation（会話識別子）


【記録系】

daily_record ─┬─< study_log >──── material
              ├─< chat_message
              └─< record_comment

daily_message（今日の一言）
ai_log       （AI通信ログ）
```

### 4.2 関連の定義

| 親 | 子 | 多重度 | 削除時動作 |
| --- | --- | --- | --- |
| goal | exam_subject | 1対多 | CASCADE |
| goal | material | 1対多 | CASCADE |
| goal | load_profile | 1対多 | CASCADE |
| goal | weekly_summary | 1対多 | CASCADE |
| goal | goal_retrospective | 1対多 | CASCADE |
| goal | ai_conversation | 1対多 | CASCADE |
| exam_subject | exam_result | 1対1 | CASCADE |
| material | exam_subject | 多対多（material_subject） | CASCADE |
| material | plan_baseline | 1対多 | CASCADE |
| material | study_log | 1対多 | RESTRICT |
| daily_record | study_log | 1対多 | CASCADE |
| daily_record | chat_message | 1対多 | CASCADE |
| daily_record | record_comment | 1対多 | CASCADE |
| resource_slot | resource_slot_weekday | 1対多 | CASCADE |

`material` から `study_log` への削除は RESTRICT とする。実績が存在する教材の削除は、実績の消失を伴うため許可しない。教材を除外したい場合は無効化フラグを用いる。

---

## 5. テーブル定義

### 5.1 列挙型の定義

アプリケーション層で Enum として定義し、データベースには文字列として保存する。

| 型名 | 値 | 意味 |
| --- | --- | --- |
| GoalStatus | DRAFT | 下書き |
| | ACTIVE | 進行中 |
| | PAUSED | 一時停止 |
| | CLOSED_WITH_RESULT | クローズ（受験結果あり） |
| | CLOSED_WITHOUT_RESULT | クローズ（中断・辞退） |
| ExamDateType | RANGE | 期間（暫定） |
| | FIXED | 確定日 |
| DayType | PLAN | 計画日 |
| | BUFFER | バッファ日 |
| | OFF | 除外日 |
| Environment | ANY | 制約なし |
| | PC | PC環境 |
| | MOBILE | 携帯端末 |
| QualityMetricType | NONE | 記録しない |
| | OBJECTIVE | 客観正答率 |
| | SELF_SCORED | 自己採点得点率 |
| | SUBJECTIVE | 主観的手応え（5段階） |
| RecordState | PROGRESS_ONLY | 進捗のみ登録済 |
| | REPORTED | 報告済 |
| ChatRole | USER | 利用者の発言 |
| | ASSISTANT | AIの応答 |
| BaselineReason | INITIAL | 初回設定 |
| | REPLAN | リプラン実施 |
| | EXAM_DATE_FIXED | 受験日確定 |
| | MATERIAL_CHANGED | 教材構成の変更 |
| | CYCLE_CHANGED | 予定周回数の変更 |
| ExamResultType | PASS | 合格 |
| | FAIL | 不合格 |
| | PENDING | 未判定 |
| AiPurpose | DAILY_FEEDBACK | 日次報告フィードバック |
| | WEEKLY_SUMMARY | 週次要約生成 |
| | DAILY_MESSAGE | 今日の一言生成 |
| | GOAL_RETROSPECTIVE | 総括レポート生成 |
| ConversationScope | DAILY_FEEDBACK | 日次報告の会話 |
| | WEEKLY_SUMMARY | 週次要約の会話 |
| | DAILY_MESSAGE | 今日の一言の会話 |
| | GOAL_RETROSPECTIVE | 総括レポートの会話 |
| ExportFormat | MARKDOWN | 人間可読形式 |
| | JSON | 機械可読形式 |

### 5.2 設定・マスタ系

**app_setting（設定）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| key | TEXT | PK | 設定キー |
| value | TEXT | NOT NULL | 設定値（文字列として保存） |
| value_type | TEXT | NOT NULL | STRING / INTEGER / FLOAT / BOOLEAN / JSON |
| updated_at | DATETIME | NOT NULL | 更新日時 |

初期投入するキーは以下とする。

| キー | 型 | 初期値 | 説明 |
| --- | --- | --- | --- |
| ai.host | STRING | 空 | 接続先Host（例: `xxx.newton-x.net`）。PAT認証の主経路で使用 |
| ai.client_id | STRING | 空 | クライアント識別子（EntraIDフォールバック時のみ使用。通常は空でよい） |
| ai.tenant_id | STRING | 空 | テナント識別子（EntraIDフォールバック時のみ使用。通常は空でよい） |
| ai.api_base_url | STRING | 空 | 空の場合は `ai.host` から自動導出（`https://{host}/api`） |
| ai.assistant_uid.daily_feedback | STRING | `d18ad1c0-c7e6-4651-9ff2-4fe86af1a73b`（GPT-5.4 高性能） | 日次報告フィードバック（AI-01/AiPurpose.DAILY_FEEDBACK）で使用するアシスタント識別子 |
| ai.assistant_uid.weekly_summary | STRING | `849c4042-c6de-404e-a1ce-89812eaf850e`（Gemini 2.5 Flash 高速） | 週次要約生成（AI-02/AiPurpose.WEEKLY_SUMMARY）で使用するアシスタント識別子 |
| ai.assistant_uid.daily_message | STRING | `8ed280bb-3040-4ee3-9821-66bb7a4db125`（GPT-5.4-mini 高速） | 今日の一言生成（AI-03/AiPurpose.DAILY_MESSAGE）で使用するアシスタント識別子 |
| ai.assistant_uid.goal_retrospective | STRING | `d18ad1c0-c7e6-4651-9ff2-4fe86af1a73b`（GPT-5.4 高性能） | 総括レポート生成（AI-04/AiPurpose.GOAL_RETROSPECTIVE）で使用するアシスタント識別子 |
| ai.folder_prefix | STRING | ミチナリ | 会話フォルダ名の接頭辞 |
| ai.timeout_seconds | INTEGER | 60 | 応答待機時間 |
| ai.max_retries | INTEGER | 1 | 開発キット側の再試行回数 |
| ai.min_interval_seconds | INTEGER | 2 | 呼び出し間隔の下限 |
| ai.max_prompt_chars | INTEGER | 30000 | プロンプト縮退の閾値 |
| threshold.warning_ratio | FLOAT | 1.20 | 警告倍率 |
| threshold.replan_overrun_days | INTEGER | 3 | 強制リプランの超過日数 |
| calendar.day_boundary_hour | INTEGER | 0 | 1日の境界時刻（0〜11） |
| holiday.treat_as_buffer | BOOLEAN | true | 祝日をバッファ日として扱うか |
| display.locale | STRING | ja | 表示言語 |
| display.theme | STRING | system | テーマ |
| display.default_granularity | STRING | WEEK | 分析画面の既定粒度 |
| log.ai_enabled | BOOLEAN | true | AI通信ログの保存 |
| log.retention_days | INTEGER | 90 | ログ保持期間 |
| summary.lookback_weeks | INTEGER | 4 | 週次要約の遡及生成上限 |
| summary.inject_weeks | INTEGER | 4 | プロンプトへの注入週数 |
| server.port | INTEGER | 8100 | 起動ポート（8400を避ける） |
| backup.retention_count | INTEGER | 5 | 自動保持する手動バックアップの世代数（超過分は古い順に削除。D-02） |

**アシスタント選定について（2026-08-23、実機確認済み）**

用途ごとに異なるアシスタントを既定値とする。全用途を単一のアシスタントに固定すると、要約向きのタスクと分析・提案向きのタスクの双方に同じ特性のモデルを使うことになり最適でないため、`get_assistants()`で取得できる説明文を根拠に用途別へ分離した。

| 用途 | 既定アシスタント | 選定理由 |
| --- | --- | --- |
| DAILY_FEEDBACK（日次報告フィードバック） | GPT-5.4（高性能） | 「複雑なタスクの整理や具体的な提案に適しています」。状況解釈・成長の指摘・具体的行動提案・計画提案という17.2の要求に合致 |
| WEEKLY_SUMMARY（週次要約） | Gemini 2.5 Flash（高速） | 「日常的な要約や調べものに適しています」。起動時に複数週分を逐次生成するため速度も重視 |
| DAILY_MESSAGE（今日の一言） | GPT-5.4-mini（高速） | 「回答速度が速く、情報整理や提案をバランスよくこなせます」。ダッシュボードで非同期表示される軽量な生成に適する |
| GOAL_RETROSPECTIVE（総括レポート） | GPT-5.4（高性能） | DAILY_FEEDBACKと同様、低頻度・高品質重視のため同一アシスタントを採用 |

**既知の非対応アシスタント（実機確認済み・使用しないこと）**

導入済み環境（`seraku-ex.newton-x.net`）で`get_assistants()`が返す一覧のうち、以下2件は`send_message()`が常に404（`ChatTableModel matching query does not exist`）で失敗することを確認済みである。カタログには残っているが実際には利用できない状態と見られる（廃止済みモデルの可能性）。

| アシスタント名 | 識別子 | 状態 |
| --- | --- | --- |
| GPT-4o mini（高速） | `13358019-9ed8-4578-8490-583804ef5c78` | 使用不可（404で確認済み） |
| GPT-4o（高性能） | `8d0bbf4e-79c1-4f9e-9e9a-a034b784b11a` | 使用不可（404で確認済み） |

設定画面（SC-11）でのアシスタント選択時、この2件を選択肢から除外する、または選択後に疎通確認を行い失敗時はエラーメッセージとともに再選択を促す実装とする。将来的にこの一覧は変動しうるため、ハードコードした除外リストに依存せず、`send_message`失敗時のエラーハンドリング（AI_ERROR、再送・再選択の導線）で汎用的に吸収できる設計とすること。

認証トークンは本テーブルに保存しない。5.8を参照。

**prompt_template（プロンプトテンプレート）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| purpose | TEXT | NOT NULL, UNIQUE | AiPurpose |
| body | TEXT | NOT NULL | テンプレート本文 |
| is_customized | BOOLEAN | NOT NULL, DEFAULT false | 利用者が編集済みか |
| updated_at | DATETIME | NOT NULL | — |

初期文面はロジック・プロンプト編に定義する。アプリケーション起動時、未登録の purpose があれば初期文面を投入する。

**holiday（祝日）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| holiday_date | DATE | PK | 祝日の日付 |
| name | TEXT | NOT NULL | 祝日名 |
| imported_at | DATETIME | NOT NULL | 取込日時 |

**day_type_default（曜日別の日種別既定値）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| weekday | INTEGER | PK | 0=月曜 〜 6=日曜 |
| day_type | TEXT | NOT NULL | DayType（PLAN / BUFFER） |

既定値は月〜金を PLAN、土日を BUFFER とする。OFF は既定値に設定できない。

**calendar_day_override（日付単位の日種別上書き）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| target_date | DATE | PK | 対象日 |
| day_type | TEXT | NOT NULL | DayType |
| note | TEXT | NULL | 理由の備考（出張、旅行など） |
| created_at | DATETIME | NOT NULL | — |

**resource_slot（時間スロット）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| name | TEXT | NOT NULL | スロット名 |
| start_time | TIME | NOT NULL | 開始時刻 |
| end_time | TIME | NOT NULL | 終了時刻 |
| environment | TEXT | NOT NULL | Environment（PC / MOBILE） |
| is_active | BOOLEAN | NOT NULL, DEFAULT true | 有効フラグ |
| display_order | INTEGER | NOT NULL | 表示順 |
| created_at | DATETIME | NOT NULL | — |
| updated_at | DATETIME | NOT NULL | — |

連続時間は `end_time - start_time` として算出する。保存しない。

**resource_slot_weekday（スロットの適用曜日）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| slot_id | INTEGER | PK（複合）, FK → resource_slot.id | — |
| weekday | INTEGER | PK（複合） | 0=月曜 〜 6=日曜 |

### 5.3 目標系

**goal（目標）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| name | TEXT | NOT NULL | 試験名 |
| start_date | DATE | NOT NULL | 学習開始日 |
| status | TEXT | NOT NULL | GoalStatus |
| resource_ratio | REAL | NOT NULL, DEFAULT 0 | リソース配分比率（0.0〜1.0） |
| memo | TEXT | NULL | 備考 |
| activated_at | DATETIME | NULL | 進行中への遷移日時 |
| closed_at | DATETIME | NULL | クローズ日時 |
| created_at | DATETIME | NOT NULL | — |
| updated_at | DATETIME | NOT NULL | — |

**制約**

- `status` が ACTIVE の全 goal について、`resource_ratio` の合計が 1.0 を超えないこと。アプリケーション層で検証する
- PAUSED および CLOSED 状態の goal は、リソース比率の合計計算に算入しない。ただし `resource_ratio` の値自体は保持し、復帰時の要求値として用いる

**exam_subject（試験科目）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| goal_id | INTEGER | NOT NULL, FK → goal.id | — |
| name | TEXT | NOT NULL | 科目名 |
| exam_date_type | TEXT | NOT NULL | ExamDateType |
| exam_date_from | DATE | NULL | 期間開始日（RANGE時） |
| exam_date_to | DATE | NULL | 期間終了日（RANGE時） |
| exam_date_fixed | DATE | NULL | 確定日（FIXED時） |
| passing_score | REAL | NULL | 合格基準点（0〜100） |
| display_order | INTEGER | NOT NULL | 表示順 |
| created_at | DATETIME | NOT NULL | — |
| updated_at | DATETIME | NOT NULL | — |

**有効受験日の導出**

算出処理で用いる有効受験日は以下とする。保存しない。

- `exam_date_type` = FIXED のとき `exam_date_fixed`
- `exam_date_type` = RANGE のとき `exam_date_from`

期間指定時に最も早い日を採用するのは、予測を保守側へ倒すためである。

**material（教材）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| goal_id | INTEGER | NOT NULL, FK → goal.id | — |
| name | TEXT | NOT NULL | 教材名 |
| unit_label | TEXT | NOT NULL | 分量の単位（問、ページ、回など） |
| total_amount | REAL | NOT NULL | 1周分の総量 |
| **planned_cycles** | **INTEGER** | **NOT NULL, DEFAULT 1** | **予定周回数** |
| start_date | DATE | NOT NULL | 開始日 |
| due_date | DATE | NOT NULL | 締切 |
| due_date_is_manual | BOOLEAN | NOT NULL, DEFAULT false | 締切が手動設定か |
| required_block_minutes | INTEGER | NULL | 必要連続時間（分） |
| required_environment | TEXT | NOT NULL, DEFAULT 'ANY' | Environment |
| quality_metric_type | TEXT | NOT NULL, DEFAULT 'NONE' | QualityMetricType |
| is_active | BOOLEAN | NOT NULL, DEFAULT true | 有効フラグ |
| display_order | INTEGER | NOT NULL | 表示順 |
| created_at | DATETIME | NOT NULL | — |
| updated_at | DATETIME | NOT NULL | — |

**v1.0からの変更点**

- `planned_cycles` を追加した
- `current_cycle` を削除した。現在周回は累積完了量から導出する派生値であり、保存すると実績との不整合が生じ得るため

**総作業量と現在周回**

```
総作業量 ＝ total_amount × planned_cycles
現在周回 ＝ floor(累積完了量 ÷ total_amount) + 1
```

いずれも保存しない。

**締切の自動導出**

`due_date_is_manual` が false のとき、`due_date` は紐づく exam_subject の有効受験日のうち最も早い日の前日として算出し、保存する。以下の契機で再計算する。

- 教材と科目の紐付けが変更されたとき
- 紐づく科目の受験日が変更されたとき

自動導出値を保存するのは、締切がガントチャート表示や締切超過判定など多数の箇所で参照されるためである。派生値を保存しない原則の例外にあたるが、再計算契機が限定的かつ明確であるため許容する。

**material_subject（教材と科目の紐付け）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| material_id | INTEGER | PK（複合）, FK → material.id | — |
| subject_id | INTEGER | PK（複合）, FK → exam_subject.id | — |

**制約**：material と exam_subject は同一の goal に属すること。アプリケーション層で検証する。

**load_profile（負荷プロファイル）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| goal_id | INTEGER | NOT NULL, FK → goal.id | — |
| date_from | DATE | NOT NULL | 適用開始日 |
| date_to | DATE | NOT NULL | 適用終了日 |
| coefficient | REAL | NOT NULL | 負荷係数（正の実数） |
| note | TEXT | NULL | 理由の備考 |
| created_at | DATETIME | NOT NULL | — |

**制約**：同一 goal 内で期間が重複しないこと。アプリケーション層で検証する。未設定期間の係数は 1.0 とする。

**plan_baseline（計画基準値）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| material_id | INTEGER | NOT NULL, FK → material.id | — |
| effective_from | DATE | NOT NULL | 適用開始日 |
| baseline_daily_quota | REAL | NOT NULL | 基準日次ノルマ |
| remaining_at_baseline | REAL | NOT NULL | 基準時点の残量 |
| plan_days_at_baseline | INTEGER | NOT NULL | 基準時点の残計画日数 |
| planned_cycles_at_baseline | INTEGER | NOT NULL | 基準時点の予定周回数 |
| reason | TEXT | NOT NULL | BaselineReason |
| created_at | DATETIME | NOT NULL | — |

現在有効な基準は、`effective_from` が本日以前で最大のレコードとする。警告判定はこの基準値との比較で行う。

本テーブルは総括レポートおよびナレッジエクスポートにおける「リプラン履歴」の情報源となる。

### 5.4 記録系

**daily_record（日次記録）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| record_date | DATE | NOT NULL, UNIQUE | 対象日 |
| record_state | TEXT | NOT NULL | RecordState |
| diary_body | TEXT | NULL | 本日の行動・所感 |
| diary_learned | TEXT | NULL | 本日学んだこと |
| reported_at | DATETIME | NULL | 報告確定日時 |
| created_at | DATETIME | NOT NULL | — |

日次記録は日付単位で一意とする。目標単位ではない。複数目標が同時進行する場合も1日1レコードとし、実績は study_log 側で教材ごとに保持する。

`record_state` が REPORTED のレコードは、`diary_body`、`diary_learned`、および関連する study_log の更新を拒否する。

`record_date` は、設定された1日の境界時刻に基づいて決定される論理的な日付である。

**study_log（学習実績）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| daily_record_id | INTEGER | NOT NULL, FK → daily_record.id | — |
| material_id | INTEGER | NOT NULL, FK → material.id | — |
| minutes_spent | INTEGER | NULL | 投下時間（分） |
| amount_completed | REAL | NOT NULL | 完了分量 |
| cycle_number | INTEGER | NOT NULL | 周回数 |
| quality_value | REAL | NULL | 品質指標（0〜100に正規化済み） |
| created_at | DATETIME | NOT NULL | — |

**制約**：`daily_record_id` と `material_id` の組み合わせは一意とする。

`minutes_spent` が NULL または 0 のレコードは、実効速度の算出から除外する。この扱いにより、進捗のみ登録で時間を入力しなかった日が速度を汚染しない。

`quality_value` は正規化後の値を保存する。主観的手応えの5段階は保存時に 20/40/60/80/100 へ変換する。

`cycle_number` は利用者が入力する。既定値として算出値を提示するが、途中から次の周回へ移る運用に対応するため上書きを許容する。

**chat_message（AI対話）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| daily_record_id | INTEGER | NOT NULL, FK → daily_record.id | — |
| role | TEXT | NOT NULL | ChatRole |
| content | TEXT | NOT NULL | 発言内容 |
| sequence | INTEGER | NOT NULL | 会話内の順序 |
| created_at | DATETIME | NOT NULL | — |

**record_comment（コメント）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| daily_record_id | INTEGER | NOT NULL, FK → daily_record.id | — |
| body | TEXT | NOT NULL | コメント本文 |
| created_at | DATETIME | NOT NULL | — |
| updated_at | DATETIME | NOT NULL | — |

コメントのみが作成・修正・削除の対象となる。日次報告本体の不変性を保ちつつ、後日の補足を可能にする。

**weekly_summary（週次要約）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| goal_id | INTEGER | NOT NULL, FK → goal.id | — |
| week_start_date | DATE | NOT NULL | 週の開始日（月曜） |
| week_end_date | DATE | NOT NULL | 週の終了日（日曜） |
| summary_body | TEXT | NOT NULL | 要約本文 |
| is_anonymized | BOOLEAN | NOT NULL, DEFAULT false | 匿名化版か（Phase10で追加。7.3「匿名化版の週次要約は別レコードとして保持し、元の版は削除しない」に対応） |
| generated_at | DATETIME | NOT NULL | 生成日時 |

**制約**：`goal_id` と `week_start_date` と `is_anonymized` の組み合わせは一意とする。

**daily_message（今日の一言）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| target_date | DATE | NOT NULL, UNIQUE | 対象日 |
| body | TEXT | NOT NULL | 本文 |
| generated_at | DATETIME | NOT NULL | 生成日時 |

**exam_result（受験結果）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| subject_id | INTEGER | NOT NULL, UNIQUE, FK → exam_subject.id | — |
| taken_date | DATE | NOT NULL | 受験日 |
| result | TEXT | NOT NULL | ExamResultType |
| score | REAL | NULL | 得点 |
| evaluation | TEXT | NULL | 評価 |
| note | TEXT | NULL | 所感 |
| created_at | DATETIME | NOT NULL | — |

**goal_retrospective（総括レポート）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| goal_id | INTEGER | NOT NULL, FK → goal.id | — |
| body | TEXT | NOT NULL | レポート本文 |
| is_anonymized | BOOLEAN | NOT NULL, DEFAULT false | 匿名化版か |
| generated_at | DATETIME | NOT NULL | 生成日時 |

再生成を許容するため、goal に対して複数レコードを持てる構造とする。最新のものを既定で表示する。匿名化版は別レコードとして保持する。

### 5.5 AI連携系

**ai_conversation（会話識別子）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| goal_id | INTEGER | NULL, FK → goal.id | 今日の一言は目標に紐づかないためNULL可 |
| scope | TEXT | NOT NULL | ConversationScope |
| scope_key | TEXT | NOT NULL | 用途内での一意キー（日付、週開始日など） |
| conversation_uid | TEXT | NOT NULL | AI基盤側の会話識別子 |
| folder_uid | TEXT | NULL | AI基盤側のフォルダ識別子 |
| last_parent_order | INTEGER | NOT NULL, DEFAULT 0 | 直近の親メッセージ順序 |
| created_at | DATETIME | NOT NULL | — |

**制約**：`scope` と `scope_key` と `goal_id` の組み合わせは一意とする。

**設計意図**

AI基盤側では、メッセージ送信前に会話を作成し識別子を得る必要がある。この識別子をローカルに保持することで、一覧取得APIによる検索を回避する。

`last_parent_order` は、同一会話内で複数回のやり取りを行う際にAI基盤へ送信する値を記録する。日次報告における複数往復がこれに該当する。**ただし導入した開発キット（NewtonX ADK v0.10.5）では、この値を実際の文脈維持には利用できないことをソースコード確認済み**（`send_message()`に当該パラメータが存在せず常に固定値0が送信される）。同一日内の文脈維持は `chat_message` テーブルの対話履歴をプロンプトへ全文注入する方式（設計書 ロジック・プロンプト編 16.3.1、17.2の`{{conversation_history}}`）で実現する。本列は将来の開発キット改修に備えた記録として残す。

`scope_key` の値は用途により以下とする。

| scope | scope_key |
| --- | --- |
| DAILY_FEEDBACK | 対象日（YYYY-MM-DD） |
| WEEKLY_SUMMARY | 週開始日（YYYY-MM-DD） |
| DAILY_MESSAGE | 対象日（YYYY-MM-DD） |
| GOAL_RETROSPECTIVE | 固定値（"main"） |

**ai_log（AI通信ログ）**

| 列名 | 型 | 制約 | 説明 |
| --- | --- | --- | --- |
| id | INTEGER | PK | — |
| purpose | TEXT | NOT NULL | AiPurpose |
| conversation_uid | TEXT | NULL | 会話識別子 |
| request_body | TEXT | NOT NULL | 送信内容 |
| response_body | TEXT | NULL | 応答内容 |
| prompt_chars | INTEGER | NULL | 送信文字数 |
| was_truncated | BOOLEAN | NOT NULL, DEFAULT false | 縮退が発生したか |
| latency_ms | INTEGER | NULL | 応答時間 |
| error_type | TEXT | NULL | 例外種別 |
| error_message | TEXT | NULL | エラー内容 |
| created_at | DATETIME | NOT NULL | — |

`prompt_chars` と `was_truncated` を記録することで、入力上限の実測値を運用の中で把握できる。これは未決事項 O-01 の解消に直結する。

`app_setting` の `log.retention_days` を超過したレコードは、起動時に削除する。

### 5.6 インデックス定義

| テーブル | インデックス | 目的 |
| --- | --- | --- |
| daily_record | record_date（UNIQUE） | 日付検索 |
| study_log | (material_id, daily_record_id)（UNIQUE） | 重複防止、教材別集計 |
| study_log | (material_id, cycle_number) | 周回別の速度・品質算出 |
| study_log | daily_record_id | 日次記録からの取得 |
| chat_message | (daily_record_id, sequence) | 対話履歴の順序取得 |
| record_comment | daily_record_id | コメント一覧 |
| material | goal_id | 目標配下の教材取得 |
| material | due_date | 締切超過判定 |
| exam_subject | goal_id | 目標配下の科目取得 |
| material_subject | subject_id | 科目からの逆引き |
| load_profile | (goal_id, date_from, date_to) | 期間検索 |
| plan_baseline | (material_id, effective_from) | 有効基準の取得 |
| weekly_summary | (goal_id, week_start_date)（UNIQUE） | 生成済み判定 |
| goal_retrospective | (goal_id, generated_at) | 最新版の取得 |
| ai_conversation | (goal_id, scope, scope_key)（UNIQUE） | 会話識別子の取得 |
| calendar_day_override | target_date（PK） | 日種別解決 |
| holiday | holiday_date（PK） | 日種別解決 |
| ai_log | created_at | 保持期間による削除 |

### 5.7 マイグレーション方針

| 項目 | 方針 |
| --- | --- |
| ツール | Alembic |
| 初期構築 | 初回リビジョンで全テーブルを作成 |
| 列追加 | batch_alter_table を使用 |
| 初期データ | マイグレーションではなく、アプリケーション起動時の初期化処理で投入 |
| 初期データの対象 | app_setting、prompt_template、day_type_default |
| 冪等性 | 初期化処理は既存レコードを上書きしない |

### 5.8 認証情報の保管

**方針：AI基盤の開発キットが提供する仕組みに委ねる。**

開発キットは、設定に基づきPAT（Personal Access Token）およびトークンをファイルとして管理する機能を備えている。アプリケーション側で独自の資格情報管理を実装しない。

**認証方式はPATを主とする。** 開発キットの現行の公式利用ガイドは、Host＋PATによる認証を標準の方法として案内している。ブラウザ経由のOAuth／デバイスコード認証（EntraID）は、開発キット内部にフォールバック経路として残るのみであり、PATが設定されていれば優先して使用される。

| 項目 | 方針 |
| --- | --- |
| PAT・トークンの保存 | 開発キットの設定に従う（`~/.newtonx/config.json` 等）。`app_setting` には保存しない |
| PATの入力 | 設定画面（SC-11）でHostとPATを入力すると、開発キットの設定へ反映する |
| 保存先パス | 開発キットの既定値を使用する。設定画面に表示のみ行う |
| 起動時の確認 | Host・PATが設定済みかを確認し、認証済みとして扱う |
| フォールバック認証 | PAT未設定・無効時のみ、設定画面からブラウザ経由の認証フローを実行できる |
| クライアントへの公開 | 認証状態の真偽値のみを返す。PAT・トークンは送出しない |

**v1.0からの変更理由**

v1.0では独自にOS資格情報ストアへ保管する方針としていたが、開発キットが独自にPAT・トークンを管理する仕様であるため、二重管理は認証状態の不整合を招く。開発キットの仕組みに一本化する。

**v1.1（本改訂）での変更理由**：当初はブラウザ経由のOAuthフローを認証の主経路として設計していたが、実際に導入した開発キットの公式利用ガイドを確認した結果、PATが現行の標準的な認証方式であることが判明したため、主経路をPATに改めた。

**フォールバック認証フローの実行方式**

ブラウザ経由の認証フローは起動して待機するため、APIリクエストのスレッドをブロックする可能性がある。バックグラウンドスレッドで実行し、クライアントは認証状態をポーリングして完了を検知する。

---

## 6. API設計

### 6.1 基本方針

| 項目 | 方針 |
| --- | --- |
| 形式 | REST |
| ベースパス | `/api/v1` |
| 認証 | なし（単一利用者、ローカル動作） |
| 応答形式 | JSON |
| エラー形式 | `{ "error": { "code": "...", "message": "...", "details": [...] } }` |
| 日付形式 | ISO 8601（YYYY-MM-DD） |
| 日時形式 | ISO 8601（タイムゾーン付き） |

### 6.2 エンドポイント一覧

**ダッシュボード**

| メソッド | パス | 概要 |
| --- | --- | --- |
| GET | /dashboard | ダッシュボードに必要な全情報を一括取得 |

初期表示2秒以内という性能要件のため、複数のリソースを個別に取得せず1回の呼び出しで返す。今日の一言はAI応答の待機が発生するため本エンドポイントに含めず、未生成時は null を返して別途 GET /daily-message で取得する。

**目標**

| メソッド | パス | 概要 |
| --- | --- | --- |
| GET | /goals | 目標一覧 |
| POST | /goals | 目標作成 |
| GET | /goals/{id} | 目標詳細（科目、教材、配分、負荷プロファイルを含む） |
| PATCH | /goals/{id} | 目標更新 |
| DELETE | /goals/{id} | 目標削除（DRAFT のみ許可） |
| POST | /goals/{id}/activate | 進行中へ遷移 |
| POST | /goals/{id}/pause | 一時停止へ遷移 |
| POST | /goals/{id}/resume | 一時停止から復帰 |
| POST | /goals/{id}/close | クローズへ遷移 |
| GET | /goals/{id}/plan | 計画状態（日次ノルマ、速度、完了予測日）を取得 |
| POST | /goals/{id}/replan | 基準値を再設定 |
| GET | /goals/{id}/baselines | リプラン履歴の取得 |

**試験科目**

| メソッド | パス | 概要 |
| --- | --- | --- |
| POST | /goals/{id}/subjects | 科目追加 |
| PATCH | /subjects/{id} | 科目更新 |
| DELETE | /subjects/{id} | 科目削除 |
| POST | /subjects/{id}/fix-date | 受験日を確定日に変更し計画を再算出 |

**教材**

| メソッド | パス | 概要 |
| --- | --- | --- |
| POST | /goals/{id}/materials | 教材追加 |
| PATCH | /materials/{id} | 教材更新 |
| DELETE | /materials/{id} | 教材削除（実績がある場合は拒否） |
| POST | /materials/{id}/deactivate | 教材の無効化 |
| GET | /materials/{id}/slot-check | スロット充足の検証結果 |
| GET | /materials/{id}/cycles | 周回ごとの進捗、速度、品質の取得 |

**負荷プロファイル**

| メソッド | パス | 概要 |
| --- | --- | --- |
| GET | /goals/{id}/load-profiles | 一覧 |
| POST | /goals/{id}/load-profiles | 追加 |
| PATCH | /load-profiles/{id} | 更新 |
| DELETE | /load-profiles/{id} | 削除 |

**リソース**

| メソッド | パス | 概要 |
| --- | --- | --- |
| GET | /resources/slots | スロット一覧 |
| POST | /resources/slots | スロット追加 |
| PATCH | /resources/slots/{id} | スロット更新 |
| DELETE | /resources/slots/{id} | スロット削除 |
| GET | /resources/allocation | 配分状況と空き容量 |
| GET | /resources/day-type-defaults | 曜日別既定値 |
| PUT | /resources/day-type-defaults | 曜日別既定値の一括更新 |
| GET | /resources/day-boundary-hour | 1日の境界時刻の取得 |
| PUT | /resources/day-boundary-hour | 1日の境界時刻の更新（0〜11時、仕様書10章） |

**カレンダー**

| メソッド | パス | 概要 |
| --- | --- | --- |
| GET | /calendar | 期間指定で日種別と記録状態を取得 |
| PUT | /calendar/{date}/day-type | 日種別の個別指定 |
| DELETE | /calendar/{date}/day-type | 個別指定の解除 |
| POST | /calendar/holidays/import | 祝日データの取込 |

**日次記録**

| メソッド | パス | 概要 |
| --- | --- | --- |
| GET | /records/today | 論理的な本日の日付と記録状態を取得 |
| GET | /records/{date} | 指定日の記録取得 |
| POST | /records/{date}/progress | 進捗のみ登録 |
| POST | /records/{date}/chat | AI対話の実行（1往復） |
| POST | /records/{date}/finalize | 報告確定 |
| GET | /records/{date}/quota | 指定日の日次ノルマ取得 |
| POST | /records/{date}/comments | コメント追加 |
| PATCH | /comments/{id} | コメント更新 |
| DELETE | /comments/{id} | コメント削除 |

`GET /records/today` は、1日の境界時刻を考慮した論理的な本日を返す。クライアント側でシステム日付から判断しない。

**分析**

| メソッド | パス | 概要 |
| --- | --- | --- |
| GET | /analytics/quality | 品質指標推移（教材別、周回別、粒度指定） |
| GET | /analytics/progress | 累積完了量と計画線 |
| GET | /analytics/forecast | 完了予測と乖離 |
| GET | /analytics/speed | 実効速度推移（周回別） |
| GET | /analytics/gantt | ガントチャート用データ |

**完了処理とエクスポート**

| メソッド | パス | 概要 |
| --- | --- | --- |
| POST | /subjects/{id}/result | 受験結果の登録 |
| PATCH | /results/{id} | 受験結果の更新（クローズ前のみ） |
| GET | /goals/{id}/retrospective | 総括レポートの取得 |
| POST | /goals/{id}/retrospective | 総括レポートの生成・再生成 |
| GET | /goals/{id}/knowledge-export/preview | エクスポート内容のプレビュー |
| POST | /goals/{id}/knowledge-export | ナレッジエクスポートの実行 |

`POST /goals/{id}/knowledge-export` のリクエストには、含める項目のリストと匿名化の有無、出力形式を指定する。

**設定・データ管理**

| メソッド | パス | 概要 |
| --- | --- | --- |
| GET | /settings | 設定一覧 |
| PUT | /settings | 設定の一括更新 |
| GET | /settings/prompts | プロンプトテンプレート一覧 |
| PUT | /settings/prompts/{purpose} | テンプレート更新 |
| POST | /settings/prompts/{purpose}/reset | テンプレートを初期値に戻す |
| GET | /ai/status | 認証状態とAI基盤の稼働状況 |
| POST | /ai/login | 認証の実行（非同期） |
| POST | /ai/logout | トークンの破棄 |
| GET | /ai/assistants | アシスタント一覧の取得 |
| GET | /data/export | 全データのエクスポート |
| POST | /data/import | データのインポート |
| POST | /data/backup | バックアップの作成 |
| GET | /data/backups | バックアップ一覧 |
| POST | /data/backups/{id}/restore | バックアップからの復元 |

バックアップ作成時、`backup.retention_count`（既定5）を超える古いバックアップを作成日時の古い順に自動削除する（D-02）。

**その他**

| メソッド | パス | 概要 |
| --- | --- | --- |
| GET | /daily-message | 今日の一言の取得（未生成時は生成） |
| GET | /health | 稼働確認 |

### 6.3 エラーコード

| コード | HTTPステータス | 意味 |
| --- | --- | --- |
| VALIDATION_ERROR | 400 | 入力値の検証エラー |
| RESOURCE_EXCEEDED | 400 | リソース配分の合計超過 |
| CYCLE_CONFLICT | 400 | 予定周回数が完了済み周回を下回る |
| IMMUTABLE_RECORD | 409 | 確定済み記録への更新 |
| INVALID_STATE_TRANSITION | 409 | 許可されない状態遷移 |
| BACKDATE_LIMIT_EXCEEDED | 400 | 遡及入力の期限超過 |
| NOT_FOUND | 404 | 対象が存在しない |
| AI_AUTH_REQUIRED | 401 | AI基盤の認証が必要 |
| AI_CONFIG_ERROR | 400 | AI連携の設定不備 |
| AI_TIMEOUT | 504 | AI応答のタイムアウト |
| AI_ERROR | 502 | AI基盤からのエラー応答 |
| INTERNAL_ERROR | 500 | 想定外のエラー |

---

## 7. ナレッジエクスポートの構造

### 7.1 JSON形式のスキーマ概要

```
{
  "schema_version": "1.0",
  "exported_at": "ISO 8601",
  "anonymized": true | false,
  "goal": {
    "name": "...",
    "start_date": "...",
    "closed_at": "...",
    "status": "..."
  },
  "subjects": [
    { "name": "...", "exam_date": "...", "passing_score": 60 }
  ],
  "materials": [
    {
      "name": "...", "unit": "...",
      "total_amount": 850, "planned_cycles": 3,
      "completed_amount": 2100, "completed_cycles": 2,
      "start_date": "...", "due_date": "...",
      "required_block_minutes": 90, "required_environment": "PC"
    }
  ],
  "summary": {
    "total_minutes": 0,
    "study_days": 0,
    "report_rate": 0.0,
    "buffer_usage_rate": 0.0,
    "replan_count": 0,
    "average_minutes_per_day": 0.0
  },
  "daily_records": [
    { "date": "...", "material": "...", "minutes": 0,
      "amount": 0, "cycle": 1, "quality": 0.0 }
  ],
  "quality_trend": [
    { "material": "...", "cycle": 1,
      "series": [ { "date": "...", "value": 0.0 } ] }
  ],
  "replan_history": [
    { "date": "...", "material": "...", "reason": "...",
      "quota_before": 0.0, "quota_after": 0.0,
      "remaining": 0.0, "plan_days": 0 }
  ],
  "weekly_summaries": [
    { "week_start": "...", "body": "..." }
  ],
  "results": [
    { "subject": "...", "result": "PASS", "score": 0.0 }
  ],
  "retrospective": "..."
}
```

### 7.2 Markdown形式の構成

| 節 | 内容 |
| --- | --- |
| 1. 概要 | 試験名、受験日、学習期間、結果 |
| 2. 総括 | 総括レポート本文 |
| 3. 学習量 | 総投下時間、学習日数、報告率、平均時間 |
| 4. 教材構成 | 教材ごとの総量、予定周回、実績周回、所要時間 |
| 5. 品質の推移 | 周回別の品質指標推移（表形式） |
| 6. 計画の修正履歴 | リプランの日時、契機、変更内容 |
| 7. 週ごとの経過 | 週次要約の全文 |
| 8. 付録 | 日別実績（表形式） |

第6節が本エクスポートの独自性である。**どの時点で計画が崩れ、いつ修正したかという情報は、他の学習記録には存在しない。**

### 7.3 匿名化の処理

| 対象 | 処理 |
| --- | --- |
| 日記本文 | 出力から除外する |
| AI対話履歴 | 出力から除外する |
| 週次要約 | 業務・家庭に関する記述を除外するようAIに指示して再生成する |
| 総括レポート | 個人的事情への言及を避けるようAIに指示して再生成する |

匿名化版の週次要約および総括レポートは、それぞれ別レコードとして保持する。元の版は削除しない。

---

## 8. ディレクトリ構成

```
michinari/
├── backend/
│   ├── app/
│   │   ├── main.py                  アプリケーション起動
│   │   ├── config.py                設定の読み込み
│   │   ├── database.py              セッション管理、PRAGMA設定
│   │   ├── models/                  SQLAlchemyモデル
│   │   │   ├── setting.py
│   │   │   ├── resource.py
│   │   │   ├── calendar.py
│   │   │   ├── goal.py
│   │   │   ├── material.py
│   │   │   ├── record.py
│   │   │   ├── retrospective.py
│   │   │   └── ai.py
│   │   ├── schemas/                 Pydanticスキーマ（goal.py/subject.py/material.py/
│   │   │                            load_profile.py/resource.py。Phase3実装）
│   │   ├── api/                     ルーター
│   │   │   ├── dashboard.py
│   │   │   ├── errors.py            ドメイン例外→HTTPレスポンス変換（Phase3実装）
│   │   │   ├── goals.py             目標・試験科目・負荷プロファイル（Phase3実装）
│   │   │   ├── materials.py         教材（Phase3実装）
│   │   │   ├── resources.py         リソーススロット・曜日別既定値・配分状況（Phase3実装）
│   │   │   ├── calendar.py
│   │   │   ├── records.py
│   │   │   ├── analytics.py
│   │   │   ├── closure.py
│   │   │   ├── export.py
│   │   │   ├── settings.py
│   │   │   └── data.py
│   │   ├── services/                業務ロジック
│   │   │   ├── goal_service.py      目標CRUD・状態遷移・負荷プロファイル（Phase3実装）
│   │   │   ├── subject_service.py   試験科目CRUD・受験日確定（Phase3実装）
│   │   │   ├── material_service.py  教材CRUD・締切自動導出・周回別進捗（Phase3実装）
│   │   │   ├── resource_service.py  リソーススロット・曜日別既定値・配分状況（Phase3実装）
│   │   │   ├── quota_service.py     日次ノルマ算出
│   │   │   ├── cycle_service.py     周回の導出
│   │   │   ├── speed_service.py     実効速度・完了予測
│   │   │   ├── calendar_service.py  日種別・境界時刻の解決
│   │   │   ├── slot_service.py      スロット割当
│   │   │   ├── record_service.py
│   │   │   ├── analytics_service.py
│   │   │   ├── threshold_service.py 警告判定
│   │   │   ├── baseline_service.py  計画基準値
│   │   │   └── export_service.py    ナレッジエクスポート
│   │   ├── ai/                      AI連携
│   │   │   ├── client.py            開発キットのラッパ
│   │   │   ├── auth.py              認証状態の管理
│   │   │   ├── conversation.py      会話とフォルダの管理
│   │   │   ├── prompt_builder.py    プロンプト組み立てと縮退
│   │   │   ├── rate_limiter.py      呼び出し間隔の制御
│   │   │   └── logger.py            通信ログ
│   │   ├── constants/               定数・列挙型
│   │   └── init/                    初期データ投入
│   ├── alembic/
│   │   └── versions/
│   ├── tests/
│   └── pyproject.toml
│
├── frontend/
│   ├── src/
│   │   ├── main.tsx
│   │   ├── App.tsx
│   │   ├── pages/                   画面（SC-01〜SC-13に対応）
│   │   ├── components/              共通コンポーネント
│   │   ├── features/                機能単位のコンポーネント
│   │   │   ├── dashboard/
│   │   │   ├── goal/
│   │   │   ├── resource/
│   │   │   ├── calendar/
│   │   │   ├── record/
│   │   │   ├── analytics/
│   │   │   └── export/
│   │   ├── api/                     APIクライアント
│   │   ├── hooks/
│   │   ├── types/
│   │   ├── locales/                 UI文言（ja.json）
│   │   └── constants/
│   ├── package.json
│   └── tailwind.config.ts
│
├── data/                            データベースファイル、バックアップ、エクスポート
├── docs/                            要件定義書、仕様書、設計書
└── README.md
```

### 8.1 責務分割の原則

| 層 | 責務 | 禁止事項 |
| --- | --- | --- |
| api | リクエスト受付、スキーマ検証、サービス呼び出し | 業務ロジックの記述 |
| services | 業務ロジック、算出、状態遷移の検証 | HTTPに関する知識 |
| models | データ構造の定義 | 業務ロジックの記述 |
| ai | AI基盤との通信、プロンプト組み立て、会話管理 | 業務判断 |

### 8.2 定数管理の原則

| 対象 | 配置先 |
| --- | --- |
| UI文言 | frontend/src/locales/ja.json |
| 列挙型の値 | backend/app/constants/、frontend/src/constants/ |
| 閾値およびパラメータ | app_setting テーブル（画面から変更可能） |
| プロンプト文面 | prompt_template テーブル（画面から変更可能） |

ソースコード中に文字列リテラルおよび数値リテラルを直接記述しない。

---

## 9. 未決事項

| ID | 内容 | 決定期限 | 状態 |
| --- | --- | --- | --- |
| D-01 | 祝日データの取得元および更新方式 | 実装着手前 | 解消済み。技術選定書 v1.0 第6章を参照（内閣府CSVの手動インポート方式） |
| D-02 | バックアップの保持世代数 | 実装着手前 | 解消済み。5世代（`backup.retention_count`、本章5.2・6.2参照） |
| D-03 | 今日の一言の非同期取得における待機表示の仕様 | 実装時 | 未解消（Phase 6着手前に決定） |
| D-04 | エクスポートJSONのスキーマ版数管理方針 | 実装時 | 解消済み（2026-08-25）。`schema_version` は固定文字列 `"1.0"` とする。単一利用者・ローカル運用のv1のため、複数版の共存や自動移行は扱わない。`POST /data/import` は `schema_version` が現行値と一致しない場合、VALIDATION_ERRORで拒否する。将来スキーマを変更する際に版数を上げ、その時点で移行方針を再検討する |

---

*算出ロジックの数式定義およびプロンプト設計は「設計書 ロジック・プロンプト編」を参照のこと。*
