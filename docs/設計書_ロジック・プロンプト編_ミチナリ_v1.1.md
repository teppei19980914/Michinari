# ミチナリ（Michinari）設計書 ─ ロジック・プロンプト編

| 項目 | 内容 |
| --- | --- |
| 文書名 | 設計書 ロジック・プロンプト編 |
| バージョン | 1.1 |
| 作成日 | 2026年8月22日 |
| 関連文書 | 要件定義書 v1.1、仕様書 v1.1、設計書 データ構造編 v1.1 |

### 改訂履歴

| 版 | 日付 | 変更内容 |
| --- | --- | --- |
| 1.0 | 2026-08-22 | 初版 |
| 1.1 | 2026-08-22 | 残量算出を総作業量ベースに変更（周回対応）。実効速度を周回別に算出する方式へ変更。1日の境界時刻の解決を追加（第3章）。AI連携の実装仕様を開発キットの実仕様に基づき全面改訂（第16章）。プロンプト縮退の仕様を追加。総括レポート用プロンプトを追加 |
| 1.1（改） | 2026-08-23 | NewtonX ADK（v0.10.5）実導入・ソースコード確認の結果を反映。16.1・16.2の認証設計をPAT主経路に変更、16.3に親メッセージ順序の既知の制約（16.3.1）を追記し文脈維持を対話履歴の全文注入方式に統一、16.6にタイムアウト判定方法の実装注記を追加、21未決事項にL-06を追加 |
| 1.1（改2） | 2026-08-29 | 14.4合格基準線の算出を、合格点の入力方式（百分率／点数）に応じた正規化を経る方式に変更（データ構造編5.3のPassingScoreType追加に対応） |
| 1.1（改3） | 2026-08-31 | 目標種別「読書」を追加。第3〜14章は資格試験目標（category=EXAM）を対象とすることを明記し、読書目標（category=READING）の算出ロジックを新設した第21章に集約。17章にDAILY_FEEDBACK_READING（17.6）・GOAL_RETROSPECTIVE_READING（17.7）のプロンプトを追加。旧21章（未決事項）を22章へ繰り下げ |
| 1.1（改4） | 2026-08-31 | Phase15実装を反映：21.2の見出しをGET /goals/{id}/plan（実装せず）からBookReadへの埋め込み方式へ修正。21.1に、既存の資格試験用AI機能（DAILY_FEEDBACK・DAILY_MESSAGE・GOAL_RETROSPECTIVE）が読書目標のデータを誤って取り込まないための暫定対応（list_active_exam_goalsによる限定、READING目標への総括レポート生成拒否）を追記 |
| 1.1（改5） | 2026-08-31 | Phase16（読書AI連携）完了を反映。21.1の暫定対応を恒常仕様として確定し、GOAL_RETROSPECTIVE_READING実装によりPhase15のガードが解消されたことを追記。chat_message.purposeによる対話履歴分離とAI対話エンドポイント分離（/chat・/reading-chat）を追記。L-08（読書用アシスタント選定、mainとのマージによりL-07から採番替え）を更新（設定APIからは変更可能、実際の値はPhase11相当の実環境疎通確認まで未選定） |
| 1.1（改6） | 2026-08-31 | mainブランチ（アーカイブ機能追加に伴う複数目標混同対策、L-04・L-07）とのマージに伴う整合対応。22章のL-04は、mainで解消済みとなった内容（日記・今日の一言・カレンダー補助マーカーの目標別分離）を採用し、読書目標のreading_logはもとよりbook_idで分離済みのため同様に混入しない旨を追記。読書用アシスタント選定の項目はL-07からL-08へ採番替え（mainのL-07「日次フィードバックの目標混同対策」と衝突するため） |

---

## 1. 本文書の目的と範囲

本文書は、ミチナリの算出ロジック、判定条件、およびAIプロンプトの設計を定義する。

データ構造、テーブル定義、API構成は「設計書 データ構造編」に記載する。

**第3〜14章は、`goal.category = EXAM`（資格試験）の目標を対象とした算出ロジックである。** `category = READING`（読書）の目標は、日次ノルマ・実効速度・警告判定などの定量的な計画管理（要件定義書R-71）を持たないため、これらの章を適用しない。読書目標が用いるロジックは第21章に集約する。

---

## 2. 記号定義

| 記号 | 意味 |
| --- | --- |
| `T` | 論理的な本日（1日の境界時刻を考慮したシステム日付） |
| `m` | 教材（material） |
| `g` | 目標（goal） |
| `d` | 日付 |
| `A(m)` | 教材 m の1周分の総量（total_amount） |
| `N(m)` | 教材 m の予定周回数（planned_cycles） |
| `TW(m)` | 教材 m の総作業量 ＝ `A(m) × N(m)` |
| `C(m)` | 教材 m の累積完了量（全周回の合計） |
| `R(m)` | 教材 m の残量 |
| `k(m)` | 教材 m の現在周回 |
| `D(m)` | 教材 m の締切（due_date） |
| `S(m)` | 教材 m の開始日（start_date） |
| `type(d)` | 日 d の日種別（PLAN / BUFFER / OFF） |
| `w(g, d)` | 目標 g における日 d の負荷係数 |
| `P(m)` | 教材 m の残計画日集合 |
| `H(g, d)` | 目標 g が日 d に確保できる時間（時間単位） |

---

## 3. 1日の境界時刻の解決

### 3.1 論理的な本日の算出

```
boundary = 設定値 calendar.day_boundary_hour（0〜11、既定値 0）

現在時刻の時が boundary 未満の場合：
    T = システム日付 − 1日
それ以外：
    T = システム日付
```

### 3.2 適用範囲

論理的な本日は、以下の全てにおいて用いる。

| 適用箇所 | 内容 |
| --- | --- |
| 日次ノルマの算出 | 基準日 |
| 日次記録の対象日 | `record_date` の決定 |
| 遡及入力の判定 | 「前日」の基準 |
| 完了予測の起点 | 累積開始日 |
| 今日の一言の生成判定 | 対象日 |
| 週次要約の生成判定 | 完了週の判定 |

### 3.3 設計意図

深夜まで学習する習慣がある場合、システム日付をそのまま用いると、午前1時の学習が翌日分として計上される。その結果、当日は未達、翌日は達成という実態と乖離した記録が生じる。

境界時刻を設定可能にすることで、この乖離を利用者の生活リズムに合わせて解消する。

**クライアント側でシステム日付から論理日を判断しない。** サーバが `GET /records/today` で返す値を用いる。設定値の解釈をサーバに一元化するためである。

---

## 4. 日種別の解決

### 4.1 解決順序

日 `d` の日種別は以下の順序で決定する。先に一致した規則を採用し、以降は評価しない。

```
1. calendar_day_override に d のレコードが存在する
   → その day_type を採用
   
2. holiday に d のレコードが存在し、
   かつ設定 holiday.treat_as_buffer が true
   → BUFFER を採用
   
3. day_type_default における d の曜日の設定を採用
```

**設計意図**

個別指定を最優先とすることで、「この土曜は勉強日にする」「この水曜は出張で不可」という実運用上の調整が、既定値の設定を変更せずに行える。除外日（OFF）は個別指定でのみ設定でき、既定値としては設定できない。

### 4.2 日種別ごとの扱い

| 日種別 | 日次ノルマ | ノルマ算出の分母 | 実効速度の算出 | 実績登録 |
| --- | --- | --- | --- | --- |
| PLAN | 算出する | 算入する | 対象 | 可能 |
| BUFFER | 0 | 算入しない | 対象 | 可能 |
| OFF | 0 | 算入しない | 対象外 | 可能 |

**BUFFER と OFF の差異**

いずれも日次ノルマの分母から除外されるが、実効速度の扱いが異なる。BUFFER 日は学習した場合にその実績を速度算出に反映する。OFF 日は「学習が不可能と事前に判明していた日」であり、仮に実績が登録されても速度算出から除外する。

この非対称性により、バッファ日の学習は翌日以降の日次ノルマを引き下げる形で即座に報われる。一方で、バッファ日に学習しなかったことによる負債は発生しない。

---

## 5. 負荷係数の解決

### 5.1 解決規則

```
w(g, d) = load_profile のうち、goal_id = g かつ
          date_from ≤ d ≤ date_to を満たすレコードの coefficient
          
          該当するレコードが存在しない場合は 1.0
```

`load_profile` は同一目標内で期間が重複しないよう制約されているため、該当レコードは0件または1件となる。

### 5.2 検証規則

負荷プロファイルの登録・更新時に以下を検証する。

| 検証項目 | 条件 |
| --- | --- |
| 期間の妥当性 | `date_from ≤ date_to` |
| 係数の妥当性 | `coefficient > 0` |
| 期間の重複 | 同一 goal 内の既存レコードと期間が重複しないこと |

---

## 6. 周回の導出

### 6.1 算出式

```
TW(m) = A(m) × N(m)

C(m)  = Σ study_log.amount_completed  （教材 m の全レコード）

R(m)  = max(0, TW(m) − C(m))

              ⎧ N(m)                        （C(m) ≥ TW(m)）
k(m) =        ⎨
              ⎩ min(N(m), floor(C(m) / A(m)) + 1)
```

### 6.2 現在周回内の進捗

```
現在周回の完了量 = C(m) − A(m) × (k(m) − 1)
現在周回の進捗率 = 現在周回の完了量 / A(m)
全体進捗率       = C(m) / TW(m)
```

### 6.3 設計意図

v1.0では残量を `A(m) − C(m)` としていたため、**1周目が完了した時点で残量が0となり、日次ノルマが消滅していた。** 資格学習では複数周回が前提であるため、この設計では運用開始から数週間で破綻する。

総作業量を基礎とすることで、周回をまたいだ計画が連続する。850問を3周する計画であれば、総作業量2550問に対して残量が算出され、1周目の完了は単なる通過点となる。

### 6.4 予定周回数の変更

予定周回数の変更は計画に大きな影響を与えるため、以下を行う。

| 項目 | 処理 |
| --- | --- |
| 検証 | 新しい予定周回数が、既に完了した周回数を下回る場合は拒否する |
| 基準値 | 変更時に `plan_baseline` へ `CYCLE_CHANGED` の理由でレコードを追加する |

予定周回数の削減は、AIによる「中度」の負荷軽減提案に含まれる。3周の計画を2周に減らすことは、範囲を絞る有効な手段である。

### 6.5 周回番号の入力

`study_log.cycle_number` は利用者が入力する。入力欄には `k(m)` を既定値として提示するが、上書きを許容する。

これは「1周目を完全に終える前に、苦手分野だけ2周目に入る」といった運用に対応するためである。算出値を強制すると、実態と記録が乖離する。

---

## 7. 日次ノルマの算出

### 7.1 中核の式

```
P(m) = { d | T ≤ d ≤ D(m) かつ type(d) = PLAN }

W(m) = Σ_{d ∈ P(m)} w(g, d)

               ⎧ R(m) × w(g, T) / W(m)   （S(m) ≤ T かつ type(T) = PLAN かつ W(m) > 0）
quota(m, T) =  ⎨
               ⎩ 0                        （それ以外）
```

`S(m) ≤ T` （今日が教材の学習期間開始日以降）は当初この式には含まれておらず、7.3の一覧条件
にのみ存在していたが、学習期間開始前の教材を「今日時点では未着手」として一貫して扱うため
式本体に追加した。この条件は `quota(m, T)` を参照するすべての箇所（本日のノルマ一覧・7.3、
警告判定・11.1、基準値記録・12.2、AI向け状況出力）に共通適用される。

### 7.2 設計の要点

**未達分を加算しない**

日次ノルマは保存されず、毎日この式で再評価される。前日に未達があった場合、`C(m)` が想定より小さいため `R(m)` が大きくなり、同時に `P(m)` が1日分減少する。結果として翌日のノルマは緩やかに上昇する。

この方式（残量再配分）と、未達分を翌日に加算する方式（全量繰越）の差は決定的である。

| 経過 | 全量繰越 | 残量再配分（採用） |
| --- | --- | --- |
| 初日 | 10 | 10 |
| 1日未達 | 20 | 約12 |
| 2日未達 | 30 | 約15 |
| 7日未達 | 80 | 約22 |

全量繰越では1週間の停滞で心理的限界を超えるが、残量再配分では緩やかに上昇するのみである。したがって負債の上限設定（キャップ）は不要となる。

**負荷係数による配分**

`w(g, d)` を分子と分母の双方に用いることで、総量は変化せず配分のみが変わる。ある期間の係数を下げれば、その分は自動的に他の計画日へ寄せられる。「今月は減らす」と「計画を諦める」が明確に区別される。

**W(m) = 0 の場合**

締切までに計画日が1日も残っていない状態を意味する。この場合ノルマは0とし、締切超過警告（NT-06）の対象とする。

### 7.3 目標単位の集約

ダッシュボードにおける「本日のノルマ」は、進行中の全目標に属する全教材について、以下の条件を満たすものを列挙する。

```
S(m) ≤ T ≤ D(m) かつ material.is_active = true
      かつ goal.status = ACTIVE
```

---

## 8. 実効速度の算出

### 8.1 周回別の算出

```
V(m, c) = { study_log | material_id = m
                        かつ cycle_number = c
                        かつ minutes_spent IS NOT NULL
                        かつ minutes_spent > 0
                        かつ type(record_date) ≠ OFF }

                 Σ_{v ∈ V(m,c)} amount_completed
speed(m, c) = ────────────────────────────────────
               Σ_{v ∈ V(m,c)} minutes_spent / 60
```

単位は「分量／時間」である。

### 8.2 完了予測に用いる速度

```
                ⎧ speed(m, k(m))          （V(m, k(m)) のサンプル数 ≥ 3）
speed_eff(m) =  ⎨ speed(m, k(m) − 1)      （現在周回が不足し、直近周回に実績がある）
                ⎩ 算出不能                 （いずれも不足）
```

### 8.3 設計意図

**周回によって速度は大きく変化する。** 2周目は既知の問題を解くため処理が速く、1周目の2倍以上になることも珍しくない。

全周回を混合して平均を取ると、1周目の後半において、まだ経験していない2周目の速度が混入することはないが、逆に2周目の予測に1周目の遅い速度が混入する。結果として2周目の完了予測が過度に悲観的となり、不要な警告が発生する。

周回別に算出し、現在周回の値を用いることで、予測の精度が保たれる。

**サンプル数の下限を3とした理由**

周回の切り替わり直後は実績が1〜2件しかなく、外れ値の影響を強く受ける。3件以上を要求し、それまでは直近周回の速度で代替する。

### 8.4 除外規則

| 除外対象 | 理由 |
| --- | --- |
| `minutes_spent` が NULL または 0 | 分母が不明。0時間として扱うと速度が不当に高く算出される |
| 日種別が OFF の日 | 学習不可日として事前登録された日であり、例外的な実績は代表値にならない |

進捗のみ登録において投下時間を任意入力としたのは、うっかり忘れの吸収を優先したためである。その結果として時間が不明な日が生じるが、それらを速度算出から除外することで、「サボりは残量に正しく反映されるが、実力の推定値は汚染されない」状態を作る。

### 8.5 サンプル不足時の扱い

`speed_eff(m)` が算出不能の場合、以下の扱いとする。

| 項目 | 扱い |
| --- | --- |
| 完了予測日 | null |
| 強制リプラン判定 | 実施しない |
| UI表示 | 「実績データが不足しています」を表示 |

---

## 9. 必要速度の算出

### 9.1 利用可能時間の算出

日 `d` において目標 `g` が確保できる時間は以下とする。

```
slots(d) = resource_slot のうち、
           resource_slot_weekday に d の曜日を含み、
           かつ is_active = true であるもの

total_hours(d) = Σ_{s ∈ slots(d)} (end_time(s) − start_time(s)) / 60

H(g, d) = total_hours(d) × resource_ratio(g)
```

### 9.2 教材へのスロット割当

目標に複数の教材がある場合、スロットの割当は以下の規則で決定する。

```
手順1：各スロット s について、適合する教材の集合 M(s) を求める
       m ∈ M(s) ⟺ (required_environment(m) = ANY
                    または required_environment(m) = environment(s))
                   かつ (required_block_minutes(m) が NULL
                         または required_block_minutes(m) ≤ duration(s))
                   かつ S(m) ≤ d ≤ D(m)
                   かつ R(m) > 0

手順2：M(s) が空でない場合、スロット s の時間を
       M(s) の各教材へ「必要時間比」で按分する

       必要時間(m) = R(m) / speed_eff(m)
       ただし speed_eff(m) が算出不能の場合は R(m) を代替値として用いる

手順3：M(s) が空の場合、そのスロットは未割当とする
```

**設計意図**

環境タグと必要連続時間による適合判定を先に行うことで、「移動中に携帯端末で解く教材」と「連続90分のPC作業を要する教材」が自動的に別のスロットへ振り分けられる。時間の総量ではなく、時間の形状が管理対象となる。

### 9.3 必要速度

```
available_hours(m) = Σ_{d ∈ P(m)} (H(g, d) に占める m への割当時間) × w(g, d)

required_speed(m) = R(m) / available_hours(m)
```

`available_hours(m)` が 0 の場合、必要速度は算出不能とし、締切超過警告の対象とする。

### 9.4 スロット充足の検証

教材登録時および目標の進行中への遷移時に以下を検証する。

```
教材 m について、
  ∃ s ∈ (全ての有効なスロット) :
      (required_environment(m) = ANY
       または required_environment(m) = environment(s))
      かつ (required_block_minutes(m) が NULL
            または required_block_minutes(m) ≤ duration(s))
      かつ (s の適用曜日に、教材期間内の PLAN 日が1日以上存在する)
```

充足しない場合、警告を表示する。保存は許容する。

**警告文の例**

```
教材「{教材名}」は{必要連続時間}分の連続した時間を必要としますが、
現在のリソース設定における最長スロットは{最長時間}分です。
時間の総量は足りていても、この教材に必要な形の時間が
確保されていない可能性があります。
```

これは時間量のみを管理するシステムでは検知できない状態であり、本アプリ固有の検証項目である。

---

## 10. 完了予測日の算出

### 10.1 算出手順

```
required_hours(m) = R(m) / speed_eff(m)

accumulated = 0
d = T

while accumulated < required_hours(m):
    if type(d) = PLAN:
        accumulated += (H(g, d) に占める m への割当時間) × w(g, d)
    d = d + 1日
    
    if d > D(m) + 365日:
        return 算出打ち切り（大幅超過）

forecast_date(m) = d − 1日
```

### 10.2 バッファ日を除外する理由

計画日のみを累積するため、予測は常に悲観方向へ振れる。実際にはバッファ日の学習によって前倒しされるため、「警告は出るが実際には間に合う」方向の誤差となる。

学習計画において、間に合わないものを間に合うと予測する誤りは致命的であるが、その逆は許容できる。したがってこの非対称性は意図的なものである。

### 10.3 乖離日数

```
overrun_days(m) = forecast_date(m) − D(m)
```

正の値は締切超過を意味する。

---

## 11. 判定ロジック

### 11.1 警告判定（NT-01）

```
baseline(m) = plan_baseline のうち material_id = m かつ
              effective_from ≤ T を満たすレコードの中で
              effective_from が最大のもの

条件： quota(m, T) / baseline_daily_quota(baseline(m))
       ≥ 設定値 threshold.warning_ratio（初期値 1.20）
```

`type(T)` が PLAN でない日は判定しない。バッファ日にはノルマが存在しないためである。

### 11.2 強制リプラン判定（NT-02）

```
条件： overrun_days(m) > 設定値 threshold.replan_overrun_days（初期値 3）
```

`speed_eff(m)` が算出不能な場合は判定しない。

### 11.3 二段階構成の意図

| 段階 | 検知対象 | 意味 |
| --- | --- | --- |
| 警告（NT-01） | ノルマの上昇率 | ペースの乱れ。まだ挽回可能な段階 |
| 強制リプラン（NT-02） | 完了予測日の超過 | 合格可能性の毀損。計画の修正が必要 |

警告は「遅れている」ことを、強制リプランは「このままでは間に合わない」ことを示す。前者はバナー表示にとどめ、後者はモーダルで目標編集画面へ誘導する。

実効速度が向上すれば、負債があっても強制リプランは発火しない。逆に毎日ノルマを達成していても、初期の総量見積もりが甘ければ発火する。判定基準を「合格に間に合うか」に置いているためである。

### 11.4 締切超過判定（NT-06）

```
条件： T > D(m) かつ R(m) > 0 かつ material.is_active = true
```

---

## 12. 計画基準値の再設定

### 12.1 再設定の契機

以下の操作により `plan_baseline` に新しいレコードを追加する。

| 契機 | reason |
| --- | --- |
| 目標の進行中への遷移 | INITIAL |
| リプラン操作の実行 | REPLAN |
| 受験日の確定日への変更 | EXAM_DATE_FIXED |
| 教材の追加・総量変更・締切変更 | MATERIAL_CHANGED |
| 予定周回数の変更 | CYCLE_CHANGED |

### 12.2 記録内容

```
T' = max(T, S(m))

effective_from             = T
baseline_daily_quota       = quota(m, T')（再設定後の値）
remaining_at_baseline      = R(m)
plan_days_at_baseline      = |{ d | T' ≤ d ≤ D(m) かつ type(d) = PLAN }|
planned_cycles_at_baseline = N(m)
reason                     = 上記の契機に対応する値
```

`baseline_daily_quota`・`plan_days_at_baseline` の算出にはいずれも T ではなく `T' = max(T, S(m))`
を起点として用いる（`effective_from` 自体は基準値の検索に使う実際の記録日 T のまま変えない）。
再設定の契機（12.1）はすべてユーザー操作であり、教材の学習期間開始日到来を契機とした自動
再設定は存在しない。そのため T をそのまま用いると、学習期間開始前（T < S(m)）に基準値が
記録された教材は quota(m, T) = 0（7.1のS(m)≤T条件）が基準値として固定されてしまい、開始日を
迎えてペースが乱れても11.1の警告判定条件（`baseline_daily_quota <= 0` の場合は判定しない）
により警告が永久に発火しなくなる。学習期間開始時点で見込まれるノルマを基準値とすることで
これを避ける。`plan_days_at_baseline` も同じ T' を起点にしないと、`baseline_daily_quota ×
plan_days_at_baseline = remaining_at_baseline` という関係が崩れ、リプラン履歴表示・AI向け
状況出力の数値が矛盾するため、両者は同じ起点で揃える。

### 12.3 設計意図

リプラン後は新しい計画が基準となるため、リプラン前の乖離を引きずらない。

基準値を履歴として保持することで、「何回リプランしたか」「各リプラン時点でどの程度の負荷だったか」を後から検証できる。**この履歴は総括レポートにおける最重要の分析材料となる。**

---

## 13. 補助指標の算出

### 13.1 バッファ消費率

```
経過バッファ日 = { d | goal.start_date ≤ d < T かつ type(d) = BUFFER }

実績あり日 = 経過バッファ日のうち、study_log が1件以上存在する日

buffer_usage_rate = |実績あり日| / |経過バッファ日|
```

**意味**

余裕度の直接指標である。バッファをほとんど使わずに計画通り進んでいれば余裕があり、毎週末フルに使ってようやく維持している状態であれば計画自体が過密である。

この指標はAIへの入力に含める。数値実績のみからは「計画が重すぎる」という判断は困難だが、バッファ消費率が高止まりしていれば、それは計画の設計そのものに問題があることを示す。

### 13.2 進捗率

```
progress_rate(m) = C(m) / TW(m)
```

教材の単位が「問」「ページ」「回」と異なるため、目標単位の進捗率は単純合算では意味を持たない。ダッシュボードでは教材別に表示し、目標単位では「教材ごとの進捗率の平均」を参考値として表示する。

### 13.3 報告率（KPI）

```
report_rate = (record_state = REPORTED の日数)
              / (goal.start_date から T までの日数)
```

要件定義書に定めたKPIである。

### 13.4 連続報告日数

```
T から遡り、record_state = REPORTED が連続する日数。
type(d) = BUFFER または OFF の日は、報告がなくても連続を中断しない。
```

バッファ日に報告しないことは想定された行動であり、それによって連続記録が途切れるのは設計思想（バッファの非対称性）に反する。

### 13.5 リプラン回数

```
replan_count(g) = plan_baseline のうち、goal 配下の material に属し、
                  reason ≠ INITIAL であるレコード数
```

総括レポートおよびエクスポートに含める。

---

## 14. 品質指標の正規化

### 14.1 変換規則

| 方式 | 入力 | 保存値 |
| --- | --- | --- |
| OBJECTIVE | 0〜100の数値 | そのまま |
| SELF_SCORED | 0〜100の数値 | そのまま |
| SUBJECTIVE | 1〜5の選択 | 1→20, 2→40, 3→60, 4→80, 5→100 |
| NONE | — | NULL |

### 14.2 集約規則

| 粒度 | 算出方法 |
| --- | --- |
| 日別 | その日の値をそのまま表示 |
| 週別 | 週内の値の単純平均 |
| 月別 | 月内の値の単純平均 |

分量による加重平均ではなく単純平均とする。「10問解いて80%」と「100問解いて60%」を加重平均すると後者に引きずられるが、推移の可視化においては各測定機会を等価に扱うほうが変化を読み取りやすい。

### 14.3 周回別の系列分離

品質指標の推移表示では、`cycle_number` ごとに系列を分離する。**2周目以降は記憶による正答が混入するため、1周目と同一系列で表示すると成長を過大評価する。**

グラフ上では、周回の切り替わり時点に区切り線を表示する。

### 14.4 合格基準線

紐づく `exam_subject` の合格点を推移グラフ上に水平線として重畳する。`passing_score_type` が RAW_SCORE（点数入力）の場合は `passing_score / passing_score_max * 100` で百分率へ正規化してから用いる（データ構造編5.3）。教材が複数科目に紐づく場合、正規化後の値が最も高いものを採用する。

### 14.5 SELF_SCORED の位置づけ

記述式など、システムが正誤を判定できない領域では自己採点を用いる。完全な客観性は担保されないが、同一人物が同一基準で採点する限り、**推移としての傾向は十分に有意である**。

正答率が算出できない領域を「測定不能」として放置すると、要件定義書に記した課題3（合格への接近を実感できない）が未解決のまま残る。近似的であっても数値化することに価値がある。

---

## 15. 週次要約の生成

### 15.1 週の定義

月曜日を週の開始日、日曜日を終了日とする。

### 15.2 生成判定

アプリケーション起動時に以下を評価する。

```
手順1：直近の完了週を求める
       last_sunday = T 以前で最も近い日曜日
                     （T が日曜の場合は T 自身）

手順2：遡及対象の週を列挙する
       設定値 summary.lookback_weeks（初期値 4）の範囲で、
       完了しているが weekly_summary が未生成の週を抽出

手順3：抽出された各週について、
       その週に study_log が1件以上存在する目標を対象に生成
       （category=READING の目標は study_log を持たないため、
        変更なしに自動的に対象外となる。21.1参照）

手順4：生成は呼び出し間隔の下限（既定2秒）を守って逐次実行する
```

**設計意図**

「日曜日に必ずアプリを起動するとは限らない」という前提に立つ。生成判定を起動時の遡及方式とすることで、バックグラウンドジョブを持たないローカル環境でも確実に生成される。

遡及上限を設けるのは、長期間の未起動後に大量のAI呼び出しが発生することを防ぐためである。

### 15.3 対話への注入

日次報告のプロンプトには、設定値 `summary.inject_weeks`（初期値 4）で指定された週数分の要約を新しい順に含める。全期間の日記原文は含めない。

---

## 16. AI連携の実装仕様

本章は、AI基盤の開発キットが提供する実仕様に基づく。**開発キットの仕様は変更できない**ため、制約を前提としたアプリケーション側の実装を定義する。

### 16.1 初期化と設定

**認証はPAT（Personal Access Token）を主とする。** 詳細は16.2を参照。

| 設定項目 | 値の出所 |
| --- | --- |
| Host（接続先サブドメイン） | `app_setting: ai.host`（例: `xxx.newton-x.net`） |
| PAT | 開発キットの設定ファイルに委ねる。`app_setting` には保存しない |
| クライアント識別子・テナント識別子 | `app_setting: ai.client_id` / `ai.tenant_id`（EntraIDフォールバック時のみ使用。通常は空でよい） |
| 接続先URL | Hostから自動導出（`https://{host}/api`）。`app_setting: ai.api_base_url` を明示指定した場合はそちらを優先 |
| タイムアウト | `app_setting: ai.timeout_seconds`（既定60） |
| 再試行回数 | `app_setting: ai.max_retries`（既定1） |
| リダイレクト先（フォールバック時のみ） | 開発キットの既定値（ローカル8400番） |

**再試行回数を1とする理由**

開発キットの既定値は3である。アプリケーション側でも再送を提供するため、既定のままでは実質的に多重の再試行となり、頻度制限に抵触するリスクが高まる。開発キット側を1に抑え、再送の判断を利用者に委ねる。

**ポートの衝突回避**

認証フローがローカル8400番を使用するため、アプリケーションサーバは別ポートで起動する。`app_setting: server.port` の既定値を8100とする。

### 16.2 認証

**開発キットの現行の公式利用ガイドは、Host＋PATによる認証を標準の方法として案内している。** ブラウザ経由のOAuth／デバイスコード認証（EntraID）は、開発キット内部にフォールバック経路として残るのみであり、PATが設定されていれば優先して使用される。当初の設計はブラウザ認証を主経路としていたが、公式情報に基づき以下のとおり改める。

```
起動時：
    Host・PATが設定済みかを確認する（開発キットの設定を参照）
    設定済み → PATによる認証ヘッダーが利用可能かを確認し、認証済みとする
    未設定・無効 → 未認証として扱い、設定画面でのHost・PAT入力を促す

設定画面からのPAT登録：
    NewtonX Web版で発行したPATとHostを入力し、
    開発キットの設定（ConfigManager）へ反映する
    以降、追加の認証操作は不要

フォールバック（PAT未設定時のみ）：
    設定画面からブラウザ経由の認証フローを起動できる
    バックグラウンドスレッドで実行する
    （ブラウザ起動と待機を伴うため、APIスレッドをブロックしない）
    クライアントは認証状態をポーリングして完了を検知する
```

トークン・PATの保存は開発キットに委ねる。アプリケーション側で独自に保管しない。

### 16.3 会話の管理

**制約：メッセージ送信前に会話を作成し、その識別子を指定する必要がある。**

**方針：用途と日付ごとに会話を分離する。**

```
手順1：会話を格納するフォルダを確保する
       目標に紐づく会話（週次要約・総括レポート）：
           フォルダ名 = "{ai.folder_prefix}_{目標名}"
       目標に紐づかない会話（今日の一言・日次報告）：
           フォルダ名 = "{ai.folder_prefix}"（共通フォルダ）
       名前指定でのフォルダ内会話作成機能を用い、
       存在しない場合は自動作成する

手順2：ai_conversation テーブルから、
       (goal_id, scope, scope_key) に一致するレコードを検索する

手順3：存在する場合 → conversation_uid を使用する
       存在しない場合 → 会話を新規作成し、識別子を保存する

手順4：メッセージを送信する
       Web検索：明示的に無効化する（既定値が有効のため必須）
       ナレッジ検索：無効化する
       親メッセージ順序：last_parent_order を送信する（16.3.1参照）

手順5：送信後、last_parent_order を更新する（将来の開発キット改修に備えた記録。16.3.1参照）
```

#### 16.3.1 親メッセージ順序に関する既知の制約（要検証）

導入済みの開発キット（NewtonX ADK v0.10.5）を実際にソースコードで確認したところ、`send_message()`のメソッドシグネチャに親メッセージ順序を指定する引数が存在せず、送信内容には常に固定値0が設定される（バンドルされた公式ドキュメントとの矛盾を確認済み）。したがって、`last_parent_order` を送信しても現状は文脈維持に寄与しない。

**この制約を前提に、同一日内の複数往復における文脈維持は17.2の `{{conversation_history}}`（本アプリが `chat_message` テーブルに保持する対話履歴の全文注入）で実現する。** この方式はAI基盤側のスレッド機能の実装状況に依存しないため、上記の制約があっても文脈維持は成立する。

`ai_conversation.last_parent_order` の記録自体は将来の開発キット改修に備えて継続するが、実装時点では実効性を持たない値として扱う。開発キット側でこの制約が解消された場合は、対話履歴の全文注入を縮退時のみに用いる構成へ最適化してよい（22.未決事項に追記）。

**回答が出力上限で途切れた場合の継続取得について**：開発キットのベストプラクティスは「終端マーカー方式」（親メッセージ順序を指定して続きを要求する）を推奨しているが、これも親メッセージ順序に依存するため本アプリでは使用しない。代わりに、直前の応答本文（またはその末尾）をプロンプトへ含めて「以下の続きを出力してください」と自己完結した指示で新規に送信する方式を取る。

**会話の単位**

| 用途 | scope_key | 会話タイトル |
| --- | --- | --- |
| 日次報告 | 対象日 | `{日付} 日次報告` |
| 週次要約 | 週開始日 | `{週開始日}週 週次要約` |
| 今日の一言 | 対象日 | `{日付} 今日の一言` |
| 総括レポート | 固定値 "main" | `総括レポート` |

**設計意図**

会話を用途ごとに分離することで、異なる目的のプロンプトが文脈として混入することを防ぐ。日次報告のフィードバックが、同日朝に生成した「今日の一言」の文脈に引きずられる事態を構造的に排除する。

目標に紐づかない会話（今日の一言・日次報告）も、goal_idはNULLのまま共通フォルダ（ai.folder_prefix）配下に作成する。これにより、NewtonX側でユーザがこの共通フォルダを削除するだけでアプリ由来の会話をまとめて解放できる。フォルダ配下に日付ごとの会話が複数蓄積されることは許容する（会話の分離方針とは独立した要件のため）。

日をまたぐ文脈は、会話の継続によってではなく、週次要約をプロンプトに注入することで実現する。この方式であれば、注入する情報量をアプリケーション側で完全に制御でき、会話の肥大化による入力上限の超過も回避できる。

**アシスタントへのナレッジ登録は行わない。** アシスタントが組織内で共有される可能性があり、個人の学習記録を登録することは不適切である。

### 16.4 呼び出し頻度の制御

```
直近の呼び出し時刻を保持し、
経過時間が設定値 ai.min_interval_seconds（既定2秒）未満の場合、
差分だけ待機してから送信する
```

週次要約の遡及生成では複数回の呼び出しが連続するため、この制御が特に重要となる。

### 16.5 プロンプトの段階的縮退

**制約：入力上限は明示されていない。**

**方針：組み立て後の文字数が閾値を超える場合、優先度の低い情報から削減する。**

```
手順1：全情報を含むプロンプトを組み立てる

手順2：文字数が設定値 ai.max_prompt_chars（既定30000）以下なら
       そのまま送信する

手順3：超過する場合、以下の順序で削減し、
       各段階で再度文字数を評価する

  段階1：週次要約を古い順に1件ずつ除外する（最低1件は残す）
  段階2：締切が遠い教材の情報を集約表示に置き換える
  段階3：当日の対話履歴を古い往復から除外する
  段階4：日記本文を末尾優先で切り詰める（先頭から削る）

手順4：縮退が発生した場合、
       ai_log の was_truncated を true として記録し、
       応答とともに縮退の発生を画面に通知する
```

**段階4で「先頭から削る」理由**

日記は時系列で書かれるため、末尾ほど直近の状況を反映している。切り詰める場合、末尾を残すほうが情報価値が高い。

**運用による上限の把握**

`ai_log.prompt_chars` と `was_truncated` を記録することで、実際にどの文字数で問題が生じたかを運用の中で把握できる。閾値の初期値30000は暫定であり、実測に基づいて調整する。

### 16.6 例外処理

| 開発キットの例外 | APIエラーコード | 画面での扱い |
| --- | --- | --- |
| 認証関連の例外 | AI_AUTH_REQUIRED | 設定画面での再認証を促す |
| API通信関連の例外 | AI_ERROR | エラー内容と再送ボタンを表示 |
| チャット関連の例外 | AI_ERROR | 同上 |
| 設定エラー | AI_CONFIG_ERROR | 設定画面への遷移を促す |
| タイムアウト | AI_TIMEOUT | 再送ボタンを表示 |
| 基本例外（上記以外） | AI_ERROR | エラー内容を表示 |

**API通信関連の例外は、頻度制限に起因する可能性がある。** この場合、エラーメッセージに「時間をおいて再試行してください」を含める。

**実装上の注意（タイムアウトの判定方法）**：導入した開発キットの例外クラスは `NewtonXError` / `AuthenticationError` / `APIError` / `ConfigurationError` / `FileUploadError` / `ChatError` の6種であり（ソースコードで確認済み）、タイムアウト専用の例外クラスは存在しない。通信タイムアウトも内部的に `APIError` へ一律ラップされるため、型による判定はできない。呼び出し側で計測した経過時間が `ai.timeout_seconds` を超えているか、またはエラーメッセージにタイムアウトを示す文字列が含まれるかによって `AI_TIMEOUT` を判定する。

### 16.7 障害時の動作保証

**AI呼び出しの失敗により、実績入力の内容が失われてはならない。**

| 保証項目 | 実装方針 |
| --- | --- |
| 入力内容の保持 | 日次報告画面では、実績と日記をクライアント側の状態として保持する。AI応答の成否と独立させる |
| 確定の許容 | AI応答が得られない状態でも報告の確定を許容する。`chat_message` は空となる |
| 記録機能の継続 | AI基盤が利用不可でも、実績登録、閲覧、分析は全て動作する |
| 状態の表示 | AI基盤の稼働状況を起動時に確認し、利用不可の場合はダッシュボードに表示する |

AIとの対話は本アプリの中核だが、それが利用できないことを理由に記録自体が失われるのは本末転倒である。

### 16.8 通信ログ

設定 `log.ai_enabled` が有効な場合、全ての呼び出しについて `ai_log` にレコードを追加する。

記録項目には、送信文字数と縮退の有無を含める。これにより、入力上限の実測値を運用の中で把握できる。

保持期間を超過したレコードは起動時に削除する。

---

## 17. プロンプト設計

### 17.1 設計方針

| 項目 | 方針 |
| --- | --- |
| 出力形式 | 自然文。構造化出力（JSON）を要求しない |
| 変数記法 | `{{変数名}}` |
| 編集可能性 | 設定画面から全文を編集可能。初期値への復帰も可能 |
| 保存場所 | `prompt_template` テーブル。ソースコードに埋め込まない |

AI基盤が構造化出力機能を持たないため、MVPでは応答の機械的解析を行わない。応答はそのまま表示する。この設計により、多層的なプロンプト防御が不要となる。

### 17.2 日次報告フィードバック（DAILY_FEEDBACK）

**注入する変数**

| 変数名 | 内容 |
| --- | --- |
| `{{today}}` | 論理的な本日の日付 |
| `{{day_type}}` | 本日の日種別 |
| `{{load_coefficient}}` | 本日の負荷係数 |
| `{{goal_summary}}` | 目標名、科目構成、各科目の受験日、残日数 |
| `{{material_status}}` | 教材ごとの総量、予定周回、現在周回、残量、締切、日次ノルマ、必要速度、実効速度、完了予測日、乖離日数 |
| `{{slot_summary}}` | 本日利用可能なスロットと教材への割当 |
| `{{buffer_usage_rate}}` | バッファ消費率 |
| `{{today_logs}}` | 本日の教材別実績（投下時間、完了分量、周回、品質指標） |
| `{{diary_body}}` | 本日の行動・所感（複数目標分の日記エントリを`ai_context_service.build_diary_text`で1本の文字列に整形したもの。内容のある目標が2件以上のときのみ`■ 目標名`見出しで結合し、1件のみなら見出しなしでその目標の文章をそのまま渡す。テンプレート変数自体は目標が増えても変更しない） |
| `{{diary_learned}}` | 本日学んだこと（`{{diary_body}}`と同じ整形規則） |
| `{{weekly_summaries}}` | 直近の週次要約 |
| `{{conversation_history}}` | 本日の対話における過去の往復 |

**初期文面**

```
あなたは資格試験の学習を伴走する専門コーチです。
学習者の日次報告を受け取り、フィードバックを返してください。

# あなたの目的
学習者を試験合格に導くことがあなたの唯一の目的です。
計画を予定通り遂行させることが目的ではありません。
この二つが矛盾する場合、必ず合格を優先してください。

# 本日の状況
日付：{{today}}
日種別：{{day_type}}
負荷係数：{{load_coefficient}}

# 目標
{{goal_summary}}

# 教材ごとの計画状態
{{material_status}}

# 本日利用可能だった学習時間
{{slot_summary}}

# バッファ日の消費状況
{{buffer_usage_rate}}

# 本日の実績
{{today_logs}}

# 本日の記録
【行動・所感】
{{diary_body}}

【学んだこと】
{{diary_learned}}

# これまでの経過（週次要約）
{{weekly_summaries}}

# 本日のこれまでの会話
{{conversation_history}}

---

# 複数目標がある場合の注意

学習者が複数の目標を同時に進めている場合があります。
上記の「目標」「教材ごとの計画状態」「本日の記録」には、
該当する目標名が「■ 目標名」の見出しで示されています。

学習者の発言が特定の目標を指している場合は、その目標の
状況に絞って回答してください。複数の目標にまたがる発言で
あれば、目標ごとに見出しを付けて区別しながら回答してください。

どの目標についての発言か判断がつかない場合は、
推測で決めつけずに学習者に確認してください。

---

# フィードバックの構成

以下の四点を、この順序で述べてください。

## 1. 状況の解釈
数値実績と記述内容を突き合わせ、本日の学習がどのような状態
だったかを解釈してください。数値だけを繰り返すのではなく、
記述内容から読み取れる背景要因（業務の繁忙、疲労、集中度、
理解の詰まり）を数値と関連付けて述べてください。

## 2. 成長の指摘
「学んだこと」の記述を、過去の週次要約に含まれる同一主題の
記述と比較してください。説明の解像度、用語の正確さ、
構造化の度合いに変化があれば、具体的に指摘してください。

これは正答率では測定できない成長です。
変化が見られない場合は無理に指摘せず、この項目を省略して
構いません。

## 3. 明日以降の行動提案
具体的で実行可能な提案を、多くとも三点に絞ってください。
「頑張りましょう」のような抽象的な激励は不要です。

## 4. 計画に関する提案（必要な場合のみ）
計画そのものに問題があると判断した場合のみ、以下から
適切な段階を選んで提案してください。

【軽度】特定期間の負荷係数を下げる
【中度】教材の優先順位を変更する、学習範囲を絞り込む、
        予定周回数を減らす
【重度】受験日を後ろ倒しにする、目標を一時停止する
【即時】本日は休息を取る

計画に問題がなければ、この項目は省略してください。

---

# 重要な原則

**負荷を減らす提案を、増やす提案と同等に扱ってください。**

遅れが出ているとき、ノルマを引き上げる提案は最も安易な選択肢
ですが、多くの場合それは正しくありません。学習者が疲弊して
学習を中断すれば、合格の可能性はゼロになります。

以下の場合、負荷を減らす提案を積極的に検討してください。
- 記述内容から疲労や余裕のなさが読み取れる
- バッファ日の消費率が継続的に高い
- 分量は達成しているが品質指標が停滞している
- 必要速度が実効速度を大きく上回っており、
  現実的な努力では埋まらない

品質指標が停滞している場合、量を増やすことは逆効果である
可能性が高いことに留意してください。同じ教材を漫然と周回する
のではなく、誤答分野への集中や理解の確認といった、
やり方の変更を提案してください。

周回数についても同様です。3周する計画が過大であるなら、
2周に減らして1周あたりの精度を上げるほうが、
合格の可能性は高まります。

**「今日は休みましょう」と言うことを躊躇しないでください。**
それが合格への最短経路である場合があります。

# 出力形式
自然な文章で記述してください。
見出しは用いても構いませんが、過度に構造化せず、
対話として自然な長さに収めてください。
```

### 17.3 週次要約（WEEKLY_SUMMARY）

**注入する変数**

| 変数名 | 内容 |
| --- | --- |
| `{{week_range}}` | 対象週の期間 |
| `{{goal_name}}` | 目標名 |
| `{{week_logs}}` | 週内の日別実績（周回を含む） |
| `{{week_diaries}}` | 週内の日記（行動・所感、学んだこと） |
| `{{week_metrics}}` | 週の集計値（総投下時間、総完了量、品質指標の平均、報告日数、バッファ消費） |
| `{{anonymize}}` | 匿名化の指示（匿名化版生成時のみ） |

**初期文面**

```
以下は、資格試験の学習における一週間分の記録です。
この記録を、後日の振り返りに使えるよう要約してください。

# 対象週
{{week_range}}

# 目標
{{goal_name}}

# 実績
{{week_logs}}

# 集計
{{week_metrics}}

# 日々の記録
{{week_diaries}}

---

# 要約の要件

この要約は、後日のフィードバック生成時に「過去の経過」として
参照されます。原文は参照されないため、**この要約に含まれない
情報は失われる**ことに留意してください。

以下を必ず含めてください。

1. 学習量と品質の推移（数値を含む）
2. この週に学習した主題と、その理解度に関する記述
   （後日の比較対象となるため、学習者が用いた表現や
     説明の粒度を保存してください）
3. 学習の妨げとなった要因（業務、体調、家庭の事情など、
   記録に現れたもの）
4. 学習者の状態に関する所見（意欲、疲労度、余裕の有無）
5. 前週からの変化
6. 教材の周回が進んだ場合、その事実と切り替えの時期

以下は含めないでください。
- 励ましや評価のコメント
- 今後の提案

これは記録であり、フィードバックではありません。
事実と観察に限定してください。

{{anonymize}}

# 出力形式
簡潔な文章で記述してください。
情報の欠落を避けることを最優先とし、
文章の美しさは考慮しなくて構いません。
```

匿名化版の生成時、`{{anonymize}}` に以下を差し込む。

```
# 追加の制約（匿名化）
業務内容、勤務先、家庭の事情、健康状態など、
個人を特定しうる情報および私的な事情への言及を
一切含めないでください。
学習の妨げとなった要因については、
「業務多忙」「体調不良」といった一般化された表現に
留めてください。
```

### 17.4 今日の一言（DAILY_MESSAGE）

複数目標が同時進行している場合、このプロンプトは**目標ごとに独立して**呼び出す（1呼び出し=1目標、他目標の情報は一切渡さない）。順調な目標の情報に引っ張られて、停滞している目標の実態と乖離した一言が生成されることを構造的に防ぐため（未決事項L-04）。ACTIVEな目標が1件も無い日のみ、`{{goal_summary}}`等が「進行中の目標はありません」となる状態で1回呼び出す。テンプレート本文・変数自体は変更しない。

**注入する変数**

| 変数名 | 内容 |
| --- | --- |
| `{{today}}` | 論理的な本日の日付 |
| `{{day_type}}` | 本日の日種別 |
| `{{goal_summary}}` | 目標名と残日数 |
| `{{progress_summary}}` | 教材ごとの進捗率と現在周回 |
| `{{recent_activity}}` | 直近7日間の報告状況と実績 |

**初期文面**

```
学習者がアプリケーションを起動した際に表示する、
短い一言を生成してください。

# 状況
日付：{{today}}
日種別：{{day_type}}

# 目標
{{goal_summary}}

# 進捗
{{progress_summary}}

# 直近の活動
{{recent_activity}}

---

# 要件

- **一文から二文**。長くしないでください
- 前向きな内容にしてください
- 進捗の具体的な事実に触れてください。
  抽象的な励ましは避けてください
- 遅れている場合でも、責める表現は用いないでください。
  事実に触れつつ、前を向ける表現にしてください
- バッファ日の場合、休息を肯定する内容も適切です
- 周回が切り替わった直後であれば、その節目に触れても
  構いません
- 毎日表示されるため、定型的な表現の繰り返しを避けてください

# 例（形式の参考）
「過去問道場が2周目に入りました。1周目より速く進んでいます」
「3日ぶりですね。今日は30分だけでも進めれば流れが戻ります」
「今日はバッファ日です。休むことも計画のうちです」

# 出力形式
一言のみを出力してください。前置きや説明は不要です。
```

### 17.5 総括レポート（GOAL_RETROSPECTIVE）

**注入する変数**

| 変数名 | 内容 |
| --- | --- |
| `{{goal_summary}}` | 試験名、学習期間、科目構成 |
| `{{material_summary}}` | 教材ごとの総量、予定周回、実績周回、投下時間 |
| `{{overall_metrics}}` | 総投下時間、学習日数、報告率、バッファ消費率、リプラン回数 |
| `{{quality_trend}}` | 品質指標の推移（周回別、月次集約） |
| `{{replan_history}}` | リプラン履歴（日付、契機、変更前後のノルマ、その時点の残量と残日数） |
| `{{weekly_summaries}}` | 全週の要約 |
| `{{exam_results}}` | 科目ごとの合否と得点 |
| `{{anonymize}}` | 匿名化の指示（匿名化版生成時のみ） |

**初期文面**

```
以下は、ある学習者が資格試験に向けて行った学習の全記録です。
この記録を分析し、総括レポートを作成してください。

# 目標
{{goal_summary}}

# 教材
{{material_summary}}

# 全体指標
{{overall_metrics}}

# 品質指標の推移
{{quality_trend}}

# 計画の修正履歴
{{replan_history}}

# 週ごとの経過
{{weekly_summaries}}

# 受験結果
{{exam_results}}

---

# このレポートの目的

このレポートには二つの読み手がいます。

1. 学習者本人（次回の受験、または別の試験に向けた振り返り）
2. 同じ試験を目指す他の学習者（経験の横展開）

したがって、**再現可能な知見**として記述してください。
「頑張った」「大変だった」といった感想ではなく、
**何を、どれだけ、どのように行った結果どうなったか**を
事実に基づいて述べてください。

---

# レポートの構成

## 1. 学習の全体像
期間、総投下時間、教材構成、周回数を簡潔にまとめてください。
数値を明示してください。

## 2. 計画と実績の乖離
当初の計画がどの程度実行されたかを述べてください。
計画の修正履歴を参照し、以下を分析してください。

- いつ、何回、どのような理由で計画を修正したか
- 修正のタイミングは適切だったか、遅すぎなかったか
- 修正が必要になった根本原因は何か
  （初期見積もりの甘さ、外部要因、実行力の問題のいずれか）

**この分析が本レポートの中核です。**
計画がいつ崩れ、いつ修正されたかという情報は、
通常の学習記録には残りません。丁寧に分析してください。

## 3. 品質の推移
品質指標がどのように推移したかを述べてください。
周回ごとの変化、停滞した時期、その時期に何が起きていたかを
週次要約と突き合わせて分析してください。

## 4. 結果の要因分析

### 合格していた場合
何が有効だったかを特定してください。
同じ試験を目指す人が再現すべき要素を、具体的に挙げてください。
時間配分、教材の選択、周回の進め方、修正の判断など、
再現可能な形で記述してください。

同時に、**運が良かっただけの要素**があれば
それも指摘してください。再現性のない成功要因を
一般化することは害になります。

### 不合格だった場合
何が不足していたかを特定してください。
学習者を責めるのではなく、構造的な要因を分析してください。

以下の観点で検討してください。
- 初期の総量見積もりは妥当だったか
- 確保できる時間に対して計画は現実的だったか
- 計画の修正が遅れた時点はあったか
- 品質指標の停滞を放置した期間はあったか
- 教材の選択や周回の進め方に問題はなかったか
- 学習時間の「形」（連続時間の確保）は適切だったか

そのうえで、**具体的に何をすべきだったか**を述べてください。
「もっと勉強すべきだった」ではなく、
「9月中旬の時点で必要速度が実効速度の1.5倍に達していたため、
この時点で受験日を後期に変更すべきだった」のように、
時点と根拠を伴う形で記述してください。

## 5. 次に向けた提言
学習者が次に同じ試験、または類似の試験に挑む際の
具体的な提言を、三点以内で述べてください。

---

# 記述上の注意

- 数値と日付を伴わない主張は避けてください
- 記録に現れていないことを推測で補わないでください
- 合格を美化せず、不合格を過度に否定的に扱わないでください
- 他の学習者が読むことを前提に、
  この学習者固有の事情に依存しない知見を優先してください

{{anonymize}}

# 出力形式
見出しを用いた構造的な文書として記述してください。
分量の制約はありません。分析の質を優先してください。
```

匿名化版の生成時、`{{anonymize}}` に以下を差し込む。

```
# 追加の制約（匿名化）
業務内容、勤務先、家庭の事情、健康状態など、
個人を特定しうる情報および私的な事情への言及を
一切含めないでください。
学習の妨げとなった要因については、
「業務多忙な時期」「体調を崩した週」といった
一般化された表現に留めてください。
```

---

### 17.6 読書日次報告フィードバック（DAILY_FEEDBACK_READING）

**注入する変数**

| 変数名 | 内容 |
| --- | --- |
| `{{today}}` | 論理的な本日の日付 |
| `{{book_summary}}` | 書名、著者、読了目標日、残日数 |
| `{{today_recall}}` | 本日の想起本文、読んだページ数、現在ページ（入力があれば） |
| `{{recent_recalls}}` | 直近の想起記録（21.4参照。週次要約を持たないため原文を直接注入する） |
| `{{conversation_history}}` | 本日の対話における過去の往復 |

**初期文面**

```
あなたは読書に伴走する対話相手です。

# あなたの目的
学習者が読んだ内容を自分の言葉で振り返り、言語化を
深める手助けをすることが目的です。
読書量やペースを管理したり、達成を評価したりすることは
あなたの目的ではありません。

# 書籍
{{book_summary}}

# 本日の想起
{{today_recall}}

# これまでの記録
{{recent_recalls}}

# 本日のこれまでの会話
{{conversation_history}}

---

# フィードバックの構成

以下を、自然な対話として述べてください。
構成を厳密に守る必要はありません。

## 1. 内容への応答
想起された内容を受け止め、書かれていることに対して
具体的に反応してください。要約を繰り返すのではなく、
「なぜそう感じたのか」「それは以前の記述とどうつながるか」
を掘り下げる問いかけを含めてください。

## 2. 気づきの接続
これまでの記録に同じテーマや似た気づきがあれば、
それとの関連を指摘してください。読書を通じた考えの変化を
本人が自覚できるようにするためです。

## 3. 問いかけ（任意）
必要であれば、次に読む部分への関心を高める軽い問いを
一つ添えてください。義務的な宿題にならないよう、
強制力のない形にしてください。

---

# 重要な原則

**ページ数や読了ペースを評価しないでください。**
何ページ読んだか、目標日までに間に合うかといった
定量的な進捗管理は、この対話の役割ではありません。

**記録が少ない日、短い想起であっても、否定的に扱わないで
ください。** 一言の感想であっても、それを言語化したこと
自体を尊重してください。

**「頑張りましょう」「このペースでは間に合いません」のような
督促や評価はしないでください。** 読書は管理対象ではなく、
本人のための記録です。

# 出力形式
自然な文章で記述してください。
対話として自然な長さに収めてください。
```

### 17.7 読了レポート（GOAL_RETROSPECTIVE_READING）

**注入する変数**

| 変数名 | 内容 |
| --- | --- |
| `{{book_summary}}` | 書名、著者、読書期間（開始日〜読了日） |
| `{{overall_metrics}}` | 記録日数、最長連続記録日数 |
| `{{reading_logs}}` | 想起記録を record_date の昇順で連結したもの（21.4参照） |
| `{{anonymize}}` | 匿名化の指示（匿名化版生成時のみ） |

**初期文面**

```
以下は、ある読者が1冊の本を読み進める中で書き残した
想起の記録です。この記録をもとに、読了レポートを
作成してください。

# 書籍
{{book_summary}}

# 記録量
{{overall_metrics}}

# 日々の想起記録
{{reading_logs}}

---

# このレポートの目的

このレポートには複数の使い方があります。

1. 読者本人による読了後の振り返り
2. ブログ記事など、他者に向けた発信の下敷き
3. 他者にこの本を紹介する際の資料

したがって、記録に書かれていた事実（読者が実際に何を
感じ、何を考えたか）を丁寧に拾いながら、単なる記録の
時系列列挙ではなく、まとまりのある文章として構成して
ください。

---

# レポートの構成

## 1. 本の要約
書籍の内容を、記録から読み取れる範囲で簡潔に要約して
ください。記録に現れていない一般的な書評的知識で
補わないでください。

## 2. 感じたこと・考えたこと
記録全体を通じて読者がどのような感情や考えを持ったかを、
具体的な記述を引用しながらまとめてください。
読書の途中で考えが変化した箇所があれば、その変化を
時系列で示してください。

## 3. 気づき
記録の中で読者自身が「気づいた」「初めて分かった」と
記述している内容、またはそれに相当する記述を抽出し、
まとめてください。

## 4. 印象に残った部分
記録の中で特に多く言及されている、あるいは強い感情を
伴って書かれている箇所を挙げてください。

---

# 記述上の注意

- 記録に現れていない内容を推測で補わないでください
- 読者を評価したり、読み方を批評したりしないでください
- 他者が読むことを想定し、この読者固有の事情に依存しない
  範囲で、本の魅力が伝わる記述を心がけてください

{{anonymize}}

# 出力形式
見出しを用いた構造的な文書として記述してください。
分量の制約はありません。
```

匿名化版の生成時、`{{anonymize}}` に以下を差し込む。

```
# 追加の制約（匿名化）
業務内容、勤務先、家庭の事情、健康状態など、
個人を特定しうる情報および私的な事情への言及を
一切含めないでください。
```

---

## 18. ナレッジエクスポートの生成ロジック

### 18.1 生成手順

```
手順1：出力対象の目標がクローズ済みであることを確認する

手順2：総括レポートが未生成の場合、生成する

手順3：匿名化が指定された場合、
       週次要約と総括レポートの匿名化版を生成する
       （既に存在する場合は再利用する）
       category=READINGの目標には週次要約が存在しないため、
       読了レポート（goal_retrospective）の匿名化版のみ生成する（18.2）

手順4：選択された項目のみを収集する

手順5：Markdown形式とJSON形式の双方を生成する

手順6：出力ディレクトリに保存し、パスを返す
```

### 18.2 匿名化の処理

| 対象 | 処理 |
| --- | --- |
| 日記本文 | 出力から除外する |
| AI対話履歴 | 出力から除外する |
| 週次要約 | 匿名化指示を付与して再生成する |
| 総括レポート | 匿名化指示を付与して再生成する |
| 実績数値 | そのまま出力する（個人特定情報を含まないため） |
| リプラン履歴 | そのまま出力する |

匿名化版は元の版とは別レコードとして保持し、元の版を削除しない。

**読書目標（category=READING）の場合**：「日記本文」は`reading_log.recall_body`に読み替えて除外する。「週次要約」は対象外（存在しないため）。「総括レポート」は読了レポートに読み替え、匿名化指示を付与して再生成する。「実績数値」「リプラン履歴」も対象外（存在しないため）。

### 18.3 生成時の注意

匿名化版の生成には、週数分のAI呼び出しが発生する。呼び出し間隔の下限を守り、進捗を画面に表示する。読書目標では週次要約が存在しないため、匿名化版生成に伴うAI呼び出しは読了レポート1回のみとなる。

---

## 19. 算出処理の性能設計

### 19.1 課題

ダッシュボードの初期表示要件は2秒以内である。しかし日次ノルマ、実効速度、完了予測日の算出には、日種別の解決を締切までの全日について行う必要がある。

### 19.2 対策

| 対策 | 内容 |
| --- | --- |
| 日種別の一括解決 | 期間内の `holiday`、`calendar_day_override`、`day_type_default` を一度に読み込み、メモリ上で辞書化する |
| 実績の一括集計 | 教材ごと周回ごとの累積完了量と実効速度を、単一の集計クエリで全教材分を取得する |
| 完了予測の反復上限 | 反復回数に上限を設ける（締切+365日）。超過時は「大幅超過」として打ち切る |
| リクエスト内キャッシュ | 同一リクエスト内で同じ日の日種別を再解決しない |
| AI呼び出しの分離 | 今日の一言をダッシュボードのエンドポイントに含めず、別途取得する |

### 19.3 キャッシュを行わない箇所

日次ノルマの算出結果はキャッシュしない。日付をまたいだ際の不整合や、実績登録後の再算出漏れを防ぐためである。算出コストは上記の対策により十分に低い。

---

## 20. 検証仕様（テスト観点）

以下は実装時に単体テストで検証すべき観点である。

| 分類 | 検証観点 |
| --- | --- |
| 境界時刻 | 境界時刻より前の時刻において論理日が前日になること |
| 境界時刻 | 境界時刻が0の場合、システム日付と一致すること |
| 日種別解決 | 個別指定が祝日および曜日既定より優先されること |
| 日種別解決 | 祝日の扱いが設定値に従って切り替わること |
| 周回 | 1周目完了時に残量が0にならず、2周目のノルマが算出されること |
| 周回 | 現在周回が累積完了量から正しく導出されること |
| 周回 | 予定周回数を完了済み周回数未満に変更できないこと |
| 周回 | 予定周回数の変更で基準値が再設定されること |
| 日次ノルマ | 7日連続未達時、全量繰越（8倍）ではなく再配分（約2.2倍）になること |
| 日次ノルマ | バッファ日のノルマが0になること |
| 日次ノルマ | 負荷係数を変更しても総量が保存されること |
| 日次ノルマ | 残計画日が0の場合に例外が発生しないこと |
| 実効速度 | 周回ごとに分離して算出されること |
| 実効速度 | 現在周回のサンプルが3件未満のとき直近周回の値で代替されること |
| 実効速度 | 時間未入力の実績が分母・分子とも除外されること |
| 実効速度 | OFF日の実績が除外されること |
| 実効速度 | 実績が存在しない場合に算出不能として扱われること |
| 完了予測 | バッファ日が累積対象から除外されること |
| 完了予測 | 反復上限で打ち切られること |
| 判定 | 警告倍率および超過日数の閾値が設定値から読まれること |
| 判定 | バッファ日に警告判定が行われないこと |
| 基準値 | リプラン後に新しい基準で警告判定が行われること |
| スロット | 環境タグと必要連続時間による適合判定が正しいこと |
| スロット | 適合するスロットが存在しない場合に警告が出ること |
| 状態遷移 | 確定済み記録への更新が拒否されること |
| 状態遷移 | 前日を超える遡及入力が拒否されること |
| 状態遷移 | 進捗のみ登録から報告への昇格が前日以内に限定されること |
| 状態遷移 | リソース不足時に一時停止からの復帰が拒否されること |
| 品質指標 | 主観的手応えが正しく正規化されること |
| 品質指標 | 周回別に系列が分離されること |
| 週次要約 | 未起動期間があっても遡及して生成されること |
| 週次要約 | 遡及上限を超えて生成されないこと |
| AI連携 | Web検索が明示的に無効化されて送信されること |
| AI連携 | 会話識別子が保存され、再利用されること |
| AI連携 | 親メッセージ順序が同一会話内で増加すること |
| AI連携 | 呼び出し間隔の下限が守られること |
| AI連携 | プロンプトが閾値超過時に段階的に縮退すること |
| AI連携 | 縮退の発生がログに記録されること |
| AI連携 | AI呼び出し失敗時に実績入力が失われないこと |
| AI連携 | AI応答なしでも報告確定が可能なこと |
| エクスポート | 選択した項目のみが出力されること |
| エクスポート | 匿名化時に日記本文が除外されること |
| エクスポート | JSON出力をインポートして復元できること |
| 読書 | reading_logが(daily_record_id, book_id)単位で一意登録されること |
| 読書 | 週次要約の生成対象からcategory=READINGの目標が除外されること |
| 読書 | category=READINGの目標がresource_ratioの合計計算に算入されないこと |
| 読書 | 1目標に2冊目の書籍を登録しようとするとBOOK_ALREADY_EXISTSで拒否されること |
| 読書 | GET /goals/{id}/planがREADINGで日次ノルマ等を返さず、残日数・直近記録日・連続記録日数を返すこと |
| 読書 | 連続記録日数（21.3）がバッファ日・除外日による中断除外を行わないこと |

---

## 21. 読書目標の扱い

### 21.1 適用範囲

本文書の第3〜14章は、`goal.category = EXAM`（資格試験）の目標を対象とした算出ロジックである。`category = READING`（読書）の目標には適用しない（要件定義書R-71、仕様書6.10）。読書目標が用いる算出ロジックは本章にまとめる。

第3章（1日の境界時刻の解決）は日次記録全体に関わる共通基盤であり、読書目標にもそのまま適用する。「今日」の判定は `GET /records/today` に一元化されており、読書に限って例外を設けない。

第15章（週次要約の生成）の対象判定（15.2手順3「その週にstudy_logが1件以上存在する目標を対象に生成」）は、読書目標にはstudy_logが存在しないため、ロジックの変更なしに自動的に読書目標を対象外とする。

**資格試験用AI機能との切り分け**：日次報告フィードバック（17.2 DAILY_FEEDBACK）・今日の一言（17.4 DAILY_MESSAGE）が対象とする「進行中の目標」は、`ai_context_service.list_active_exam_goals`によりEXAMのみへ限定する（Phase15で対応）。限定しない場合、`{{goal_summary}}`に読書目標が「試験科目未登録」という誤った文脈で混入するためであり、読書用のDAILY_FEEDBACK_READING（17.6）が別途存在する以上、DAILY_FEEDBACK・DAILY_MESSAGEは恒常的にEXAM専用として扱う（読書用の「今日の一言」相当機能は設けない設計、要件定義書6.10）。

`POST /goals/{id}/retrospective`（`retrospective_service.generate_retrospective`）はPhase16で `goal.category` に応じてGOAL_RETROSPECTIVE（17.5）／GOAL_RETROSPECTIVE_READING（17.7）へ分岐する実装とした。Phase15時点で暫定的に設けていた「READING目標への呼び出しをVALIDATION_ERRORで拒否するガード」はこの分岐に置き換えて解消済みである（書籍が未登録のREADING目標に対する呼び出しのみ、引き続きVALIDATION_ERRORとする）。

**対話履歴の分離（chat_message.purpose、Phase16）**：資格試験（DAILY_FEEDBACK）と読書（DAILY_FEEDBACK_READING）の対話は、同一日の同一`daily_record`に混在しうる。会話自体はAiPurpose／ConversationScopeが異なるため`ai_conversation`側では自然に分離されるが、`chat_message`はdaily_record単位のフラットな一覧であるため、`purpose`列（データ構造編5.4）を追加し、対話履歴（`{{conversation_history}}`）への注入時にpurposeで絞り込むことで、用途間の文脈混入を防いでいる。AI対話の実行エンドポイントも`POST /records/{date}/chat`（資格試験）と`POST /records/{date}/reading-chat`（読書）に分離した（データ構造編6.2「AI対話エンドポイントの分離について」）。

### 21.2 読書進捗（BookRead）の算出

教材の派生値（総作業量・現在周回・残量・進捗率）が独立エンドポイントではなく`MaterialRead`に
埋め込まれているのと同じ方針で、書籍の派生値も独立エンドポイントを設けず`BookRead`
（`GET /goals/{id}`、`PATCH /books/{id}`等の応答）に埋め込む（データ構造編6.2、Phase15実装）。

```
remaining_days = book.due_date − T

last_reading_date = MAX(daily_record.record_date)
                     （対象bookのreading_logが存在する daily_record のうち）

current_streak = 21.3で定義する

page_progress（book.total_pages が設定されている場合のみ）：
    current_page = 直近の reading_log.current_page（NULLでない最新値）
    progress_rate = current_page / total_pages
```

日次ノルマ・実効速度・完了予測日・締切との乖離（第7〜10章）は算出しない。

### 21.3 連続記録日数（読書版）

```
T から遡り、対象bookのreading_logが存在する日が連続する日数。
```

13.4（連続報告日数）と異なり、バッファ日・除外日による中断除外を行わない。読書は日種別（第4章）による計画運用の対象外であり、資格試験の計画運用と独立させるためである（要件定義書R-64）。

### 21.4 想起記録の時系列注入

日次報告フィードバック（17.6の`{{recent_recalls}}`）および読了レポート（17.7の`{{reading_logs}}`）に注入する想起記録は、対象bookに属する `reading_log` を `record_date` の昇順で連結する。週次要約による圧縮（第15章）を経由しない（読書目標は週次要約を持たないため）。

両者で注入範囲が異なる。

| 用途 | 注入範囲 |
| --- | --- |
| `{{recent_recalls}}`（17.6） | 直近 `ai.reading_recall_recent_days`（既定14）日分のみ。日次対話は直近の文脈があれば十分であり、書籍の読書期間全体を毎回注入する必要はない |
| `{{reading_logs}}`（17.7） | 対象bookの全期間分。読了レポートは書籍全体を俯瞰する必要があるため |

プロンプトの段階的縮退（16.5）は資格試験と同じ仕組みをそのまま適用し、`{{reading_logs}}`が閾値を超える場合の安全弁とする。

---

## 22. 未決事項

| ID | 内容 | 決定期限 |
| --- | --- | --- |
| L-01 | AI基盤の入力上限の実測値。縮退閾値30000文字の妥当性 | 実装後の疎通確認時 |
| L-02 | 警告倍率1.20および超過日数3日の妥当性。運用実績をもとに調整する | 運用開始後 |
| L-03 | 実効速度のサンプル数下限3の妥当性 | 運用開始後 |
| L-04 | 複数目標が同時進行する場合の日次報告の粒度。表示面（ダッシュボード「本日のノルマ」・日次報告の実績入力欄）は教材別一覧に目標名を付与しグルーピング表示することで解消済み（データ構造編5.4）。日記欄も daily_goal_diary テーブルへの目標別分離により解消済み（データ構造編5.4、9a1c3e7d5b2fマイグレーション）。週次要約・ナレッジエクスポートで他目標の日記が混入していた潜在バグも本対応の副作用として解消した。「今日の一言」も目標ごとの独立生成（daily_message.goal_id、c7d391a6f0e5マイグレーション）により、順調な目標に引っ張られた楽観的な一言が生成される懸念を解消済み。カレンダー補助マーカーも目標別に区別して表示するよう解消済み（frontend resolveAuxiliaryMarkers.ts）。読書目標の実績（reading_log）はもとよりbook_idで分離済みのため、本対応と同様に目標種別を跨いだ混入は生じない | 解消済み |
| L-05 | プロンプト初期文面の調整。実際の応答品質を確認したうえで改訂する | 運用開始後（17.6・17.7の読書用プロンプトも対象に含める） |
| L-06 | NewtonX ADKの親メッセージ順序（parent_order）が実装バージョン（v0.10.5）で機能しない件。ADK側の改修状況を継続確認し、解消され次第16.3.1の暫定対応を見直す | Phase 5実装時に暫定対応を確定、以後は継続監視 |
| L-07 | 日次フィードバックの会話は複数目標にまたがる1本の共有スレッド（goal_id=NULL、意図的な既存設計）であり、ユーザーが同じ日に複数目標について行き来して質問した場合、AIがどちらの目標の話か混同するリスクがあった。DAILY_FEEDBACKプロンプトに「複数目標がある場合の注意」セクションを追加し、既存の目標別見出し（■ 目標名）を手がかりにAI自身が判断・確認するよう指示することで解消済み（b4c8e2f19a3dマイグレーション） | 解消済み |
| L-08 | 読書用アシスタント（AI-06・AI-07）の既定値選定。仕様書12章S-07と対応。Phase16で設定API（`ai.assistant_uid.daily_feedback_reading`／`goal_retrospective_reading`）からの変更を可能にする実装は完了したが、実際のUID値は未選定（空文字のまま） | 実環境でのPhase11相当の疎通確認時 |

---

*データ構造、テーブル定義、API構成は「設計書 データ構造編」を参照のこと。*
